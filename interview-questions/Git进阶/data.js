const QUESTIONS = [{"num": 1, "title": "Mac 下 git push 避免每次都输入用户名和密码的配置？", "difficulty": "简单", "difficulty_emoji": "🟢", "tags": "Git, Git进阶, Git操作", "answer": "在 Mac 下配置 Git 避免每次 push 输入密码，主要有两种主流方案：\n\n**方案一：使用 Git 凭证缓存**\n通过系统工具临时或永久存储账号密码。\n1. **临时缓存**（默认15分钟）：执行 `git config --global credential.helper cache`\n2. **永久缓存**：执行 `git config --global credential.helper store`，密码将明文保存在 `~/.git-credentials` 文件。\n*注意：此方法适用于 HTTPS 协议，安全性较低，建议在可信环境下使用。*\n\n**方案二：使用 SSH 密钥对（推荐）**\n更安全，无需传输密码。\n1. 生成密钥：终端执行 `ssh-keygen -t ed25519 -C \"your_email@example.com\"`，按提示操作。\n2. 添加公钥：复制 `~/.ssh/id_ed25519.pub` 内容，添加到 GitHub/GitLab 等平台的 SSH Keys 设置中。\n3. 切换协议：将仓库远程地址改为 SSH 格式（如 `git@github.com:用户名/仓库名.git`），之后即可免密推送。\n\n**总结**：\n- **安全便捷首选 SSH**：一次配置，长期有效，且避免密码明文存储。\n- **快速临时方案用缓存**：适合临时环境或对安全性要求不高的场景。\n- 配置完成后，通过 `git push` 测试即可。"}, {"num": 2, "title": "你使用过哪些分支系统？它们分别有什么优缺点？", "difficulty": "简单", "difficulty_emoji": "🟢", "tags": "Git, Git进阶, Git协作应用", "answer": "我在项目中主要使用过三种分支模型，各具特点：\n\n**1. Git Flow**  \n- **核心**：基于master/develop两个长期分支，功能开发在feature分支，发布通过release分支，紧急修复用hotfix。  \n- **优点**：流程严谨，适合版本发布周期较长的项目，易于管理并行开发和多版本维护。  \n- **缺点**：分支数量多，合并复杂度高，对于快速迭代的互联网项目可能显得冗长。\n\n**2. GitHub Flow**  \n- **核心**：只有master为长期分支，所有新功能通过feature分支开发，完成后发起Pull Request合并至master，并直接部署。  \n- **优点**：流程简单，鼓励持续集成和快速交付，适合敏捷开发和持续部署的团队。  \n- **缺点**：对自动化测试和部署要求高，不适合需要多版本长期维护的场景。\n\n**3. Trunk-Based Development（主干开发）**  \n- **核心**：开发者直接在主干（master）上频繁提交小变更，通过特性开关控制功能，结合短期特性分支（可选）。  \n- **优点**：最大限度减少合并冲突，加速集成，适合高频率发布的团队。  \n- **缺点**：对代码质量和团队协作要求极高，需要完善的特性开关和回滚机制。\n\n**总结**：我会根据团队规模、发布节奏和项目类型选择分支策略。例如，对外部发布产品可用Git Flow；内部SaaS服务倾向GitHub Flow；高度敏捷的小团队可尝试主干开发。关键在于匹配团队工作流并严格执行代码评审。"}, {"num": 3, "title": "在什么情况下应该使用 git stash 命令？", "difficulty": "简单", "difficulty_emoji": "🟢", "tags": "Git, Git进阶, Git操作", "answer": "git stash 用于临时保存工作目录中尚未提交的修改，将其恢复到上一次提交的干净状态。以下为主要使用场景：\n\n**一、核心使用场景**\n1. **紧急任务切换**：当前开发任务未完成且不便提交时，需立即切换分支处理线上问题。\n2. **代码同步与更新**：拉取远程更新或合并分支前，暂存本地未完成的修改以避免冲突。\n3. **临时实验与测试**：需要尝试不同实现方案时，可暂存当前修改以便随时恢复。\n\n**二、关键命令**\n- `git stash`：保存当前修改。\n- `git stash list`：查看所有暂存记录。\n- `git stash apply`：恢复最近的暂存（保留记录）。\n- `git stash pop`：恢复最近的暂存并删除记录。\n\n**三、注意事项**\n- 仅跟踪已被 Git 管理的文件，未跟踪文件需先使用 `git stash -u` 包含。\n- 长期未恢复的暂存可能导致代码冲突或过时，建议定期清理。\n- 多人协作时，应谨慎共享仅存在于本地 stash 的代码。"}, {"num": 4, "title": "如何在 Git 中撤销一个 git commit 提交？", "difficulty": "简单", "difficulty_emoji": "🟢", "tags": "Git, Git进阶, Git操作", "answer": "在 Git 中撤销 commit 主要有三种方式，选择取决于是否需要保留更改以及是否已 push：\n\n**1. 保留更改，仅撤销 commit**\n使用 `git reset --soft HEAD~1`。此命令将最后一次 commit 退回到暂存区（staged），所有文件修改内容均保留，适合 commit message 需修改或需追加代码的场景。\n\n**2. 彻底丢弃 commit 及所有更改**\n使用 `git reset --hard HEAD~1`。此命令不仅撤销 commit，还会将工作区和暂存区的修改全部清除，**务必谨慎使用**，适合完全废弃该次提交的情况。\n\n**3. 撤销已 push 到远程的 commit（生成新提交）**\n使用 `git revert <commit-hash>`。该命令会创建一个新的 commit，用于抵消指定 commit 的更改。这是公共分支（如 master）最安全的操作方式，避免了强制推送（`git push -f`）带来的协作问题。\n\n**总结**：本地修改未提交时，根据是否保留代码选择 `--soft` 或 `--hard`；已推送的公共提交，使用 `revert` 保证记录清晰可追溯。"}, {"num": 5, "title": "适用于 Linux 的图形 Git 客户端有哪些？", "difficulty": "简单", "difficulty_emoji": "🟢", "tags": "Git, Git进阶, Git操作", "answer": "Linux 上常用的图形化 Git 客户端主要有以下几类，可以根据需求选择：\n\n**主流跨平台客户端：**\n1. **GitKraken**：界面现代美观，操作直观，集成了合并工具、交互式变基等高级功能，支持 GitHub、GitLab 等平台，有免费版和专业版。\n2. **SmartGit**：功能全面，支持 Git 与 SVN，工作流配置灵活，适合需要深度操作的专业开发者，提供非商业免费授权。\n\n**轻量级或原生工具：**\n3. **Sublime Merge**：与 Sublime Text 同系列，界面简洁高效，搜索性能强大，适合追求速度和简洁体验的用户。\n4. **Git Cola**：基于 PyQt 开发，轻量快速，支持常见 Git 操作，适合偏好原生 Linux 风格的用户。\n5. **QGit**：使用 Qt 编写，专注于查看历史和提交，功能简单直接。\n\n**环境集成工具：**\n- **Nautilus/Thunar 集成插件**：如 Gitg 或 RabbitVCS，可直接在文件管理器中操作 Git。\n- **IDE 内置工具**：如 VS Code、JetBrains 系列的 Git 集成已足够强大，可替代独立客户端。\n\n选择时建议考虑团队协作、操作习惯和功能深度。多数工具提供免费试用，可实际体验后决定。"}, {"num": 6, "title": "Git Flow 工作流程是什么？Git Flow 有哪些优势？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git协作应用", "answer": "Git Flow 是一种基于 Git 的版本控制工作流程模型，它通过规范化的分支管理策略，帮助团队高效协作、稳定发布软件。其核心在于定义五种分支角色：主分支（master）存放生产环境代码，开发分支（develop）作为功能集成分支，以及辅助分支如功能分支（feature）、发布分支（release）和热修复分支（hotfix）。工作流程通常为：新功能开发从 develop 分支创建 feature 分支，完成后合并回 develop；准备发布时从 develop 创建 release 分支进行测试，通过后合并到 master 并打标签，同时同步回 develop；紧急修复则从 master 创建 hotfix 分支，修复后合并到 master 和 develop。\n\nGit Flow 的优势主要体现在：第一，结构清晰，分支角色明确，减少协作混乱；第二，稳定性高，master 分支始终保持可部署状态，develop 分支确保集成测试；第三，支持并行开发，多个功能分支可同时进行，提升效率；第四，便于版本管理和问题追踪，"}, {"num": 7, "title": "Git Flow 有哪些分支类型？作用分别是什么？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git协作应用", "answer": "Git Flow 是一种流行的 Git 分支管理模型，主要有以下五种分支类型，各司其职：\n\n1.  **主分支（Master）**：存放正式发布的历史版本，每个提交都对应一个可发布的版本号。它非常稳定，只能从其他分支合并而来，不能直接在上面开发。\n2.  **开发分支（Develop）**：作为日常开发的集成分支，包含最新的已交付功能代码。所有新功能的开发都基于此分支，完成后也最终合并回此分支。\n3.  **功能分支（Feature）**：用于开发新功能。它从 `Develop` 分支创建，开发完成后合并回 `Develop`。其命名通常为 `feature/xxx`。\n4.  **预发布分支（Release）**：当 `Develop` 分支积累足够功能，准备发布新版本时创建。它用于版本发布前的最后测试和修复，完成后同时合并到 `Master`（并打标签）和 `Develop`。\n5.  **热修复分支（Hotfix）**：用于紧急修复线上 `Master` 分支的严重问题。它从 `Master` 创建，修复后同时合并回 `Master` 和 `Develop`，确保修复同步。\n\n**核心作用**：通过清晰的职责划分，Git Flow 实现了并行开发、版本发布和紧急修复的有序管理，特别适合有明确发布周期的项目，能有效降低协作冲突，保证主分支的稳定性。"}, {"num": 8, "title": "Git merge 支持哪些合并策略？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "Git merge 主要支持以下几种合并策略：\n\n1. **Fast-forward**：当目标分支可直接移动到当前分支时，Git 会直接移动指针，不产生合并提交。适用于当前分支没有新提交的情况。\n\n2. **Recursive（默认）**：处理多个共同祖先的合并场景，通过递归算法解决冲突。适用于大多数常规分支合并，尤其是存在分叉的情况。\n\n3. **Octopus**：支持同时合并多个分支，但不解决冲突。常用于将多个特性分支合并到主分支，适合自动化流程中合并已完成的功能。\n\n4. **Ours**：强制以当前分支内容为准，忽略其他分支的修改。适用于需要废弃某分支改动但保留合并记录的场景。\n\n5. **Subtree**：将另一个仓库作为子目录合并，适用于管理项目依赖或子项目。\n\n面试中可根据团队协作场景举例：日常开发常用 `recursive` 策略；多分支集成时可用 `octopus`；而 `fast-forward` 多用于保持线性历史。实际使用时可通过 `git merge -s <策略名>` 指定策略。"}, {"num": 9, "title": "Git 中 Forking 工作流程的优点是什么？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git概念, Git协作应用", "answer": "Forking工作流程的核心在于为每位贡献者提供一个独立的、完整的远程仓库副本（即Fork），其优点主要体现在以下几个方面：\n\n首先，它实现了**高度隔离与并行开发**。每位开发者在自己的Fork仓库中自由创建分支、进行实验和提交代码，完全不会干扰主项目的稳定性，特别适合开源社区等多人协作场景。\n\n其次，它建立了**清晰、可控的代码集成流程**。贡献者通过向主项目提交Pull Request来发起代码合并请求。维护者可以在此阶段对代码进行**充分的评审、讨论和测试**，确保代码质量，只有符合标准的代码才会被合并。\n\n再者，它有效**降低了权限管理的风险与复杂度**。贡献者无需在主仓库中拥有写入权限，只需拥有自己Fork的权限即可。这极大简化了权限配置，并保护了主仓库的安全。\n\n最后，它支持**灵活、异步的协作模式**。开发者可以根据自己的节奏在本地或Fork仓库中工作，完成后一次性发起合并，非常适合分布式团队和跨时区协作。\n\n总之，Forking工作流程通过“独立副本+PR审核”的机制，在促进开放协作的同时，保障了代码质量和项目主干的安全稳定。"}, {"num": 10, "title": "Git 中 HEAD、工作树和索引有什么区别？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git概念", "answer": "**HEAD、工作树和索引（暂存区）是Git工作流程中的三个核心概念，它们共同管理着代码的版本状态。**\n\n1.  **HEAD**：是一个特殊的指针，**指向当前所在的分支或提交**。它决定了你接下来执行提交（`git commit`）时，新的提交对象将挂载在哪个分支上。通过`git checkout`切换分支时，HEAD会随之移动。\n\n2.  **工作树（工作目录）**：即项目文件夹，是你**直接进行文件编辑和修改的地方**。这里的文件改动（增删改）都是临时的、未被Git跟踪的状态。\n\n3.  **索引（暂存区）**：是一个过渡区域，也叫“暂存区”。它像一个购物车，用于**记录你下次打算提交的修改内容**。你需要用`git add`命令将工作树的改动放入索引。只有暂存区的内容才会被下一次`git commit`永久保存到Git仓库中。\n\n**核心协作流程**：你在**工作树**中修改文件，用`git add`将选定的改动放入**索引**，再用`git commit`将索引的内容永久记录为一次提交，同时**HEAD**会更新指向这个新的提交。"}, {"num": 11, "title": "Git 中复原 revert 和重设 reset 有什么区别？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "Git 中 revert（复原）和 reset（重设）是两个用于撤销更改的核心命令，主要区别在于操作方式和影响：\n\n**revert（复原）**  \n通过创建一个新的提交来撤销指定提交的更改，历史记录会保留原有提交，并新增一个“反向提交”。这种方式不会改变项目历史，适合团队协作中安全撤销错误提交。\n\n**reset（重设）**  \n直接将分支指针移动到指定提交，之后的提交会被“丢弃”（或暂存）。它会修改项目历史，可能影响已推送的提交。reset 有三种模式：\n- `--soft`：仅移动 HEAD，保留工作区和暂存区。\n- `--mixed`（默认）：移动 HEAD 并重置暂存区，但保留工作区。\n- `--hard`：彻底重置工作区、暂存区和提交历史。\n\n**核心区别**  \n1. **历史处理**：revert 保留历史，reset 会重写历史。  \n2. **使用场景**：revert 适合公共分支的安全撤销；reset 适合本地分支的整理或修正未推送的提交。  \n3. **风险性**：revert 对团队更安全，reset 的 `--hard` 模式可能导致数据丢失。\n\n**面试建议**  \n回答时可强调：revert 是“添加修正提交”，reset 是“时光倒流”，并结合协作场景说明选择依据。"}, {"num": 12, "title": "Git 中如何回退到以前的提交记录？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "在Git中回退到以前的提交记录主要通过`git reset`和`git revert`两种命令实现，具体选择取决于是否需要保留历史记录。\n\n**1. 使用`git reset`**  \n此命令用于将当前分支重置到指定提交，有三种模式：  \n- **soft**：仅移动HEAD指针，暂存区和工作目录的更改保持不变，适合重新提交但不丢失工作内容。  \n- **mixed**（默认）：移动HEAD并重置暂存区，但工作目录更改保留，适合取消暂存文件。  \n- **hard**：移动HEAD并重置暂存区和工作目录，完全丢弃更改，适合彻底回退到旧状态。  \n命令示例：`git reset --hard <commit-hash>`。\n\n**2. 使用`git revert`**  \n此命令通过创建一个新的提交来撤销指定提交的更改，不会修改历史，适合团队协作或公共分支。命令示例：`git revert <commit-hash>`。\n\n**3. 其他辅助命令**  \n- `git reflog`：查看所有操作记录，可找回被重置或丢失的提交，用作安全网。  \n- `git checkout`：仅查看旧提交"}, {"num": 13, "title": "Git 中如何处理合并冲突后的 commit message？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "处理 Git 合并冲突后的 commit message，主要有以下两种方式：\n\n**1. 使用默认 message，或进行优化**\n解决冲突并执行 `git add` 后，直接运行 `git commit`，Git 会自动生成一条类似 `Merge branch 'xxx'` 的默认 message。如果需要补充说明，可以在该命令后使用 `-m \"你的描述\"` 参数来自定义。更推荐的做法是运行 `git commit`（不带 `-m`），这会打开编辑器，让你基于默认的模板（包含合并的来源分支等信息）进行补充和修改。\n\n**2. 后续修改 message**\n如果提交后才发现 message 不理想，可以使用 `git commit --amend` 命令，对最近一次的 commit message 进行修正。\n\n**最佳实践建议**\n合并冲突的 commit message 应当清晰说明：\n*   合并的来源分支。\n*   冲突的主要原因或背景（如功能整合、同步上游）。\n*   解决冲突的简要思路或关键决策。\n清晰的 message 有助于团队成员理解代码历史，便于未来的代码审查和问题追溯。"}, {"num": 14, "title": "Git 中如何处理文件的换行符问题？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "在Git中处理换行符问题主要通过以下三种方式：\n\n**1. 全局配置core.autocrlf**\n- Windows：`git config --global core.autocrlf true`（提交时转换为LF，签出时转换为CRLF）\n- Linux/Mac：`git config --global core.autocrlf input`（提交时转换为LF，签出时保持原样）\n\n**2. 使用.gitattributes文件（推荐）**\n在仓库根目录创建`.gitattributes`文件，按文件类型指定换行符规则：\n```\n* text=auto\n*.sh text eol=lf\n*.bat text eol=crlf\n*.bin binary\n```\n此文件应提交到仓库，确保团队统一规范。\n\n**3. 转换现有文件**\n- 使用`dos2unix`/`unix2dos`工具批量转换\n- 通过`git add --renormalize .`重新规范化已跟踪文件\n\n**注意事项**：\n- 团队需统一换行符规范\n- `.gitattributes`优先级高于全局配置\n- 二进制文件应标记为binary避免转换\n\n面试中可强调：核心是通过配置实现自动化转换，减少手动操作和跨平台冲突，`.gitattributes`是最推荐的仓库级解决方案。"}, {"num": 15, "title": "Git 中如何强制切换到另一个分支？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "在 Git 中，强制切换到另一个分支通常用于**丢弃当前工作区的所有未提交更改（包括暂存的和未暂存的）**，并强制切换到目标分支。其核心命令是使用 `checkout` 或 `switch` 命令的强制选项。\n\n具体操作如下：\n1.  **使用 `git checkout -f <branch_name>`** 或 **`git switch -f <branch_name>`**（更现代的命令）。\n    例如：`git checkout -f main` 将强制切换到 main 分支，并清除所有本地未提交的修改。\n\n**适用场景与原理**：\n此操作适用于你确信当前工作区的所有更改都不再需要，并且当前分支的本地状态阻碍了切换（例如，修改了目标分支也存在的文件）的情况。它通过重置工作区和暂存区，使其与目标分支的最新提交保持一致，从而绕过 Git 的安全检查。\n\n**重要注意事项（面试加分点）**：\n这是一个**破坏性操作**，无法撤销。执行前必须确认所有未提交的更改都是可以丢失的。更安全的做法是使用 `git stash` 暂存更改，或先进行一次提交。在团队协作中，应谨慎使用，以免丢失他人可能需要的工作。理解其背后是重置工作树，有助于更好地掌握 Git 的状态管理。"}, {"num": 16, "title": "Git 中如何恢复被误删的文件？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "在 Git 中恢复被误删的文件，主要依赖版本控制系统的快照特性。根据文件删除的不同阶段，可以采用以下方法：\n\n首先，如果文件被删除但尚未提交到暂存区（即仅从工作区删除），可以使用 `git checkout -- <文件名>` 或 Git 2.23+ 版本的 `git restore <文件名>` 命令，直接从最近一次提交中恢复文件到工作区。\n\n其次，如果文件已通过 `git add` 添加到暂存区但被误删，可以通过 `git reset HEAD <文件名>` 将文件从暂存区移出，再使用上述 `git checkout` 或 `git restore` 恢复工作区文件。\n\n更复杂的情况是文件已提交但后来被误删。此时，如果删除操作还未推送，可以使用 `git revert` 撤销最近的删除提交，或者用 `git reset --hard HEAD~1` 回退到前一个提交（注意这会影响其他更改）。如果删除已推送到远程仓库"}, {"num": 17, "title": "Git 中如何撤销工作区中未提交到暂存区的更改？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "在 Git 中，若要撤销工作区中尚未添加到暂存区（未执行 `git add`）的更改，核心命令是使用 `git checkout`。\n\n**具体操作：**\n- 撤销对**单个文件**的修改：`git checkout -- <文件名>`\n- 撤销对**所有文件**的修改：`git checkout -- .`\n\n**原理与说明：**\n该命令的实质是将**暂存区的版本**覆盖到工作区。这意味着：\n1. **目标明确**：仅影响工作区中已修改但未暂存的文件。\n2. **操作可逆**：执行后，文件内容将恢复到上一次暂存或提交时的状态。\n3. **不可逆警告**：此操作会**永久丢弃**工作区中的更改，因此执行前务必确认这些更改确实不再需要。\n\n**最佳实践：**\n在执行前，可先用 `git status` 确认文件状态，确保目标文件仅显示为 \"Changes not staged for commit\"。\n\n**补充：**\n在 Git 2.23 版本后，推荐使用语义更清晰的命令 `git restore <文件名>` 来达到相同目的，这是官方推荐的新式用法。\n\n总之，`git checkout --`（或 `git restore`）是撤销工作区未暂存修改的直接有效方式，操作时需谨慎，避免意外数据丢失。"}, {"num": 18, "title": "Git 中如何查看并解决合并冲突？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "Git 合并冲突通常发生在 `git merge` 或 `git rebase` 时，当对同一文件的同一部分进行了不同修改，Git 无法自动合并。\n\n**1. 查看冲突：**\n执行合并命令后，Git 会提示冲突文件。使用 `git status` 可查看具体是哪些文件处于 “both modified” 状态。\n\n**2. 解决步骤：**\n*   **编辑文件：** 打开冲突文件，找到被 `<<<<<<< HEAD`、`=======` 和 `>>>>>>> branch-name` 标记出的冲突块。仔细分析，决定保留哪部分修改或手动合并两者。\n*   **标记解决：** 编辑完成后，使用 `git add <file>` 将文件标记为已解决。\n*   **提交更改：** 使用 `git commit` 完成合并提交。Git 会自动生成一个默认的合并提交信息。\n\n**核心要点：** 解决冲突的本质是人工决策，通过编辑、暂存、提交这三个步骤，将代码库统一到一个新的、兼容的状态。建议在团队协作中及时沟通，以减少复杂冲突。"}, {"num": 19, "title": "Git 中如何标记和管理标签？常用命令有哪些？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "## Git 标签管理\n\n**标签（Tag）** 用于标记特定的提交，通常用于标记版本发布点（如 v1.0、v2.0），方便后续快速定位和回溯。\n\n### 两种标签类型\n\n| 类型 | 特点 |\n|------|------|\n| **轻量标签** | 仅指向提交的指针，不包含额外信息 |\n| **附注标签** | 包含标签者信息、日期、注释，建议用于正式发布 |\n\n### 常用命令\n\n```bash\n# 创建轻量标签\ngit tag v1.0\n\n# 创建附注标签（推荐）\ngit tag -a v1.0 -m \"版本1.0发布\"\n\n# 给历史提交打标签\ngit tag -a v0.9 <commit-id>\n\n# 查看所有标签\ngit tag\ngit tag -l \"v1.*\"    # 模糊匹配\n\n# 查看标签详情\ngit show v1.0\n\n# 推送标签到远程\ngit push origin v1.0      # 推送单个\ngit push origin --tags     # 推送所有标签\n\n# 删除本地标签\ngit tag -d v1.0\n\n# 删除远程标签\ngit push origin --delete v1.0\n```\n\n### 面试加分点\n\n- 实际开发中，**附注标签**更常用，因为它记录了完整的版本信息。\n- `git push` 默认不会推送标签，需要显式使用 `--tags` 参数。\n- 标签可作为 `git checkout` 或 `git log` 的引用，便于版本切换和追溯。"}, {"num": 20, "title": "Git 中如何管理远程仓库？常用命令有哪些？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作, Git协作应用", "answer": "在Git中，管理远程仓库主要通过`git remote`系列命令完成。核心操作如下：\n\n1.  **添加远程仓库**：使用 `git remote add <远程名> <仓库URL>` 命令将本地仓库与远程服务器（如GitHub）关联。通常默认远程名为`origin`。\n2.  **查看远程仓库信息**：`git remote -v` 可列出所有远程仓库的名称和对应的URL。\n3.  **数据交互**：\n    *   **推送**：`git push <远程名> <本地分支>` 将本地分支的更新推送到远程仓库。常用 `git push origin main`。\n    *   **拉取**：`git fetch` 仅下载远程的最新提交和分支信息，但不自动合并到本地工作区。`git pull` 相当于 `git fetch` + `git merge`，会获取远程分支并尝试合并到当前分支。\n4.  **其他管理**：\n    *   `git remote rename <旧名> <新名>` 重命名远程仓库。\n    *   `git remote remove <远程名>` 移除一个远程仓库。\n\n管理流程示例：首先用`add`关联远程仓库，通过`push`推送代码，用`fetch`或`pull`同步远程更新。可通过`-v`选项检查配置，确保远程地址正确。合理设置跟踪分支可简化命令。"}, {"num": 21, "title": "Git 中常见的分支策略有哪些？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git协作应用", "answer": "## Git 常见分支策略\n\n**一、Git Flow**\n\n经典的分支模型，包含五类分支：\n- **master/main**：存放稳定发布版本\n- **develop**：开发主分支，集成最新开发功能\n- **feature/**：功能分支，从 develop 拉取，完成后合并回 develop\n- **release/**：预发布分支，用于版本发布前的测试和修复\n- **hotfix/**：紧急修复分支，从 master 拉取，修复后同时合并回 master 和 develop\n\n适合版本发布周期较长、需要多版本并行维护的项目。\n\n**二、GitHub Flow**\n\n流程简化，只有 main 和 feature 分支。开发者从 main 创建功能分支，开发完成后提交 Pull Request，经代码审查后合并回 main，随后可直接部署。强调持续部署，适合 SaaS 类项目。\n\n**三、GitLab Flow**\n\n在 GitHub Flow 基础上增加了环境分支（如 staging、production）和发布分支。代码先合并到 main，再逐级推送到对应环境分支，实现环境隔离和可控发布。\n\n**四、主干开发（Trunk-Based Development）**\n\n所有开发者直接在 main 分支上工作，配合短生命周期的功能分支（通常不超过一天）。强调频繁集成和特性开关（Feature Flag），适合 CI/CD 成熟、团队协作能力强的团队。\n\n**选型建议**：小型团队或持续交付项目推荐 GitHub Flow；大型企业级项目或需要严格版本管理可选 Git Flow；追求快速迭代、DevOps 成熟的团队适合主干开发模式。"}, {"num": 22, "title": "Git 中有哪些合并的方法？它们有什么区别？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作, Git概念", "answer": "Git 中主要的合并方法是**普通合并**和**变基合并**。\n\n**1. 普通合并**\n*   **操作**：使用 `git merge` 命令。\n*   **原理**：将目标分支的最新提交与当前分支的最新提交进行三方合并，并自动生成一个全新的**合并提交**。这个提交拥有两个父提交。\n*   **优点**：保留了完整的分支历史和合并轨迹，真实反映开发时间线，不改变历史提交的哈希值。\n*   **缺点**：历史记录会产生分叉和合并点，对于频繁更新的功能分支，历史可能显得复杂。\n*   **适用场景**：合并公共分支（如将 `feature` 分支合并回 `main`）。\n\n**2. 变基合并**\n*   **操作**：使用 `git rebase` 命令。\n*   **原理**：将当前分支的提交“摘取”下来，在目标分支的最新提交之后**重新逐个应用**，从而“变基”到新的基础上。这会**重写提交历史**（生成新的提交哈希）。\n*   **优点**：能生成一个**线性、整洁**的提交历史，避免不必要的合并提交。\n*   **缺点**：重写了历史，已推送到远程仓库的提交变基后可能引发冲突，对协作有风险。\n*   **适用场景**：整理个人本地功能分支的提交，使其基于最新的主分支之上，再用于合并或推送。\n\n**核心区别**：普通合并是**记录“合并点”**，保留分叉历史；变基合并是**改写“分叉历史”**，使其线性化。简单比喻，合并是画一个路口连接两条路，变基是将一条路的起点接到另一条路的终点上。"}, {"num": 23, "title": "Git 中有哪些合并策略，比如 recursive 和 ours？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "Git 提供了多种合并策略以适应不同场景，常见策略如下：\n\n1.  **`recursive`（现更名为 `ort`）**：这是**默认策略**（Git 2.33 之前）。它通过三路合并算法处理两个分支，当遇到复杂冲突时，会递归寻找共同祖先进行合并。适用于大多数常规分支合并。\n2.  **`ours`**：合并结果**始终采用当前分支（ours）的内容**，完全忽略其他分支的修改。注意：这与合并时的 `-X ours` 选项不同，后者是在冲突时采用当前分支的解决方案。\n3.  **`octopus`**：用于**合并两个以上的分支**（例如同时合并多个特性分支）。它简化了多分支合并的过程，但无法处理需要手动解决的冲突。\n4.  **`subtree`**：适用于**将一个项目作为另一个项目的子目录**进行合并或集成，它会自动调整路径。\n\n**选择建议**：日常开发使用默认的 `ort` 策略即可。若需强制保留当前分支所有改动，可选用 `ours` 策略。合并多个无冲突的分支时，可考虑 `octopus`。进行项目嵌套时，则使用 `subtree`。"}, {"num": 24, "title": "Git 中的 commit amend 命令是什么？如何使用？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "Git中的commit amend命令用于修改最近一次提交的内容或消息。它允许开发者在不创建新提交的情况下，对已有提交进行更正，常用于修复小错误或添加遗漏文件。这有助于保持提交历史的整洁。\n\n使用方法如下：首先，如果要修改提交消息，直接运行`git commit --amend -m \"新消息\"`。其次，如果要添加或修改文件，先使用`git add`将文件暂存，然后执行`git commit --amend`，Git会将暂存区的更改合并到上一个提交中。例如，提交后发现少了一个文件，可以添加后运行此命令来更新提交。\n\n注意事项包括：amend会改变提交的SHA哈希值，相当于重写历史。如果提交已推送到远程仓库，强制推送（`git push --force`）才能同步更改，但这可能影响协作，应谨慎使用。在团队环境中，最好只修改未推送的提交。总之，commit amend是一个高效的工具，但需根据场景合理运用。"}, {"num": 25, "title": "Git 中的 filter-branch 命令是什么？有什么作用？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "Git 的 `filter-branch` 是一个用于**重写提交历史**的命令。它的核心作用是**批量修改历史提交中的特定内容**，常用于清理或修正仓库历史。\n\n其主要功能包括：\n1.  **重写提交信息**：修改历史提交的作者、提交者信息或提交说明。\n2.  **修改文件内容**：在所有历史提交中过滤（添加、删除或替换）文件。\n3.  **提取子项目**：将仓库中的某个子目录分离为独立仓库的历史。\n\n典型使用场景包括：**从历史中永久删除敏感文件**（如误提交的密码、密钥）、**修复错误的作者信息**或**大规模重命名文件**。\n\n**重要注意事项**：\n- 它会**彻底重写项目历史**，生成新的提交哈希，属于破坏性操作。\n- 执行前**必须备份仓库**。\n- 由于性能和复杂性问题，官方推荐使用更高效的替代工具 `git filter-repo`（或专门处理大文件的 `BFG Repo-Cleaner`）。\n- 在共享仓库中重写历史后，需要所有协作者强制同步，应谨慎使用。"}, {"num": 26, "title": "Git 中的 merge --squash 选项有什么作用？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "`git merge --squash` 的核心作用是将一个分支上的多个提交“压缩”成一个，然后再合并到目标分支。其主要价值在于保持项目历史记录的清晰和线性。\n\n具体来说，当你在一个特性分支上进行了多次零散提交，在合并回主分支时，使用 `--squash` 选项会将所有变更暂存，但不会自动产生合并提交。你需要手动执行 `git commit`，从而生成一个单一的、概括性的提交记录。这避免了主分支上出现过多琐碎的、实验性的提交，使主线历史更整洁，便于追溯和回滚。\n\n典型应用场景包括：\n1.  **合并功能分支**：将一个功能的所有开发工作以“一个提交”的形式整合到主干。\n2.  **清理提交历史**：合并前未整理分支提交时，用此选项快速整合。\n3.  **保持线性历史**：适用于推崇“快进式”或线性提交历史的团队工作流。\n\n需要注意的是，`--squash` 会丢弃源分支的提交历史，且合并后需手动解决冲突并提交。操作后通常建议删除已合并的源分支。"}, {"num": 27, "title": "Git 中的 patch 模式是什么？如何使用 git add -p 命令？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "Git 中的 patch 模式是指交互式暂存功能，允许用户将工作目录中的修改拆分为多个代码块（hunk），并选择性地将它们添加到暂存区，而不是暂存整个文件。这有助于精细控制提交内容，使版本历史更清晰。\n\n使用 `git add -p` 命令即可进入 patch 模式。执行后，Git 会逐段显示未暂存的修改，并提示用户选择操作。常用选项包括：输入 `y` 暂存当前块，`n` 跳过，`s` 将当前块分割为更小的块以便进一步选择，`e` 手动编辑块，以及 `q` 退出。通过这种方式，开发者可以精确管理哪些修改被纳入下次提交，适用于部分代码修改或调试场景，提升提交的逻辑性和可读性。"}, {"num": 28, "title": "Git 中的 rebase --onto 命令有什么作用？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "`git rebase --onto <newbase> <oldbase> <branch>` 命令的核心作用是将指定分支（`<branch>`）在旧基准（`<oldbase>`）之后提交的提交，“移植”到新的基准（`<newbase>`）之上。\n\n它比普通的 `git rebase` 更灵活。主要应用于两个经典场景：\n\n1.  **转移分支基础**：当特性分支（如`feature`）是从一个旧的发布分支（如`release-v1`）上创建的，但你希望将其变基到最新的主干（如`main`）上继续开发。可以使用 `git rebase --onto main release-v1 feature`。这会精确地将 `feature` 中不属于 `release-v1` 的提交重放到 `main` 的顶端。\n\n2.  **拆分提交历史**：可以将一个分支上的一段连续提交摘取出来，应用到另一个完全不同的基础上。例如，将 `feature` 分支中从提交`A`之后到`B`之前的提交，重放到 `main` 上，可以使用 `git rebase --onto main A B`。\n\n总之，`rebase --onto` 提供了对提交历史“手术刀”式的精确控制，是处理复杂分支关系和整理提交历史的强大工具，常用于维护干净、线性的项目历史。"}, {"num": 29, "title": "Git 中的 stash list 和 stash show 命令有什么区别？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "在Git中，stash命令用于临时保存工作目录的修改，方便切换任务。stash list和stash show是常用的子命令，但功能不同。\n\nstash list命令列出所有已保存的stash条目。执行后，会显示一个列表，每个条目包含唯一标识符（如stash@{0}）和保存时的描述信息（默认基于分支和提交）。这提供了stash栈的概览，帮助用户快速识别可用暂存项。\n\nstash show命令用于查看特定stash条目的详细内容。默认显示该stash与保存基准点之间的代码差异，类似git diff输出。可以指定具体条目（如stash show stash@{0}），并使用选项如-p获取完整差异。\n\n核心区别在于：stash list是概览工具，列出所有stash；stash show是细节工具，展示单个"}, {"num": 30, "title": "Git 中的 submodule update 命令有什么作用？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "`git submodule update` 命令主要用于**初始化和更新子模块**，确保主项目引用的子模块代码与主项目记录的提交版本保持一致。\n\n**核心作用：**\n\n1. **同步子模块版本**：当主项目克隆下来后，子模块目录默认是空的。执行该命令会根据 `.gitmodules` 文件和父仓库中记录的子模块 commit hash，将子模块切换到指定的提交版本。\n\n2. **初始化子模块**：配合 `--init` 参数，可以完成子模块的初始化和更新，相当于先执行 `git submodule init` 再执行 `git submodule update`。\n\n**常用参数：**\n\n- `--init`：初始化尚未初始化的子模块\n- `--recursive`：递归处理嵌套的子模块\n- `--remote`：使用子模块远程分支的最新提交，而非主项目记录的版本\n- `--force`：强制签出，丢弃本地修改\n\n**典型使用场景：**\n\n```bash\n# 克隆项目后初始化并更新所有子模块\ngit clone --recurse-submodules <repo_url>\n\n# 或者分步执行\ngit submodule update --init --recursive\n\n# 拉取主项目后，同步子模块到最新记录版本\ngit submodule update --remote\n```\n\n**面试加分点：** 子模块本质上是将外部仓库固定在某个 commit 上，`submodule update` 保证了团队协作时所有人使用一致的依赖版本，避免因依赖版本差异导致的兼容性问题。"}, {"num": 31, "title": "Git 中的 subtree 子树是什么？如何使用？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "Git subtree 是一种将一个仓库作为子目录嵌入另一个仓库的管理方式，用于整合外部项目或模块。它允许在不破坏历史记录的前提下，将子项目的代码和提交记录合并到主项目中。\n\n**核心使用场景：**\n1.  **项目依赖管理**：替代Git子模块，更适合需要对依赖代码进行修改或同步的场景。\n2.  **代码合并与复用**：将公共库或组件仓库作为子目录集成到多个项目中，方便统一管理和更新。\n\n**基本操作流程：**\n1.  **添加子树**：使用 `git subtree add` 命令将外部仓库添加为子目录。例如：`git subtree add --prefix=<子目录名> <仓库URL> <分支> --squash`（`--squash`将历史合并为一个提交）。\n2.  **更新子树**：使用 `git subtree pull` 从远程仓库拉取最新代码并合并。\n3.  **推送更改**：若子树有修改，使用 `git subtree push` 将改动推回原始仓库。\n\n**优点：**\n- 所有代码集中在一个仓库，克隆和协作更简单。\n- 对子目录的修改可以直接提交，便于定制化。\n\n**缺点：**\n- 仓库体积可能增大。\n- 分支管理相对复杂，需要熟悉特定命令。\n\n在面试中可强调，subtree适合需要深度集成且可能修改依赖代码的中小型项目，而子模块更适合仅需引用的大型项目。"}, {"num": 32, "title": "Git 中的 theirs 选项是什么？如何用 theirs 解决合并冲突？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "`theirs` 选项是 Git 在合并（merge）或变基（rebase）操作中用于解决冲突的一个策略。它表示“接受对方分支的更改”。具体来说，在合并冲突中，选择 `theirs` 意味着保留被合并分支（即正在引入的分支）的内容，而放弃当前分支的对应更改。\n\n**如何用 `theirs` 解决合并冲突？**\n有两种常用方式：\n1. **自动合并时使用 `-X theirs` 策略**：执行 `git merge -X theirs <branch>`，Git 会在合并过程中遇到冲突时自动采用对方分支的版本。\n2. **手动处理特定文件**：先使用 `git checkout --theirs <file>` 将冲突文件替换为对方分支的内容，然后执行 `git add` 标记为已解决。\n\n**注意**：在 `git rebase` 中，`ours` 和 `theirs` 的含义与合并相反。变基时，`theirs` 指的是当前正在变基的分支的更改，而 `ours` 则是目标基底分支的更改。因此，在变基中使用 `theirs` 会保留当前分支的修改。理解这一区别有助于在不同场景下正确应用策略。"}, {"num": 33, "title": "Git 中的交互式 rebase 是什么？如何使用？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "交互式 rebase 是 Git 中一种强大的历史编辑工具，允许开发者在 rebase 操作过程中，对提交记录进行**修改、合并、重新排序或删除**。它常用于在共享或合并代码前，整理本地提交历史，使其更清晰、逻辑更连贯。\n\n**使用方法：**\n1.  **启动命令**：执行 `git rebase -i <目标分支或提交哈希>`（如 `git rebase -i HEAD~3` 编辑最近 3 次提交）。Git 会打开一个文本编辑器，列出选中的提交记录。\n2.  **编辑提交指令**：在每条提交前，默认指令是 `pick`（保留提交）。可将其改为：\n    *   `reword`：保留提交但修改提交信息。\n    *   `squash`：将该提交与前一个提交合并，并合并提交信息。\n    *   `fixup`：与 `squash` 类似，但丢弃该提交的信息。\n    *   `drop`：删除该提交。\n3.  **保存并执行**：编辑并保存文件后，Git 会按新的指令顺序执行 rebase。若过程中需修改提交信息，Git 会再次打开编辑器。\n4.  **完成操作**：所有步骤完成后，提交历史将被重写。\n\n**核心要点与注意事项：**\n*   **主要用途**：清理提交信息、合并琐碎提交（如 “修复笔误”）、调整提交顺序，使项目历史更易于审查。\n*   **关键原则**：**永远不要对已推送到公共仓库的提交进行交互式 rebase**，因为这会重写历史，影响其他协作者。\n*   **优势**：相比 `git merge`，能创造更线性、整洁的提交历史，是高级 Git 工作流的重要部分。"}, {"num": 34, "title": "Git 中的分支策略是什么？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git协作应用", "answer": "Git中的分支策略是团队协作中管理代码变更的规范流程。常见策略有两种：\n\n一是Git Flow，适合有明确版本发布周期的项目。它包含长期分支（main/master、develop）和临时分支（feature、release、hotfix）。功能开发在feature分支进行，完成后合并到develop；发布时从develop创建release分支，测试稳定后合并到main并打标签，同时合并回develop。\n\n二是GitHub Flow，更轻量，适合持续部署的团队。它只有main一个长期分支，所有开发在独立的功能分支进行，完成后通过Pull Request请求合并到main，经代码审查和自动化测试后即部署。\n\n选择策略需考虑团队规模、发布频率和协作复杂度。核心原则是：保持main分支稳定、使用短生命周期的功能分支、通过代码审查保证质量。合理的分支策略能提升协作效率，降低集成风险。"}, {"num": 35, "title": "Git 中的裸存储库是什么？它有什么作用？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git概念", "answer": "裸存储库（Bare Repository）是一个没有工作目录（Working Directory）的Git仓库。它只包含 `.git` 文件夹下的所有内容（如对象数据库、引用等），但没有被版本控制的实际项目文件。通常通过 `git init --bare` 或 `git clone --bare` 命令创建。\n\n其核心作用在于作为**中央服务器或协作中心**。主要特点和作用如下：\n\n1.  **纯粹的存储中心**：它只管理版本历史，不进行开发。所有开发者都从这个“中央仓库”推送（push）和拉取（pull）代码，确保了协作流程的清晰。\n2.  **允许推送（push）**：由于没有工作目录，开发者可以直接向裸仓库推送分支更新。普通仓库默认拒绝推送至当前检出的分支，以避免覆盖工作区，而裸仓库没有此限制。\n3.  **分离与安全**：将作为“档案馆”的裸仓库与进行“开发编辑”的本地仓库分开，避免了误操作对中央历史记录的影响。\n\n简单理解，普通的Git仓库像一个“编辑工作室”（既有草稿又有档案），而裸仓库则是一个纯粹的“图书档案馆”。我们日常使用的GitHub、GitLab等平台上的远程仓库，其本质就是裸存储库。"}, {"num": 36, "title": "Git 垃圾收集器是什么？它是如何工作的？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git概念", "answer": "Git垃圾收集器（git gc）是用于清理Git仓库中无用对象并优化存储性能的维护工具。其核心作用是将松散对象打包、清除历史遗留的垃圾数据，从而缩减仓库体积、提升读写效率。\n\n它主要清理以下对象：1. 悬空对象（如被删除的分支或修改的暂存区内容）；2. 过期的提交或标签（通常指90天前未被任何引用指向的对象）；3. 已用reflog清理掉的引用记录。这些对象最初不会立即删除，而是标记为“待删除”，最终由gc统一处理。\n\n垃圾收集器通过引用计数识别对象：当某个对象不再被任何分支、标签或reflog引用时，即被视为垃圾。gc运行时会将松散对象打包成packfile以压缩存储，同时执行增量打包优化历史链。它可自动触发（如git fetch/push后）或手动执行（git gc --prune），具体行为受gc.auto等配置参数控制。\n\n定期运行gc能有效防止仓库臃肿，保持操作速度，是Git底层仓库维护的重要机制。"}, {"num": 37, "title": "Git 的合并提交（Merge Commit）和常规提交（Regular Commit）有哪些区别？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "**定义区别**\n常规提交（Regular Commit）是对代码库的一次独立改动记录，通过 `git commit` 创建，具有一个父提交。合并提交（Merge Commit）则是一个特殊的提交，通过 `git merge` 创建，用于将两个或多个分支的修改历史合并在一起，通常具有两个或多个父提交。\n\n**主要区别**\n1.  **产生方式**：常规提交由开发者主动添加暂存区并提交产生；合并提交则是在执行合并命令时，由Git自动生成（或手动确认）。\n2.  **父提交数量**：常规提交只有一个直接父提交；合并提交则至少有两个父提交，分别指向被合并分支的末端。\n3.  **内容来源**：常规提交的内容来自开发者的工作区改动；合并提交本身通常不包含新的代码修改（Fast-forward模式除外），其作用是将多个分支的代码历史“编织”在一起。\n4.  **历史表现**：常规提交在时间线上是线性前进的一个点；合并提交则会在历史记录中创建一个“分叉点”和“汇合点”的拓扑结构，清晰地展示了分支的整合。\n\n**简要总结**\n常规提交是**记录增量改动**的基本单元，而合并提交是**整合并行开发历史**的连接点。在团队协作中，理解两者区别有助于选择合适的合并策略（如 `--no-ff` 来强制生成合并提交以保留分支历史），从而保持清晰可追溯的项目演进脉络。"}, {"num": 38, "title": "GitHub Flow 有哪些分支类型？作用分别是什么？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git协作应用", "answer": "GitHub Flow 主要包含两种核心分支类型：\n\n1.  **`main` (或 `master`) 分支**：\n    *   **作用**：这是项目的主干分支，代表**生产环境就绪**的代码。它必须始终保持稳定、可部署和可发布状态。所有新功能开发完成后，都最终合并回此分支。\n    *   **重要性**：任何时刻，从 `main` 分支拉取的代码都应该是可信赖的。\n\n2.  **`feature` 分支 (或称功能分支)**：\n    *   **作用**：为开发新功能、修复bug或进行实验而从 `main` 分支创建的临时分支。它用于隔离开发工作，避免影响主干稳定性。\n    *   **命名**：通常采用描述性名称，如 `feature/add-login-button` 或 `fix/navbar-alignment`。\n    *   **生命周期**：开发完成并通过代码审查后，通过**Pull Request (PR)** 合并回 `main` 分支，随后即可被删除。\n\n**核心协作流程**：开发者在 `feature` 分支上工作，完成后发起 PR，在 PR 中进行代码评审和讨论，合并后功能就集成到了 `main` 分支。这种流程简单、高效，非常适合持续部署的团队。有时也会为紧急线上问题创建 `hotfix` 分支，但其本质仍是特殊的功能分支。"}, {"num": 39, "title": "GitLab Flow 有哪些分支类型？作用分别是什么？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git协作应用", "answer": "GitLab Flow 主要包含四种核心分支类型：\n\n1. **主分支（main）**  \n   作为生产环境部署的唯一源头，始终代表已发布的稳定版本。所有提交需经过严格测试，通常采用保护机制（如合并请求审核），确保代码质量。\n\n2. **开发分支（develop）**  \n   （注：GitLab Flow 以环境分支为核心，此分支可能不强制使用，但可选）  \n   用于集成新功能，是日常开发的汇聚点，稳定后合并至主分支。\n\n3. **功能分支（feature/*）**  \n   从开发分支或主分支创建，用于开发独立功能或修复。完成后通过合并请求（Merge Request）审核，合并回上游分支，保持主分支整洁。\n\n4. **发布分支（release/*）**  \n   当需要发布新版本时，从主分支创建。仅允许修复bug，测试通过后合并至主分支并打标签，同时可部署到生产环境。\n\n核心逻辑：通过环境分支（如production）和特性分支的严格流转，实现持续集成与交付，同时保障生产环境代码的稳定性。"}, {"num": 40, "title": "git gc --no-prune 的作用是什么？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "`git gc --no-prune` 的作用是在执行 Git 仓库的垃圾回收时，**优化仓库存储，但显式保留那些本应被清理的不可达对象（即没有任何引用指向的提交、树或文件对象）**。\n\n**核心要点如下：**\n\n1.  **与默认行为的区别**：默认的 `git gc` 或带 `--prune` 的命令，在打包优化后会删除不可达对象以节省空间。而 `--no-prune` 阻止了这最后的“修剪”步骤。\n2.  **主要使用场景**：\n    *   **数据恢复**：当你误删了分支或标签，但知道其历史提交仍存在于对象数据库中时，使用此命令可以防止这些对象被清理，为后续使用 `git fsck --lost-found` 或 `git reflog` 来找回丢失的提交提供保障。\n    *   **仓库调试**：在需要分析仓库历史，包括已“丢失”的对象时，保留它们以便调查。\n3.  **效果**：仓库依然会通过打包对象来优化存储结构，但由于未清理不可达对象，优化后的仓库大小可能会比执行完整 gc（带修剪）的仓库大。\n\n**总结**：它是一个安全阀，允许你对仓库进行优化整理，同时有选择地保留那些已脱离引用历史但仍可能有用的对象，为数据恢复或高级调试留出时间窗口。通常不作为日常操作，而是在特定维护或修复场景下使用。"}, {"num": 41, "title": "git gc 和 git gc --auto 命令有什么区别？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "**核心区别：主动清理 vs 条件触发**\n\n**1. git gc**\n- **作用**：显式调用垃圾回收，全面优化Git仓库性能。它会合并松散对象、清理无用的文件、优化打包等。\n- **使用场景**：通常在大量提交、合并或分支操作后，由开发者手动执行，以主动压缩仓库、提升后续操作速度。\n\n**2. git gc --auto**\n- **作用**：仅在Git认为必要时（如过多的松散对象）自动执行轻量级清理，不会影响当前操作。\n- **使用场景**：作为其他Git命令（如`git pull`, `git commit`）的子步骤在后台静默运行，实现自动化维护。\n\n**总结**：`git gc`是用户发起的“大扫除”，确保仓库整洁；`git gc --auto`是系统的“日常维护”，按需自动优化，无感知且开销小。日常开发中，通常依赖`--auto`，仅在仓库明显膨胀时手动运行`git gc`。"}, {"num": 42, "title": "git pull 和 git fetch 命令有什么区别？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "**git pull 与 git fetch 的核心区别**\n\n**1. 核心作用不同**\n*   **git fetch**：仅从远程仓库**下载**新的提交、分支等更新到本地，但**不会自动合并**到你的工作目录。它相当于一个安全的“信息同步”步骤。\n*   **git pull**：相当于 `git fetch` + `git merge`。它不仅下载远程更新，还会**立即将当前分支与远程对应分支进行合并**。\n\n**2. 操作流程与安全性**\n*   **git fetch** 后，更新保存在本地的“远程追踪分支”（如 `origin/main`）中。你可以先查看差异（用 `git log` 或 `git diff`），确认无误后再决定是否合并。**更安全、可控**。\n*   **git pull** 一步到位，可能直接引入冲突或未经检查的代码，风险较高。适合在确信远程更新与本地无冲突时使用。\n\n**3. 典型使用场景**\n*   **使用 `git fetch`**：当你需要先审查远程团队的最新工作，或在合并前进行代码审查时。\n*   **使用 `git pull`**：当你确信自己本地分支与远程分支同步良好，希望快速获取最新代码并继续工作时。\n\n**4. 常见命令组合**\n```bash\n# 安全的同步工作流\ngit fetch origin\ngit log origin/main  # 检查更新\ngit merge origin/main # 手动合并\n\n# 快速同步\ngit pull origin main\n```\n\n**总结**：`fetch` 是“只下载，不安装”，给用户决策权；`pull` 是“下载并安装”，追求便捷但需承担合并风险。在团队协作中，`fetch` 通常是更稳妥的选择。"}, {"num": 43, "title": "git rebase 命令有什么作用？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "git rebase 的主要作用是将一个分支的提交“重新应用”到另一个分支的基底之上，从而实现提交历史的线性化整理。\n\n简单来说，它能将当前分支的起点移动到指定的目标分支的最新提交上。其主要作用包括：\n\n1.  **保持提交历史的整洁与线性**：与 `git merge` 会产生额外的合并提交不同，rebase 能让提交历史变成一条清晰的直线，避免不必要的交叉合并记录，便于代码审查和问题追溯。\n2.  **同步上游更新**：当主分支有新提交时，在个人功能分支上使用 `rebase` 可以将这些更新拉取下来，并应用在自己的提交之上，确保功能分支基于最新的代码。\n3.  **在提交到共享分支前整理提交记录**：开发者可以在本地使用 `git rebase -i` 进行交互式变基，合并琐碎的提交、修改提交信息或重新排序，使提交逻辑更清晰后再推送到远程仓库。\n\n**注意**：变基会**重写提交历史**。因此，对于已经推送到远程并可能被他人拉取的共享分支进行变基，可能会造成协作冲突。**黄金法则是：只对尚未推送的本地提交进行变基**。在操作时如遇冲突，解决后需使用 `git rebase --continue` 继续。"}, {"num": 44, "title": "git rebase 和 git merge 命令有什么区别？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "git rebase 和 git merge 都是用于整合不同分支代码的命令，但工作流和影响有本质区别。\n\n**核心区别在于历史处理方式：**\n- **git merge**：保留完整的分支历史，创建一个新的“合并提交”连接两个分支。历史图呈网状，清晰反映合并点，但可能产生冗余的合并提交。\n- **git rebase**：将当前分支的提交“变基”到目标分支的最新提交上，重写提交历史，形成一条线性、整洁的直线。它不会产生合并提交。\n\n**适用场景与选择建议：**\n- **用 merge**：当你需要保留完整的协作痕迹（如公共分支合并到主干），或需要明确标记合并事件时。安全性高，不改写已共享的历史。\n- **用 rebase**：当你整理本地提交历史，使项目日志更清晰时（如功能分支更新前同步主干）。适合未推送到远程的本地提交，避免对已共享提交使用 rebase 以防协作者混乱。\n\n**一句话总结**：merge 像“立交桥”，保留所有路径；rebase 像“铁轨整平”，创造线性历史。实践中常组合使用：本地用 rebase 整理，合并到公共分支时用 merge。"}, {"num": 45, "title": "git reset 与 git revert 命令有哪些区别？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "git reset 与 git revert 都是用于撤销更改的 Git 命令，但核心区别在于对历史记录的影响方式不同：\n\n**1. 核心目的**\n*   `git reset`：通过移动 HEAD 指针来“回退”到某个历史提交，**修改或丢弃**其后的提交历史。\n*   `git revert`：通过创建一个新的提交来“反转”指定提交的更改，**保留原有历史**，并生成一条新的历史记录。\n\n**2. 影响范围**\n*   `git reset` 有三种模式，影响不同：\n    *   `--soft`：仅移动 HEAD，暂存区和工作区不变。\n    *   `--mixed`（默认）：移动 HEAD，并重置暂存区，工作区不变。\n    *   `--hard`：移动 HEAD，并重置暂存区和工作区。\n*   `git revert` 只创建一个反向提交，工作区会应用该反向更改。\n\n**3. 适用场景**\n*   `git reset`：适用于**本地未推送**的、错误的提交，可以彻底清理历史。\n*   `git revert`：适用于**已推送到远程公共分支**的提交，因为它不重写历史，对团队协作更安全。\n\n**总结**：若需安全地撤销已公开的提交，使用 `git revert`；若需在本地清理未公开的历史，则使用 `git reset`。两者选择的关键在于是否涉及共享历史。"}, {"num": 46, "title": "git stash 命令有什么作用？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "git stash 是 Git 中用于临时保存工作区修改的命令。它的核心作用是：在不提交当前更改的情况下，将工作区和暂存区的内容保存到一个栈结构中，让工作区恢复到干净状态。\n\n主要使用场景包括：\n1. **临时切换任务**：需要紧急修复 bug 或切换分支时，当前功能未完成又不想提交半成品。\n2. **拉取远程更新**：本地有修改时想同步远程最新代码，避免 merge 冲突。\n3. **清理工作区**：临时测试或检查代码，需要快速恢复原有状态。\n\n常用操作：\n- `git stash`：保存当前修改（包括暂存区）\n- `git stash save \"说明\"`：添加描述信息\n- `git stash list`：查看所有存储记录\n- `git stash apply`：恢复最近一次存储（保留记录）\n- `git stash pop`：恢复并删除记录\n- `git stash drop`：删除指定存储\n\n注意事项：\n- 存储是按栈结构管理的，可多次存储\n- 未跟踪的新文件默认不存储，需加 `-u` 参数\n- 存储内容不会影响提交历史，适合短期暂存\n\n这个命令就像开发中的“临时抽屉”，帮助开发者灵活管理未完成的代码修改，是分支管理和任务切换的重要辅助工具。"}, {"num": 47, "title": "什么情况下推荐使用 git rebase 代替 git merge 命令？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "在以下场景中，推荐使用 **git rebase** 代替 **git merge**：\n\n1. **保持线性、整洁的提交历史**：当你希望项目历史呈现清晰的直线结构，避免大量合并提交造成的“菱形”混乱时，rebase 能将当前分支的提交“重新应用”到目标分支的顶端，形成易于追溯和代码审查的历史。\n\n2. **整理本地未推送的提交**：在本地开发新功能时，可以先用多个小提交记录进度，功能完成后通过 **交互式 rebase**（`git rebase -i`）合并、修改或重排这些提交，再推送到远程，使公共历史更专业。\n\n3. **同步上游主分支的更新**：将本地 feature 分支更新到主分支最新状态时，使用 rebase 而非 merge 可以避免产生无意义的“合并主分支”的提交，保持分支历史干净。\n\n4. **确保提交的原子性**：rebase 后每个提交都基于最新的代码基础，便于排查问题或回滚单个变更。\n\n**注意事项**：\n- 避免对已推送到远程的公共分支进行 rebase，以免重写历史影响他人协作。\n- 团队协作时应遵循统一规范，明确何时使用 rebase 或 merge。\n\n**示例流程**：\n```bash\ngit checkout feature\ngit rebase main   # 将 feature 提交重新应用到 main 最新节点上\n```\n总体而言，当追求清晰历史、线性提交和本地提交优化时，rebase 是更优选择。"}, {"num": 48, "title": "什么是 Git 中的 fast-forward 合并？如何执行？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "Fast-forward合并是Git中一种特殊的合并方式。当目标分支（如main）的指针直接位于当前分支（如feature）的祖先链上时，Git执行合并时只需将目标分支指针直接移动到当前分支的最新提交，无需创建新的合并提交。这相当于“快进”到最新的点，历史记录保持线性。\n\n**执行方式：**\n当使用`git merge`命令时，如果条件满足（即没有分叉），Git默认会执行fast-forward合并。例如，在`feature`分支开发完成后，切换到`main`分支并执行`git merge feature`，若`main`自创建分支后无新提交，Git会直接移动`main`指针到`feature`的最新提交。\n\n**非fast-forward的情况：**\n如果`main`分支在`feature`分支创建后有了新提交，历史出现分叉，Git会创建一个新的合并提交（merge commit），将两个分支的修改结合，这不再是fast-forward。\n\n**控制选项：**\n- **强制fast-forward：** 使用`git merge --ff-only`，若无法快进则合并失败。\n- **禁止fast-forward：** 使用`git merge --no-ff`，即使能快进也创建合并提交，以保留分支合并的上下文记录。\n\n在实际开发中，对于临时性的功能分支，常使用`--no-ff`来保持完整的合并记录，便于追溯分支历史。"}, {"num": 49, "title": "什么是 Git 合并冲突？什么情况下会导致提交冲突？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git概念", "answer": "Git 合并冲突（Merge Conflict）是指在将两个分支的更改合并时，Git 无法自动整合的差异。当不同分支对同一文件的同一部分进行了相互冲突的修改，或者一个分支删除了文件而另一个分支修改了该文件时，Git 无法确定应保留哪个版本，便会标记为冲突，需要开发者手动介入解决。\n\n导致提交冲突的典型情况主要有以下几种：\n\n1.  **并行修改**：两个分支都修改了同一文件的相同行或相邻行。\n2.  **删除与修改**：一个分支删除了某个文件，而另一个分支对该文件进行了修改。\n3.  **添加与添加**：两个分支都添加了同名的文件，但内容不同。\n\n当发生冲突时，Git 会在冲突文件中插入冲突标记（如 `<<<<<<<`、`=======`、`>>>>>>>`），清晰地标示出不同分支的修改内容。开发者需要审查代码，选择保留哪部分更改，或进行整合编辑，然后手动标记冲突已解决，再完成合并提交。理解并熟练解决合并冲突，是团队协作和版本控制中的一项必备技能。"}, {"num": 50, "title": "什么是 Git 多分支开发和单分支开发？各自的优缺点是什么？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git协作应用", "answer": "Git 多分支开发是指利用 Git 的分支功能，在多个独立分支上进行并行开发的方式。通常，主分支（如 main）用于存放稳定代码，开发者创建功能分支或修复分支完成特定任务，再通过合并（merge）或拉取请求（pull request）集成到主分支。这种方式支持同时进行多项开发任务，"}, {"num": 51, "title": "什么是 Git 的 blame 命令？有什么作用？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "Git 的 `blame` 命令用于**逐行追溯指定文件内容的修改历史**。它的核心作用是将每一行代码与其对应的提交信息关联起来，清晰地展示出“谁在什么时间因为什么原因修改了哪行代码”。\n\n主要作用体现在以下三方面：\n1.  **定位修改责任人**：能快速找到每一行代码的最后修改者，便于进行代码审查、问题追责或技术咨询。\n2.  **分析代码演变**：配合 `-L` 参数可查看特定行数范围内的修改历史，或使用 `-w` 忽略空白变更，帮助理解一段复杂逻辑的演变过程。\n3.  **辅助调试与重构**：当发现潜在缺陷或需要重构某段代码时，可通过 `blame` 定位到引入问题的具体提交（commit），进而分析其改动上下文和意图。\n\n在实际工作中，它常用于代码审查、排查线上问题根源或理解陌生代码。例如，执行 `git blame -L 10,20 file.py` 可直接查看该文件第10至20行的具体修改历史，是高效追溯代码来源的重要工具。"}, {"num": 52, "title": "什么是 Git 的 cherry-pick？有什么作用？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "Git cherry-pick 是一个用于精确复制提交（commit）的命令，它可以将其他分支上的某个特定提交，应用于当前所在的分支。\n\n其主要作用体现在两个典型场景：\n1.  **紧急修复**：当在开发分支（dev）上修复了一个 bug 并提交后，需要将这个修复同步到生产分支（release）或主分支（main），但又不想合并整个开发分支的代码，此时可以使用 `git cherry-pick <commit-hash>` 只选取这个修复提交。\n2.  **功能模块移植**：当某个分支上的某个功能提交非常独立，你想将它“移植”到另一个分支单独使用，cherry-pick 是最直接的方式。\n\n与 `git merge` 或 `git rebase` 合并整个分支不同，cherry-pick 操作的对象是单个提交。它实质上是在当前分支创建一个新的提交，这个新提交的代码更改和原提交相同，但拥有一个新的哈希值。\n\n**注意事项**：由于是独立应用提交，如果被选取的提交与当前分支的代码环境差异较大，可能会产生合并冲突，需要手动解决。"}, {"num": 53, "title": "什么是 Git 的 reflog？如何使用它来恢复丢失的分支或提交？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "Git的reflog（引用日志）是Git在本地维护的一个特殊日志，它记录了HEAD指针和所有分支引用的移动历史，包括那些通过`reset`、`rebase`、`commit`等操作“丢失”的提交记录。\n\n使用reflog恢复的核心步骤如下：\n1.  **查看日志**：运行`git reflog`或`git reflog show <branch>`，会看到一个按时间排列的列表，显示了每次操作前后的提交哈希（如`HEAD@{3}`）。\n2.  **恢复分支**：若分支被误删，可在reflog中找到删除前的最后一次提交哈希，然后执行`git branch <分支名> <哈希>`即可重建该分支。\n3.  **恢复提交**：若执行了`git reset --hard`导致提交丢失，同样在reflog中找到目标提交的哈希，用`git reset --hard <哈希>`或`git checkout -b <新分支> <哈希>`将其恢复。\n\nreflog是纯粹的本地记录，它为误操作提供了宝贵的“后悔药”，是解决此类问题的首选工具。"}, {"num": 54, "title": "什么是 Git 的 shallow fetch？如何使用？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "Git的shallow fetch是一种限制克隆或拉取深度的操作，仅获取仓库的指定层数（通常为最新的n个）提交历史，而非完整的提交记录。其主要目的是提升大型仓库的克隆速度、节省磁盘与网络开销，尤其适用于持续集成（CI）等只需最新代码的场景。\n\n**使用方法**：\n通过`git clone`或`git fetch`命令附加`--depth`参数实现。例如，克隆时仅获取最新1个提交：\n```\ngit clone --depth 1 <仓库URL>\n```\n后续拉取时继续限制深度：\n```\ngit fetch --depth 5\n```\n若需将浅克隆转为完整仓库，可执行`git fetch --unshallow`。\n\n**优势**：\n1.  **高效快速**：大幅减少数据传输量，加速仓库初始化。\n2.  **节省空间**：本地仅存储部分提交对象，降低磁盘占用。\n\n**注意事项**：\n浅克隆会限制部分Git操作（如`git log`无法查看完整历史、`git blame`可能不完整）。如需完整历史，应转换为完整克隆。在团队协作或需要历史追溯的场景中，应谨慎使用。"}, {"num": 55, "title": "什么是 Git 的 stash apply 和 stash pop？有什么区别？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "Git 的 stash 命令用于临时保存当前工作区的修改，以便切换分支或处理其他任务，而不会丢失当前的工作进度。其中：\n\n1. **stash apply**：将指定的 stash 内容重新应用到当前工作区。它不会自动删除该 stash，因此同一份 stash 可被多次应用。使用格式为 `git stash apply [stash@{n}]`，若不指定则默认应用最近一次（stash@{0}）。\n\n2. **stash pop**：在将 stash 内容应用到工作区后，会自动从 stash 列表中删除该条记录。如果应用过程中出现冲突，stash 记录将被保留以便手动解决。\n\n**核心区别**在于是否删除 stash 记录：`apply` 保留 stash 以便重复使用，而 `pop` 在应用后自动删除。通常，若确定只需使用一次，推荐 `pop` 以保持列表整洁；若需在多个分支应用同一 stash，则使用 `apply`。\n\n**使用场景示例**：当开发中途需要紧急修复 bug 时，可先 `git stash` 保存当前修改，切到 bug 修复分支，完成后返回原分支，通过 `stash pop` 或 `stash apply` 恢复之前的工作。"}, {"num": 56, "title": "什么是 Git 的 submodule update --remote 选项？有什么作用？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "在 Git 中，`submodule update --remote` 选项用于更新 Git 子模块，但其更新逻辑与默认行为有重要区别。\n\n默认情况下，执行 `git submodule update` 会将子模块切换到**父仓库中记录的特定提交**（即父仓库最后一次提交时所指向的子模块 commit SHA）。这是一种“快照”式更新，确保子模块版本与父仓库绑定，保持项目整体状态的稳定。\n\n而加上 `--remote` 选项后，命令行为会改变。它会忽略父仓库中记录的提交，直接**将子模块更新到其跟踪的远程分支的最新提交**（通常是 `origin/main` 或 `origin/master`，也可通过 `.gitmodules` 配置其他分支）。这相当于让子模块独立地同步其自身远程仓库的最新代码。\n\n**核心作用与场景**：该选项主要用于**主动跟踪上游子模块的最新进展**。适用于当你希望主项目的某个依赖库（作为子模块）持续跟进其独立开发的最新版本时。使用时需注意，这可能会引入未经充分测试的新代码，因此更适用于开发或集成阶段，而在需要严格版本控制的生产环境中，应谨慎使用或回退到默认的精确提交更新模式。"}, {"num": 57, "title": "什么是 Git 的分离头指针状态？如何处理？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "Git 的分离头指针状态是指当前 HEAD 指针直接指向了一个具体的提交对象（commit hash），而不是指向一个本地分支。此时，你处于一个“游离”的提交状态，而非任何一个分支上。\n\n**出现场景**：当你执行 `git checkout <commit_id>` 或 `git switch --detach <commit_id>` 切换到某个特定的历史提交时，就会进入此状态。\n\n**风险**：在此状态下进行的新提交不会自动归属于任何现有分支。一旦你切换到其他分支，这些提交将成为“无根”的悬挂对象（dangling commits），最终可能被垃圾回收机制清除，导致工作丢失。\n\n**如何处理**：\n1.  **识别与确认**：运行 `git status`，会明确提示 “HEAD detached at <commit_id>”。\n2.  **保留工作**：如果在该状态下有新的提交想要保留，应立即基于当前提交创建一个新分支：`git switch -c <new-branch-name>`。\n3.  **安全切换**：如果仅是查看历史，无需新提交，直接切换到目标分支即可：`git switch <branch-name>` 或 `git checkout <branch-name>`。\n4.  **放弃工作**：若想丢弃在该状态下的所有更改，可强制切换分支或重置：`git switch -f <branch-name>`。\n\n**最佳实践**：避免直接检出到提交 hash。如需检查历史代码，建议使用 `git checkout <commit_id> -- <file>` 检查单个文件，或利用 `git stash` 暂存工作后，再切换分支。"}, {"num": 58, "title": "什么是 Git 的大文件存储 LFS？如何使用它？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "Git LFS（Large File Storage）是Git的一个扩展，用于高效管理大型文件（如设计资源、数据集、媒体文件等）。它将大型文件存储在独立的服务器中，而在Git仓库中仅保存一个轻量级的指针文件，从而避免仓库体积膨胀和传输缓慢的问题。\n\n**核心原理**：  \n使用LFS后，当您添加大型文件时，Git会将其替换为包含文件元数据的指针文件。实际文件内容被上传至LFS服务器（如GitHub LFS、GitLab LFS等）。拉取仓库时，Git自动从服务器下载对应的大文件，对用户透明。\n\n**基本使用步骤**：  \n1. **安装配置**：  \n   - 安装Git LFS客户端（如 `git lfs install`）。\n   - 配置仓库：在仓库中运行 `git lfs track \"*.psd\"` 来指定要追踪的文件类型（支持通配符）。\n\n2. **日常操作**：  \n   - 添加文件：正常用 `git add` 添加大文件，LFS自动接管。\n   - 提交推送：`git commit` 和 `git push` 时，指针文件存入Git历史，大文件传至LFS服务器。\n\n3. **克隆与拉取**：  \n   - 克隆含LFS的仓库：自动下载指针文件，需运行 `git lfs pull` 或通过配置自动拉取大文件。\n\n**优势与场景**：  \n- **节省空间**：本地仓库和Git服务器仅存储指针，大文件独立存储。\n- **版本控制**：仍可对大文件进行版本跟踪，但避免冗余拷贝。\n- **协作高效**：团队共享大型资产时，无需反复传输全量文件。\n\n**注意事项**：  \n- 需服务器支持（如GitHub、GitLab已集成）。\n- 免费版通常有存储和带宽限制。\n- 已有的大文件可通过 `git lfs migrate` 迁移至LFS。\n\nGit LFS是管理二进制资产的关键工具，能显著提升包含大型文件项目的协作效率。"}, {"num": 59, "title": "什么是 Git 的工作流？有哪些常见的 Git 工作流？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git概念, Git协作应用", "answer": "Git 工作流是团队使用 Git 进行协作时，为管理代码变更、分支策略和发布流程而制定的一套规范与最佳实践。其核心目的是让协作高效、代码历史清晰、发布过程可控。\n\n常见的 Git 工作流主要有以下几种：\n\n1.  **集中式工作流**：所有人直接在单一的主分支（通常是 `main`）上工作。它最简单，适合非常小的团队或个人项目，但容易产生冲突和混乱。\n\n2.  **功能分支工作流**：任何新功能或修复都应在一个新的特性分支（如 `feature/login`）上开发，完成后通过合并请求（Merge Request）合并回主分支。它实现了隔离开发，是当前最流行的基础模式。\n\n3.  **Gitflow 工作流**：定义了严格的分支模型，包括常设的 `main`（生产）、`develop`（开发）分支，以及用于功能、发布和热修复的临时分支。它流程严谨，适合有明确版本发布周期的大型项目，但相对复杂。\n\n4.  **Forking 工作流**：开发者先“派生”（Fork）上游仓库，在自己的副本上修改，完成后向上游仓库发起合并请求。这是开源项目（如 GitHub）的通用协作方式，便于管控外部贡献。\n\n在实际工作中，团队通常会基于这些经典模型进行调整，选择最适合自身项目规模和发布节奏的工作流。"}, {"num": 60, "title": "什么是 Git 的浅克隆？如何执行？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "Git 的浅克隆（Shallow Clone）是一种仅克隆仓库部分历史的克隆方式。与完整克隆不同，它只拉取最近一个或几个提交的代码快照，而不是整个项目的完整历史记录。其主要目的是大幅减少克隆所需的时间和磁盘空间，尤其适用于大型仓库或仅需最新代码的CI/CD流水线场景。\n\n执行浅克隆的命令非常简单，核心是在 `git clone` 命令后添加 `--depth` 参数来指定克隆的深度。例如：\n*   `git clone --depth 1 <仓库URL>`\n  该命令只克隆最新一次提交，这是最常用的形式。\n*   `git clone --depth 5 <仓库URL>`\n  该命令会克隆最近的5次提交。\n\n**核心优势与注意事项：**\n*   **优点**：速度快，节省空间。\n*   **限制**：克隆出的仓库不完整，无法进行完整的日志查询（`git log`）或基于历史的分支合并等操作。后续如果需要获取更多历史，可以使用 `git fetch --unshallow` 或 `git fetch --depth=<N>` 来加深克隆深度。\n\n因此，浅克隆是针对特定场景下对效率有要求时的有效工具。"}, {"num": 61, "title": "什么是 Git 的签署提交？如何在 Git 中签署提交？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "Git 的签署提交是指开发人员使用 GPG 密钥对提交进行数字签名，以验证提交来源的真实性和完整性，确保代码未经篡改。\n\n签署提交的核心作用是建立信任链：通过 GPG 签名，其他协作者或系统可以验证该提交确实由指定的开发者完成，而非他人伪造。这尤其在开源项目协作或对安全要求较高的团队中至关重要。\n\n在 Git 中签署提交的步骤如下：\n1.  **准备 GPG 密钥**：首先需要在本地生成 GPG 密钥对（`gpg --gen-key`）。\n2.  **配置 Git**：将你的 GPG 密钥 ID 配置给 Git。命令为：`git config --global user.signingkey <你的GPG密钥ID>`。\n3.  **进行签署提交**：在提交时使用 `-S` 标志。例如：`git commit -S -m \"你的提交信息\"`。也可以设置为默认签署所有提交：`git config --global commit.gpgsign true`。\n\n完成配置后，每次签署的提交在查看历史记录时会显示 `signed` 标记，确保了提交的可验证性和不可否认性。"}, {"num": 62, "title": "什么是 Git 的补丁 patch？如何在 Git 中创建和应用补丁？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "Git的补丁（Patch）是一个包含代码更改的文本文件，它记录了差异信息。Git中创建补丁主要有两种方式：\n1.  使用`git diff`命令：针对未提交的更改（工作区与暂存区、暂存区与版本库之间的差异），生成一个通用差异文件。例如，`git diff > changes.patch`可以生成一个补丁文件。\n2.  使用`git format-patch`命令：专门为已提交的更改生成补丁，常用于代码贡献。它会生成包含提交信息、作者等元数据的`.patch`文件。例如，`git format-patch -1`为最近一次提交生成补丁。\n\n应用补丁的方法同样对应两种：\n1.  使用`git apply`命令：应用由`git diff`生成的通用差异补丁。它通常只更新工作区，需要手动添加和提交。例如，`git apply changes.patch`。\n2.  使用`git am`命令：应用由`git format-patch`生成的格式化补丁。它会保留原始提交信息，并直接创建一个新的提交。例如，`git am 0001-某个修改.patch`。\n\n**核心用途**：补丁便于在没有直接Git仓库访问权限的情况下交换代码变更，是代码离线传输、严格代码审查或自动化集成流程中的实用工具。"}, {"num": 63, "title": "什么是 Git 的软重置、混合重置和硬重置？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "Git的三种重置（reset）模式是撤销提交历史操作的核心工具，它们的主要区别在于对工作目录（Working Directory）、暂存区（Staging Area）和HEAD指针的影响不同。\n\n**1. 软重置 (`--soft`)**\n仅将HEAD指针移动到指定提交，**保留**工作目录和暂存区的所有更改。被撤销的提交所包含的更改会全部保留在暂存区中，相当于将多个提交合并为一个暂存状态。适用于需要重新组织或合并最近几次提交的场景。\n\n**2. 混合重置 (`--mixed`)**\n这是**默认模式**。它会将HEAD指针移动到指定提交，并**重置暂存区**以匹配该提交。但工作目录中的文件内容保持不变，这些更改会变为“未暂存”状态。适用于想撤销提交但保留文件修改，以便重新选择性暂存的场景。\n\n**3. 硬重置 (`--hard`)**\n**彻底重置**。它会同时将HEAD指针、暂存区和工作目录都重置到指定提交的状态。所有未提交的更改（包括暂存和未暂存的）都会被永久丢弃。适用于彻底放弃所有本地修改，回到某个干净的历史提交点。\n\n**总结**：软重置最“温和”，只动指针；硬重置最“彻底”，完全回溯；混合重置介于两者之间，是常用的撤销提交但保留修改的模式。"}, {"num": 64, "title": "什么是 Git 的重定位？如何使用重定位？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "Git 的重定位（即变基，Rebase）是一种将一系列提交移动到新基础提交之上的操作。它的核心目的是保持提交历史的线性与整洁，避免因频繁合并产生复杂的交叉提交记录。\n\n**主要作用：**\n1.  **整合上游更改：** 将本地特性分支的提交“重新播放”到目标分支（如 main）的最新提交之后，使特性分支基于最新代码。\n2.  **简化历史：** 相比合并（merge）产生的额外合并提交，变基能生成一条直线式的提交记录，更易于理解和追踪。\n\n**基本使用方法：**\n假设你在特性分支 `feature` 上开发，需要同步主分支 `main` 的最新更新：\n```bash\ngit checkout feature\ngit rebase main\n```\n执行后，`feature` 分支的提交将移动到 `main` 分支最新提交之后。若过程中出现冲突，需手动解决后使用 `git rebase --continue` 继续。变基完成后，通常使用 `git merge` 或 `git push` 来集成更改。\n\n**注意事项：**\n- **重写历史：** 变基会改变提交的哈希值，因此**切勿对已推送到远程的公共分支进行变基**，以免造成协作混乱。\n- **适用场景：** 个人分支或尚未分享的提交上使用变基是安全的，可保持历史清晰；在多人协作的共享分支上，应优先使用合并操作。\n\n简单来说，变基是一种“整理”提交历史的工具，合理使用能让项目演进记录更整洁，但需谨慎以避免协作问题。"}, {"num": 65, "title": "什么是 Git 的钩子 Hooks？包括哪些内容？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git概念", "answer": "Git Hooks（钩子）是 Git 在特定事件（如提交、推送、接收等）发生时自动触发执行的脚本机制。它存储在 `.git/hooks` 目录下，通常为 Shell 脚本，也可用 Python 等语言编写。\n\nHooks 主要分为**客户端**和**服务端**两类：\n\n1.  **客户端 Hooks**：在本地操作时触发。\n    *   **pre-commit**：提交前运行，常用于代码风格检查（如 ESLint）。\n    *   **commit-msg**：检查提交信息格式（如是否包含任务号）。\n    *   **pre-push**：推送前运行，可执行本地测试。\n\n2.  **服务端 Hooks**：在服务器接收推送时触发。\n    *   **pre-receive**：处理前验证推送权限。\n    *   **update**：类似，但针对每个分支。\n    *   **post-receive**：推送成功后触发，常用于自动部署或通知。\n\n**核心价值**：通过自动化流程，保障代码质量、统一开发规范、实现简易的持续集成/部署。开发者可自定义脚本，实现个性化的工作流控制。只需将脚本文件（去掉 `.sample` 后缀）放入 hooks 目录并设置可执行权限即可生效。团队常将自定义 Hook 脚本纳入版本控制，并通过符号链接或项目模板共享，以确保一致性。"}, {"num": 66, "title": "什么是 SubGit？为什么要使用它？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git协作应用", "answer": "SubGit 是一款专业的版本控制迁移工具，主要用于将 Subversion (SVN) 仓库无缝迁移到 Git。它通过实时双向同步机制，允许 SVN 和 Git 仓库在迁移期间保持数据一致性，从而支持渐进式迁移，避免一次性切换带来的风险。\n\n使用 SubGit 的主要原因包括：首先，它能实现零停机迁移，团队可以逐步从 SVN 过渡到 Git，无需中断现有工作流程。其次，SubGit 完整保留了版本历史、分支和标签，确保迁移后数据的可追溯性。此外，它支持高级功能如分支映射和属性转换，适应复杂的协作场景。对于企业而言，SubGit 降低了迁移成本，提高了协作效率，是 Git 进阶应用中处理遗留系统集成的理想选择。\n\n总之，SubGit 通过同步技术和自动化处理，简化了 SVN 到 Git 的迁移过程，适用于追求平滑过渡和高效 Git 协作的环境。"}, {"num": 67, "title": "什么是 git bisect？如何使用它来定位错误的来源？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "git bisect 是一个 Git 内置的调试工具，采用二分查找算法，通过标记“好”与“坏”的提交，帮助开发者高效定位引入错误的具体提交。\n\n**核心使用场景**：当发现代码存在 Bug，且不确定是哪个提交引入时，使用 bisect 能快速从大量提交历史中找到罪魁祸首。\n\n**使用步骤如下**：\n1.  **启动**：在包含 Bug 的分支上，执行 `git bisect start` 开始。\n2.  **标记端点**：用 `git bisect bad` 标记当前（有 Bug）的提交，再用 `git bisect good <某个已知正常的提交哈希>` 标记一个正常的提交。\n3.  **二分查找与测试**：Git 会自动检出中间的一个提交。此时测试代码，如果该版本正常，执行 `git bisect good`；如果仍有 Bug，则执行 `git bisect bad`。\n4.  **重复与结束**：重复步骤 3，Git 会不断缩小范围，直到精确定位到第一个引入错误的提交。完成后执行 `git bisect reset` 返回原来的分支。\n\n**高效技巧**：可以编写脚本实现自动化测试，通过 `git bisect run <脚本路径>` 命令，让 Git 自动完成“标记好坏”的整个过程，进一步提升效率。\n\n总之，git bisect 是将 Git 的版本历史优势与高效的查找算法结合，解决“历史追溯”难题的强大工具。"}, {"num": 68, "title": "什么是 git stash drop？它有什么作用？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "git stash drop 是 Git 中用于删除指定储藏（stash）的命令。其作用是删除栈中某一条储藏记录，从而清理不再需要的临时保存的工作状态。\n\n具体来说，当使用 `git stash` 保存当前工作进度后，会创建一个储藏条目并存入储藏栈。若后续想移除某条特定的储藏（例如保存错误或不再需要），可通过 `git stash drop stash@{n}` 指定索引来删除。该命令不会影响工作区或暂存区的当前内容，仅删除储藏记录。\n\n使用时需注意：默认删除最新一条储藏（即 `stash@{0}`），因此删除前建议通过 `git stash list` 查看列表，避免误删重要备份。与 `git stash pop`（应用并删除最新储藏）不同，`git stash drop` 仅执行删除操作，不应用储藏内容。\n\n总结：`git stash drop` 用于精确管理储藏栈，保持环境整洁，是 Git 工作流中清理临时保存状态的常用操作。"}, {"num": 69, "title": "介绍你在项目中使用 Git 协作开发的完整流程（从拉取项目到上线）", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git协作应用", "answer": "我在项目中使用Git协作开发的标准流程如下：\n\n首先通过`git clone`从远程仓库克隆主分支代码到本地。基于主分支创建功能分支进行开发，分支名通常采用`feature/xxx`格式。开发过程中通过`git add`和`git commit`提交代码，并使用`git push`定期推送到远程功能分支。\n\n当功能开发完成，会发起Pull Request请求合并。团队成员进行代码评审，通过CI/CD流水线自动执行测试和构建。合并前需先通过`git fetch`和`git rebase`将主分支最新代码合并到功能分支，解决可能出现的冲突。\n\n代码合并到主分支后，会部署到测试环境进行集成测试。测试通过后，通过打Tag方式标记版本，触发生产环境部署流水线。上线后监控系统运行状态，如有问题及时回滚。\n\n整个流程强调分支策略规范、代码评审质量和自动化测试，确保团队协作高效且代码质量可控。"}, {"num": 70, "title": "删除 Git 分支后，对提交记录有什么影响？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git概念", "answer": "# 删除 Git 分支后，对提交记录的影响\n\n## 核心结论\n\n**删除分支只是删除了分支指针，不会直接删除提交记录。** Git 的提交是独立存储的对象，分支本质上只是指向某个提交的可移动指针。\n\n## 具体影响分析\n\n**情况一：提交被其他分支引用**\n- 如果该分支的提交已经合并到其他分支（如 main），这些提交仍然完整存在于 Git 历史中\n- 完全不受影响，可通过其他分支正常访问\n\n**情况二：提交未被任何引用指向**\n- 删除分支后，这些提交成为\"悬空对象\"（dangling commits）\n- 提交数据不会立即删除，仍保留在 Git 对象数据库中\n- Git 执行垃圾回收（`git gc`）时，这些无引用的提交最终会被清理\n\n## 找回被删除分支的方法\n\n- **reflog**：通过 `git reflog` 查看操作记录，找到分支最后指向的 commit hash\n- **恢复命令**：`git branch <分支名> <commit-hash>` 即可重建分支\n\n## 面试加分点\n\n理解 Git 底层原理：分支是指针（41字节文件）、提交是 SHA-1 哈希对象。这种设计使得分支创建/删除成本极低，同时保证了数据安全性——误删分支通常可在一定时间内恢复。"}, {"num": 71, "title": "在 Git 中如何处理和管理大型文件？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "在 Git 中处理大型文件的核心问题是：Git 默认存储完整文件快照，会导致仓库体积急剧膨胀、克隆速度缓慢。标准解决方案是使用 **Git LFS（Large File Storage）**。\n\n其工作原理是：LFS 不直接在仓库中存储大文件本身，而是存储一个轻量的“指针”文件，真实文件则通过后台加密存储在独立的服务器（如 GitHub LFS 服务或自建服务器）上。当您 `git add` 一个被 LFS 追踪的文件时，Git 实际提交的是这个指针。\n\n**管理流程如下：**\n1.  **安装与初始化**：先安装 Git LFS，然后在仓库中运行 `git lfs install`。\n2.  **指定追踪文件**：使用 `git lfs track \"*.psd\"` 等命令来指定需要由 LFS 管理的文件类型，该信息会记录在 `.gitattributes` 文件中。\n3.  **正常操作**：后续执行 `git add` 和 `git commit`，Git LFS 会自动处理大文件的替换。\n4.  **获取文件**：克隆或拉取时，LFS 会自动下载所需的大文件版本。\n\n**其他注意事项：**\n*   **版本控制**：LFS 支持对大文件进行版本管理，但每次更改都会在服务器端存储新版本，需注意存储成本。\n*   **替代方案**：对于超大型二进制资产（如游戏资源），有时会考虑 `git-annex` 或完全使用外部存储+哈希引用的方案，但 Git LFS 是目前最通用、与主流平台集成最好的方案。\n\n总之，通过 Git LFS 将大型二进制文件的存储与版本控制解耦，是保持 Git 仓库高效管理的最佳实践。"}, {"num": 72, "title": "在 Git 中，如何处理和解决重复提交的问题？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "在 Git 中处理重复提交（如误操作导致内容相同的多次提交）主要通过整合历史提交来解决，核心是使用 `git reset` 或 `git rebase` 命令，并辅以规范的提交流程预防。\n\n解决方法分为两步：\n1.  **识别与定位**：使用 `git log --oneline` 查看提交历史，定位需要合并的重复提交区间。\n2.  **整合提交**：\n    *   **使用 `git reset`**：若要回退到重复提交之前的某个状态，可执行 `git reset --soft <目标提交哈希>` 将后续提交移回暂存区，再重新提交一次。\n    *   **使用 `git rebase`**：更推荐用于交互式整理。执行 `git rebase -i <父提交哈希>`，在编辑器中将重复提交前的 `pick` 改为 `squash` 或 `fixup`，即可将其合并到前一个提交中，使历史更整洁。\n\n**预防措施同样重要**：\n*   提交前使用 `git diff --staged` 仔细检查。\n*   规范团队提交信息，避免模糊提交。\n*   可配置 Git Hooks（如 `pre-commit`）或集成代码审查工具，在推送前进行自动化检查。\n\n总之，解决关键在于善用 `rebase -i` 整理历史，并建立良好的提交习惯与规范，从源头上减少问题发生。"}, {"num": 73, "title": "在 Git 中，如何对特定文件的更改进行 cherry-pick？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "在 Git 中对特定文件的更改进行 cherry-pick，通常用于从一个提交中只提取部分文件的修改，而不是整个提交。核心场景是当某个提交包含了多个文件的修改，但你只想应用其中的一部分。\n\n核心操作方法有两种：\n\n**1. 使用 `git checkout`（推荐，直接高效）**\n这是最常用且直观的方法。假设你想从提交 `abc123` 中提取文件 `path/to/file.txt` 的更改。\n```bash\n# 第一步：从目标提交中检出该特定文件的版本到暂存区\ngit checkout abc123 -- path/to/file.txt\n\n# 第二步：提交这个更改\ngit commit -m \"Cherry-pick file.txt from commit abc123\"\n```\n此命令会将文件的特定版本放入暂存区，你即可直接提交。\n\n**2. 生成并应用补丁**\n如果需要更精细的控制或记录，可以先生成补丁再应用。\n```bash\n# 第一步：生成仅包含该文件更改的补丁\ngit show abc123 -- path/to/file.txt > change.patch\n\n# 第二步：应用补丁\ngit apply change.patch\n# 或者，如果希望直接暂存更改：git apply --index change.patch\n\n# 第三步：提交\ngit commit -m \"Apply change from commit abc123 for file.txt\"\n```\n\n**补充说明：**\n*   **确认工作目录干净**：操作前确保没有未提交的更改，以免冲突或覆盖。\n*   **验证结果**：操作后使用 `git diff --staged` 或 `git diff HEAD` 检查更改是否正确。\n*   **替代命令**：`git diff abc123^..abc123 -- path/to/file.txt` 也可以"}, {"num": 74, "title": "在 Git 中，如何恢复意外更改或删除的文件？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "在 Git 中恢复误操作的核心是理解其三大区域：工作区、暂存区和版本库。根据不同场景，主要有以下几种方法：\n\n**1. 恢复工作区未暂存的修改**\n若文件在工作区被修改但尚未 `git add`，可使用 `git checkout -- <file>` 丢弃更改，将文件还原到最后一次提交或暂存的状态。\n\n**2. 恢复已暂存的修改**\n如果文件已通过 `git add` 加入暂存区但尚未提交，需先执行 `git reset HEAD <file>` 将其从暂存区移除（变回“未暂存”状态），再按第一种方法用 `git checkout -- <file>` 恢复工作区。\n\n**3. 恢复已提交的更改**\n- **回退到指定版本**：使用 `git reset --hard <commit_id>` 可彻底回退到历史提交点（慎用，会丢失之后提交）。`--hard` 同时重置工作区、暂存区和版本库。\n- **生成新提交以撤销**：若需安全地撤销某个提交的修改，可使用 `git revert <commit_id>`，它会创建一个新的“反向提交”来抵消原提交的效果，常用于公共分支。\n\n**4. 恢复误删的文件**\n- 若文件已提交到版本库后被删除，可直接用 `git checkout -- <file>` 从最近一次提交中恢复。\n- 若文件从未提交过但被误删，且之前执行过 `git add`，则可通过 `git fsck --lost-found` 查找并恢复 Git 对象，但较为复杂。\n\n**实用提示**：定期使用 `git status` 检查状态；使用 `git reflog` 查看所有操作记录，可找回被 `reset --hard` 丢弃的提交。\n\n**核心原理**：Git 几乎所有操作都可撤销，因为它在 `.git` �"}, {"num": 75, "title": "在 Git 中，如何确保提交历史的清晰和可追踪性？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "为确保 Git 提交历史的清晰和可追踪性，可采取以下关键措施：\n\n1. **规范提交信息**：采用标准化格式（如“类型: 描述”），例如使用 Conventional Commits，明确提交类型（如 feat、fix、docs）和变更内容，使历史易于理解和搜索。\n\n2. **使用分支策略**：遵循 Git Flow 或 GitHub Flow 等模式，明确分支用途（如主分支用于稳定版本、功能分支用于开发），避免直接在主分支提交，减少历史混乱。\n\n3. **代码审查与合并方式**：通过 Pull Request 或 Merge Request 进行审查，合并时优先使用 rebase 或 fast"}, {"num": 76, "title": "在 Git 中，如何签署提交并验证签名？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "签署提交用于确保代码的真实性和完整性，防止未授权篡改。在Git中，通常使用GPG（GNU Privacy Guard）密钥进行签名。首先，生成GPG密钥对：运行`gpg --gen-key`并遵循提示创建密钥。然后，配置Git使用该密钥，通过命令`git config --global user.signingkey <密钥ID>`设置签名密钥。要签署提交，在提交时"}, {"num": 77, "title": "在 Git 中，如何进行代码审查并集成到工作流中？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git协作应用", "answer": "在 Git 中，代码审查通常通过 Pull Request（PR）或 Merge Request（MR）流程集成到团队协作工作流中。核心流程如下：\n\n1.  **创建特性分支**：开发者从最新的主干（如 main）创建一个描述性的分支进行开发。\n2.  **提交与推送**：在特性分支上完成开发并提交代码，随后推送到远程仓库。\n3.  **发起审查请求**：在 GitHub、GitLab 等平台上，基于该分支创建一个 PR/MR。PR 标题和描述应清晰说明变更目的和内容。\n4.  **同行审查**：团队成员在 PR 页面中审查代码。审查重点包括：代码逻辑与功能是否正确、是否符合编码规范、是否有测试覆盖、潜在的性能或安全问题。审查者可以逐行评论或提出总体意见。\n5.  **讨论与修改**：开发者根据反馈进行讨论，并在**同一个特性分支**上持续提交修改，新的提交会自动更新到 PR 中。\n6.  **批准与合并**：当代码通过审查并获得足够数量的批准后，由拥有权限的成员将分支合并到主干。推荐使用“Squash and Merge”或“Rebase and Merge”来保持主干历史整洁。\n\n此流程将审查作为强制步骤，并通过分支隔离保证了主干代码的稳定性。实践中，应保持 PR 规模适中，并利用自动化工具（如 CI/CD）运行测试，以提升审查效率。"}, {"num": 78, "title": "在团队开发中，如何有效管理和区分 Git 分支？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git协作应用", "answer": "在团队开发中，有效管理和区分 Git 分支通常遵循清晰的分支策略与规范。首先，**采用合适的分支模型**是关键。例如，使用 **Git Flow** 或 **GitHub Flow**。Git Flow 适用于发版周期明确的项目，通过 `main`、`develop`、`feature`、`release`、`hotfix` 等分支区分线上版本、开发主线、功能开发、发布准备和紧急修复。GitHub Flow 则更轻量，以 `main` 分支为基准，所有新功能通过新建分支开发，经代码审查后合并，适合持续部署。\n\n其次，**建立统一的命名规范**。分支名应包含类型、任务编号或简要描述，如 `feature/JIRA-123-login`、`fix/bug-order-calc`。这能快速识别分支用途和负责人，提升协作效率。\n\n再者，**严格执行代码审查与合并流程**。功能分支合并至主干前，必须通过 Pull Request 或 Merge Request 进行同行评审，并结合持续集成工具确保测试通过。合并后及时删除已合并的临时分支，保持仓库整洁。\n\n最后，**定期同步主干代码**。开发者应频繁将主干（如 `develop` 或 `main`）的变更合并到自己的功能分支，避免长期分支产生严重冲突。\n\n通过以上措施，团队能清晰区分各分支用途，保障开发流程有序，减少集成风险，提升交付质量。"}, {"num": 79, "title": "如何从 Git 中删除文件，而不将其从文件系统中删除？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "在Git中，若需删除文件但不从文件系统中删除，主要方法是使用`git rm --cached`命令。此命令会将文件从Git索引（暂存区）中移除，但保留工作目录中的实际文件，使文件变为未跟踪状态。\n\n具体操作步骤如下：\n1. **执行命令**：在终端中运行`git rm --cached <文件名>`，例如`git rm --cached example.txt`。若要删除整个目录，可使用`git rm --cached -r <目录名>`。\n2. **确认更改**：文件会从Git的跟踪中删除，但仍存在于本地文件系统中。使用`git status`可查看文件状态变为未跟踪。\n3. **提交更改**：运行`git commit -m \"移除文件跟踪\"`以记录此操作。\n\n为防止文件被重新跟踪，建议将文件名或模式添加到`.gitignore`文件中。例如，添加`example.txt`到`.gitignore`后，Git会忽略该文件的更改。\n\n注意事项：此方法适用于已跟踪文件。若文件从未被跟踪，直接添加到`.gitignore`即可。在团队协作中，应确保`.gitignore`更新并提交，以避免他人误操作。\n\n总之，核心是使用`git rm --cached`解除Git跟踪，同时保持文件系统完整，再结合`.gitignore`管理忽略规则"}, {"num": 80, "title": "如何使用 Git 钩子 hooks 自动化任务？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git协作应用", "answer": "Git 钩子是在特定事件发生时自动执行的脚本，位于项目 `.git/hooks` 目录下。通过自定义这些脚本，可以自动化代码提交前检查、集成与部署等任务。\n\n常见钩子分为客户端和服务器端两类。客户端如 `pre-commit` 可在提交前运行代码检查（如语法校验、单元测试）；`commit-msg` 能规范提交信息格式。服务器端的 `pre-receive` 可在代码推送前进行权限校验或集成测试。\n\n使用步骤为：进入 `.git/hooks` 目录，以相应事件名（如 `pre-commit`）创建脚本文件，赋予执行权限并编写自动化逻辑。例如，`pre-commit` 中可配置ESLint检查，不通过则阻断提交。\n\n需注意：钩子脚本默认不随仓库克隆，应通过版本控制工具或安装脚本进行同步。同时应避免在钩子中执行过于耗时或阻塞的操作，确保开发流程顺畅。合理运用Git钩子能显著提升代码质量和团队协作规范性。"}, {"num": 81, "title": "如何使用 git bisect 来查找引入 bug 的提交？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "`git bisect` 是一个用于二分查找定位引入 bug 的提交的调试工具。其核心原理是：在已知一个包含 bug 的坏提交（bad）和一个之前的好提交（good）之间，通过二分法不断切换到中间提交进行测试，从而快速锁定导致问题的单个提交。\n\n**具体步骤如下：**\n\n1.  **启动并指定范围**：首先，使用 `git bisect start` 命令启动调试会话。然后，用 `git bisect bad` 标记当前已知有问题的提交（通常是 HEAD），再用 `git bisect good <commit>` 标记一个已知没有问题的较早提交（如某个发布标签或分支）。\n2.  **二分查找与测试**：Git 会自动检出范围中间的一个提交。此时，你需要编译并测试这个版本的代码。\n3.  **标记提交**：测试后，根据该版本是否仍有 bug，执行相应命令：\n    *   如果问题依然存在，输入 `git bisect bad`。\n    *   如果问题已修复，输入 `git bisect good`。\n4.  **重复与定位**：Git 会根据你的反馈，再次跳到剩余范围的中间提交，重复“测试-标记”的过程。此过程持续进行，直到 Git 找到导致问题的**第一个坏提交**，并会输出类似 `xxxxxxx is the first bad commit` 的信息。\n5.  **结束会话**：定位完成后，使用 `git bisect reset` 退出 bisect 状态，回到最初的分支。\n\n**使用技巧**：你可以编写一个自动测试脚本，配合 `git bisect run <script>` 命令实现全自动化查找，极大提升效率。此方法特别适用于在代码历史中快速定位一个隐蔽的回归 bug。"}, {"num": 82, "title": "如何修复一个混乱的 Git 提交？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "修复混乱的 Git 提交需根据场景选择方法。若仅修改最近一次提交信息或内容，可使用 `git commit --amend`。若需修改更早的提交或整理提交历史，核心工具是**交互式变基（Interactive Rebase）**，即 `git rebase -i`。\n\n执行后，编辑器会列出提交列表，可对每个提交进行：\n- **修改信息**：将 `pick` 改为 `reword`。\n- **合并提交**：将需要合并的提交前的 `pick` 改为 `squash`。\n- **拆分提交**：将提交设为 `edit`，暂停后拆分多次提交。\n- **删除提交**：直接删除对应行。\n\n若提交已推送，变基后需使用 `git push --force`（谨慎操作，最好在特性分支上）。对于未提交的修改，可用 `git reset` 或 `git stash` 先清理工作区，重新组织后再提交。\n\n为预防混乱，建议团队规范提交信息（如约定式提交），并善用分支隔离功能开发。"}, {"num": 83, "title": "如何删除 GitHub 等代码托管平台上的文件夹？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作, Git协作应用", "answer": "在GitHub等代码托管平台上，无法直接删除文件夹，因为Git跟踪的是文件变更而非文件夹。要删除文件夹，需删除其中所有文件并提交更改。以下是两种常用方法：\n\n**方法一：使用命令行**\n1. **克隆仓库**：`git clone <仓库URL>`\n2. **删除目标文件夹**：\n   ```bash\n   git rm -r <文件夹名称>\n   ```\n   （`-r` 表示递归删除）\n3. **提交并推送**：\n   ```bash\n   git commit -m \"删除文件夹\"\n   git push origin main\n   ```\n\n**方法二：通过GitHub网页界面**\n1. 进入目标文件夹。\n2. 逐个选择文件，点击“Delete this file”并提交更改。\n3. 删除所有文件后，空文件夹会自动消失。\n\n**注意事项**：\n- 操作前确保本地有最新版本：`git pull`\n- 若误删，可通过`git checkout HEAD -- <文件路径>`恢复\n- 大规模清理建议使用命令行，效率更高\n\n完成后刷新仓库页面即可验证删除效果。此操作不可逆，建议先创建分支测试。"}, {"num": 84, "title": "如何在 Git 中创建一个临时的工作区来进行实验性开发？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "在 Git 中创建临时工作区进行实验性开发，推荐使用 `git worktree` 功能。它允许同时拥有多个工作目录，每个关联独立分支"}, {"num": 85, "title": "如何在 Git 中创建只读的分支？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "Git 中创建“只读”分支并非通过单一命令直接实现，而是通过**分支保护策略**结合**权限控制**来达成。核心思路是限制对该分支的直接推送（push）和强制推送（force push）。\n\n具体实现通常有以下几种方法：\n\n1.  **使用代码托管平台的分支保护规则（主流方案）**：\n    在 GitHub、GitLab 或 Gitee 等平台上，进入仓库的 “Settings” -> “Branches”，为特定分支（如 `main`, `release`）添加保护规则。启用以下关键选项：\n    *   **禁止直接推送**：阻止任何人向该分支直接 `git push`。\n    *   **要求合并请求（MR/PR）**：所有修改必须通过合并请求合入。\n    *   **要求代码审查和CI检查**：确保合并前经过评审和自动化测试。\n\n2.  **通过服务器端钩子脚本**：\n    在自托管的 Git 服务器上，可以编写 `pre-receive` 钩子脚本。当有推送时，脚本会检查推送的目标分支是否为受保护分支以及操作人权限，若不符合则拒绝推送。\n\n3.  **仓库权限设置**：\n    将分支保护与团队成员权限结合。通常将开发人员角色设置为“开发者”，仅允许向特性分支推送；而将主分支的推送权限仅授予“维护者”或“管理员”，大多数成员只有只读权限。\n\n4.  **本地工作流与团队约定**：\n    通过团队规范强制执行，例如规定所有工作必须在特性分支进行，通过合并请求合入，不直接操作主分支。\n\n**典型应用场景**：保护主分支（`main`/`master`）的稳定性，维护发布分支（`release`）的版本完整性，确保关键分支的代码经过严格审核。\n\n**最佳实践**：将**分支保护**、**必需的代码审查**和**自动化CI检查**三者结合，是构建安全、可靠只读分支的标准方式。"}, {"num": 86, "title": "如何在 Git 中删除已经推送到远程仓库的文件？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "在 Git 中删除已推送到远程仓库的文件，需要执行以下步骤：\n\n1. **删除文件**：使用 `git rm <文件名>` 命令。该命令会同时从暂存区和工作目录中删除文件。若只想从版本控制中移除（保留本地文件），可使用 `git rm --cached <文件名>`。\n\n2. **提交更改**：执行 `git commit -m \"描述信息\"`，提交删除操作。\n\n3. **推送到远程**：使用 `git push origin <分支名>` 将删除操作同步到远程仓库。\n\n**重要注意事项**：\n- 如果文件已被提交多次，且希望彻底从历史记录中清除（例如敏感文件），需要使用 `git filter-branch` 或 BFG Repo-Cleaner 等工具重写历史，并强制推送（`git push --force`）。此操作会影响所有协作者，需谨慎执行。\n- 为防止再次提交，建议将文件名添加到 `.gitignore` 文件中。"}, {"num": 87, "title": "如何在 Git 中处理二进制文件的合并冲突？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "二进制文件无法像文本文件一样进行行级合并，处理冲突的核心原则是**预防为主，手动选择**。具体操作如下：\n\n**一、 预防冲突（最佳实践）**\n1.  **使用 `.gitattributes` 文件**：在项目根目录配置，明确指定二进制文件类型（如 `*.png binary`），并配置合并策略，例如使用 `union` 驱动让Git在某些场景下尝试自动合并，或设置为 `binary` 让Git直接在冲突时标记为二进制冲突。\n2.  **采用文件锁**：对于频繁修改的二进制文件（如设计稿），可使用 `git lfs` 的文件锁功能，或在协作工具（如GitLab、GitHub）上锁定文件，避免多人同时修改。\n3.  **规范分支与提交**：尽量将二进制文件的修改放在独立分支或提交中，并通过清晰的提交信息说明改动，减少并行修改带来的冲突概率。\n\n**二、 解决冲突**\n当冲突发生时：\n1.  **识别冲突**：Git会提示二进制文件有冲突，`git status` 可查看。\n2.  **手动决策**：无法自动合并，必须由开发者**手动选择保留哪个版本**。通常：\n    *   若冲突不大，可选择 `git checkout --ours <文件名>`（保留当前分支版本）或 `git checkout --theirs <文件名>`（保留对方分支版本）。\n    *   更常见的是，在工作区**手动用正确的文件替换冲突的文件**。\n3.  **完成合并**：解决后，执行 `git add <文件名>` 标记为已解决，最后 `git commit` 完成合并。\n\n**总结**：处理二进制文件冲突，重点在于通过配置和协作流程预防其发生。一旦冲突，必须人工选择并替换文件，无法依赖Git的自动合并工具。"}, {"num": 88, "title": "如何在 Git 中处理分支名称冲突？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "在 Git 中，分支名称冲突通常指本地分支与远程分支存在同名但内容不同的情况，常见于 `git fetch` 或 `git pull` 后。处理方式如下：\n\n1.  **理解冲突原因**：当远程仓库已有一个与你本地分支同名的分支，且历史发生分叉时，Git 会阻止直接推送。此时需评估两个分支的内容。\n\n2.  **核心解决方案**：\n    *   **重命名本地分支**：这是最安全的方式。执行 `git branch -m <old-name> <new-name>` 重命名本地分支，然后将其作为新分支推送到远程，避免冲突。\n    *   **强制更新/推送**：若确定本地分支为准，可使用 `git push --force` 或 `git push --force-with-lease` 覆盖远程分支。**注意：`--force` 会覆盖远程历史，需在团队协作中谨慎使用并提前沟通。**\n\n3.  **沟通与协作**：在团队环境中，强制推送前务必与相关开发者确认，避免覆盖他人的工作。可使用 `git log origin/<branch-name>..<branch-name>` 检查差异。\n\n4.  **预防措施**：\n    *   推送新分支前，使用 `git fetch` 更新远程分支信息。\n    *   使用 `git branch -a` 查看所有分支，注意远程分支的命名。\n    *   建立团队分支命名规范（如功能分支使用 `feature/` 前缀）可有效减少冲突。\n\n总结：优先采用重命名分支的方式，若必须强制更新则确保团队知情。良好的协作流程是避免此类问题的关键。"}, {"num": 89, "title": "如何在 Git 中处理文件名大小写敏感的问题？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "Git 默认在 Linux 等系统上对文件名大小写敏感，在 macOS 和 Windows 上则不敏感。这会导致跨平台协作时出现文件替换、冲突等意外问题。\n\n**核心解决方案是配置 Git 的 `core.ignorecase` 参数：**\n1.  **全局设置**（推荐）：`git config --global core.ignorecase false`。这会让 Git 在所有操作中严格区分大小写，是跨平台团队的最佳实践。\n2.  **仓库设置**：仅在当前仓库生效，使用 `git config core.ignorecase false`。\n\n**若已出现问题，修复步骤如下：**\n1.  确保 `core.ignorecase` 已设为 `false`。\n2.  使用 `git rm -r --cached <目录/文件>` 将错误大小写的文件从暂存区移除。\n3.  再使用正确的 `git add` 命令添加文件。\n4.  提交变更，完成重命名。\n\n**注意事项**：更改此配置后，可能需要清理暂存区或重新克隆仓库以确保一致性。在项目初期统一配置，可避免后续的协作冲突。"}, {"num": 90, "title": "如何在 Git 中处理重命名的文件？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "在 Git 中处理重命名的文件，主要涉及识别和记录文件路径的变更。Git 本身不直接存储“重命名”操作，而是通过比较内容相似度来智能检测。\n\n**核心处理流程：**\n1.  **使用标准命令**：推荐使用 `git mv old_name new_name` 命令。该命令会直接更新 Git 索引（暂存区）并记录此路径变更，是明确且高效的方式。\n2.  **备选方案**：也可手动移动文件（如使用 `mv` 命令），然后依次执行 `git rm old_name` 和 `git add new_name` 来更新索引。此操作等效于重命名。\n3.  **自动检测机制**：Git 在提交或比较差异时，会计算文件变更的相似度。当一个文件的删除与另一个新增文件的内容相似度足够高（默认50%）时，Git 会自动识别为重命名。可通过 `git log -M` 或 `git diff -M` 参数调整敏感度。\n4.  **注意事项**：为确保 Git 正确识别为重命名而非独立操作，建议将重命名与对文件内容的修改分开提交。这样在 `git log --follow` 或 `git blame` 时，能更完整地追踪文件历史。\n\n**最佳实践**：始终优先使用 `git mv`，它为 Git 提供了最明确的操作意图，便于后续的版本追踪和历史查看。"}, {"num": 91, "title": "如何在 Git 中对 commit message 进行规范化管理？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git协作应用", "answer": "在 Git 中对 commit message 进行规范化管理，是保证代码历史清晰、协作高效的关键实践。核心在于建立统一的格式标准并借助工具强制执行。\n\n首先，制定 commit message 格式规范。广泛采用的是 Conventional Commits 规范，结构为：`<类型>[可选作用域]: <描述>`。类型（如 feat, fix, refactor, docs）明确改动性质；作用域指明影响范围；主体描述应简洁准确，通常以动词原形开头。例如：“feat(auth): add OAuth2 login support”。主体描述后，可添加详细的正文和脚注（如关联的 Issue 编号）。\n\n其次，通过工具确保规范落地。利用 Git 钩子（如 `commit-msg` 钩子）在提交前自动验证消息格式。集成 commitlint 等工具与 husky 配合，可在本地拦截不规范的提交。此外，在 CI/CD 流程中加入检查，作为合并请求的强制门槛。\n\n规范化 commit message 能带来清晰的项目历史、便于自动化生成更新日志，并显著提升代码审查与问题排查效率，是专业团队工程化能力的重要体现。"}, {"num": 92, "title": "如何在 Git 中对分支进行版本控制？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git协作应用", "answer": "在 Git 中，分支本质上是指向提交对象（commit）的可变指针。对其进行版本控制，主要通过 Git 的原生命令来实现，其核心流程包括：\n\n1.  **创建与切换**：使用 `git branch <name>` 创建新分支，或用 `git checkout -b <name>`（或 `git switch -c <name>`）直接创建并切换到新分支。这确保了主分支（如 `main`）不受影响。\n\n2.  **独立开发与提交**：在新分支上进行代码修改和提交（`git commit`），所有变更仅记录在该分支的历史中，实现了并行开发。\n\n3.  **合并与更新**：开发完成后，通过 `git merge <branch>` 将目标分支的更改合并回当前分支（如主分支）。为保持历史整洁，可使用 `git rebase` 变基。定期用 `git pull --rebase` 更新本地分支。\n\n4.  **查看与管理**：使用 `git branch -v` 查看所有分支及其最新提交，用 `git branch -d <name>` 删除已合并的分支。分支间的差异可用 `git diff` 或 `git log` 比较。\n\n5.  **协作与策略**：通过 `git push` 和 `git pull` 与远程仓库同步分支。团队通常采用 Git Flow 或 GitHub Flow 等策略来规范分支的创建、合并与删除，确保工作流清晰。\n\n关键在于，Git 通过轻量级的分支指针和强大的合并功能，实现了高效的并行开发与协作，这是其版本控制的核心优势之一。"}, {"num": 93, "title": "如何在 Git 中对历史提交进行重写和清理？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "在 Git 中，对历史提交进行重写和清理通常用于删除敏感信息、合并冗余提交、修正错误或优化仓库体积。以下是核心方法与步骤：\n\n**1. 交互式变基（git rebase）**  \n适用于修改、重排或合并本地未推送的提交。执行 `git rebase -i HEAD~n` 进入交互界面，可将多个提交合并为一个（使用 `squash` 或 `fixup`），或修改提交消息（`reword`）。完成后用 `git push --force-with-lease` 强制推送更新。\n\n**2. 过滤分支（git filter-branch 或 git filter-repo）**  \n用于批量修改历史，例如移除大文件或敏感数据。`git filter-branch --tree-filter 'rm -f 文件名'` 可删除特定文件，但该命令已逐渐被更高效的 `git filter-repo` 取代。使用前务必创建备份分支。\n\n**3. BFG Repo-Cleaner 等专用工具**  \n针对清理大型二进制文件或密码等场景，BFG 提供简洁命令（如 `bfg --delete-files 文件名`），能快速重写历史且性能较好。\n\n**注意事项**：  \n- 重写历史是"}, {"num": 94, "title": "如何在 Git 中对比不同分支的差异？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "在Git中对比分支差异主要通过以下方式实现：\n\n**1. 基本命令对比**\n- `git diff branch1..branch2`：显示两个分支间所有文件的具体差异\n- `git diff branch1...branch2`：查看自共同祖先以来branch2的所有变更\n- `git log branch1..branch2`：列出branch2中独有的提交记录\n\n**2. 图形化比较**\n- 使用`git difftool`调用图形化对比工具（如Beyond Compare）\n- 通过`git log --graph --oneline --all`查看分支拓扑关系\n\n**3. IDE集成操作**\n- 在VS Code等编辑器中直接右键分支选择\"Compare with...\"\n- 使用GitKraken等GUI工具可视化对比分支差异\n\n**实用技巧**：\n- 添加`--stat`参数查看变更统计摘要\n- 使用`git diff branch -- filename`聚焦特定文件\n- 通过`git merge-base branch1 branch2`找到共同祖先后精确对比\n\n**选择建议**：\n- 快速检查：使用`--stat`或日志对比\n- 代码审查：使用完整diff命令\n- 复杂冲突解决：结合图形化工具分析\n\n这些方法能有效支持代码审查、合并准备和变更追踪等开发场景。"}, {"num": 95, "title": "如何在 Git 中将一个提交拆分成多个提交？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "在 Git 中拆分一个提交，主要通过交互式变基（Interactive Rebase）实现，核心步骤如下：\n\n1.  **启动交互式变基**：执行 `git rebase -i <要拆分提交的父提交哈希>`（或使用 `HEAD~n` 指定）。这会打开一个编辑器，显示从该点开始的所有提交列表。\n2.  **标记目标提交**：在列表中，找到你想拆分的那条提交，将其开头的 `pick` 改为 `edit`。保存并退出编辑器。Git 会在此提交处暂停。\n3.  **拆分提交**：此时工作区状态是该提交完成后的状态。执行 `git reset HEAD^` 将此提交完全撤销，工作区文件不变，但更改变为未暂存状态。\n4.  **分次提交**：现在你可以将更改分成多次 `git add` 和 `git commit`，创建多个更小、更原子的提交。\n5.  **完成变基**：所有小提交完成后，执行 `git rebase --continue`。Git 会依次应用后续的提交，完成整个操作。\n\n**关键点与注意事项**：\n*   此操作会重写提交历史，如果已推送，后续需使用 `git push --force` 或 `--force-with-lease` 更新远程分支。\n*   操作前建议备份分支或使用 `git stash` 保存未提交更改。\n*   若过程中出现冲突，需手动解决后，使用 `git add` 标记解决，再执行 `git rebase --continue`。\n*   若想中止操作，可使用 `git rebase --abort` 恢复原状。\n\n通过这种方式，可以将一个臃肿的提交重构为一系列逻辑清晰、易于理解和回滚的小提交，便于代码审查和维护。"}, {"num": 96, "title": "如何在 Git 中将多个提交合并成一个提交？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "在 Git 中合并多个提交为一个提交，通常使用 **交互式变基（Interactive Rebase）** 来实现，核心命令是 `git rebase -i`。这主要用于整理提交历史，使其更清晰。\n\n**主要步骤：**\n1.  **启动交互式变基**：执行 `git rebase -i HEAD~n` 或 `git rebase -i [目标提交的父提交哈希]`。`n` 表示要向前合并的提交数量。\n2.  **标记提交**：编辑器会打开一个提交列表。将需要合并的提交前面的 `pick` 命令改为 `squash` 或 `s`。**第一个提交保持 `pick`**，后续要合并的提交改为 `squash`。\n3.  **编辑提交信息**：保存退出后，Git 会打开新编辑器，让你修改合并后的提交信息。编辑并保存即可。\n4.  **完成合并**：Git 将标记的提交合并为一个，并应用新的提交信息。\n\n**重要注意事项：**\n- **风险操作**：此操作会**重写历史**。如果这些提交已推送到远程仓库，需要使用 `git push --force` 覆盖历史，**这可能影响其他协作者**，通常仅在个人分支或推送前使用。\n- **替代方案**：若只是想合并未推送的本地提交，`git reset --soft [基准提交]` 然后重新提交也是一种可选思路。\n\n面试时，清晰阐述 `rebase -i` 的命令、`squash` 的作用以及改写历史的注意事项，是回答此问题的关键。"}, {"num": 97, "title": "如何在 Git 中将本地分支推送到不同的远程仓库？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "在 Git 中，将本地分支推送到不同远程仓库，主要通过管理多个远程仓库地址实现。核心步骤如下：\n\n1. **添加多个远程仓库**  \n   使用 `git remote add <仓库别名> <仓库URL>` 添加新远程仓库。例如：\n   ```bash\n   git remote add upstream https://github.com/other/repo.git\n   ```\n   此时默认远程仓库通常为 `origin`，可添加如 `upstream` 等别名。\n\n2. **推送至指定远程仓库**  \n   使用 `git push <远程仓库别名> <本地分支名>` 指定目标仓库推送。例如：\n   ```bash\n   git push upstream feature-branch\n   ```\n   若需同时推送并设置上游跟踪，可添加 `-u` 参数。\n\n3. **验证远程仓库列表**  \n   通过 `git remote -v` 可查看所有已配置的远程仓库及其URL，确保地址正确。\n\n**应用场景**：常用于开源项目（向原项目仓库及自己 fork 的仓库分别推送）、多平台同步（如 GitHub 与 GitLab）或备份至不同远程服务器。\n\n**注意事项**：确保远程仓库URL权限正确，且本地分支已提交最新代码。不同仓库的分支权限可能独立管理，需根据实际情况操作。"}, {"num": 98, "title": "如何在 Git 中恢复已删除的分支？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "在 Git 中恢复已删除的分支主要依赖于引用日志（reflog），它记录了分支和提交的移动历史。以下是恢复步骤：\n\n首先，使用 `git reflog` 命令查看 HEAD 的历史记录。这个命令会显示所有提交和分支切换的哈希值，包括删除分支前的活动。找到删除分支前的最后一个提交，例如通过提交消息或时间戳识别。\n\n然后，基于该提交哈希，使用 `git checkout -b <新分支名> <提交哈希>` 或 `git branch <新分支名> <提交哈希>` 重新创建分支。这将基于旧分支的最新状态创建新分支。\n\n如果 reflog 信息不完整，可以尝试 `git fsck --lost-found` 查找悬空对象（dangling commits），但 reflog 通常更高效。\n\n注意事项：\n- 及时操作，因为 Git 的垃圾回收可能会定期清理未引用的对象。\n- 确保有备份或版本控制历史，以防 reflog 过期。\n- 此方法适用于本地删除的分支；如果远程分支已删除，需结合远程仓库操作。\n\n总之，恢复删除分支的关键是利用 Git 的引用日志快速找回提交点并重建分支。"}, {"num": 99, "title": "如何在 Git 中找到并删除未被引用的对象？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "在 Git 中，未被引用的对象（悬空对象）是指不属于任何分支、标签或远程引用的对象，通常是由于 rebase、reset 等操作遗留的旧提交或未暂存的临时数据。查找并删除它们的步骤如下：\n\n1.  **查找悬空对象**：\n    使用 `git fsck` 命令检查数据库完整性。添加 `--dangling` 参数可专门列出悬空对象（如悬空的提交、树或 blob）：\n    ```bash\n    git fsck --dangling\n    ```\n    该命令会列出对象的 SHA-1 哈希值，可配合 `git show <hash>` 查看内容。\n\n2.  **安全删除悬空对象**：\n    - **自动清理**：推荐使用 `git gc --prune=now` 命令。`git gc` 是垃圾回收机制，`--prune=now` 参数会立即清除所有不可达对象。\n    - **手动删除**：可使用 `git prune` 命令（属于低层命令），直接删除所有悬空对象。但 `git prune` 通常配合 `git gc` 使用更安全。\n\n3.  **补充说明**：\n    - **`git prune` 与 `git gc` 的区别**：`git prune` 仅删除不可达对象；`git gc` 则执行更全面的仓库优化（压缩文件、清理悬空对象等）。\n    - **注意事项**：删除操作不可逆。若需保留临时提交，应先创建分支引用（如 `git branch temp <hash>`）。建议在清理前备份仓库。\n\n**总结**：通过 `git fsck --dangling` 定位未引用对象，再使用 `git gc --prune=now` 安全清理，可有效释放仓库空间，保持版本库整洁。"}, {"num": 100, "title": "如何在 Git 中控制某些文件不被提交？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "在 Git 中控制文件不被提交，主要通过以下三种方式实现，其中最核心的是使用 **`.gitignore`** 文件。\n\n**1. 使用 .gitignore 文件（最常用）**\n在项目根目录创建 `.gitignore` 文件，并写入需要忽略的文件或目录的匹配规则（如 `*.log`、`/build/`、`node_modules/`）。该文件本身应被提交，使规则对所有协作者生效。注意：此方法只能忽略尚未被 Git 跟踪的文件。如果文件已被提交，需先执行 `git rm --cached <file>` 将其从跟踪中移除。\n\n**2. 使用本地排除文件（.git/info/exclude）**\n将忽略规则写入 `.git/info/exclude` 文件。其格式与 `.gitignore` 相同，但**仅对本地仓库生效**，不会提交到远程仓库。适用于个人本地的临时或敏感文件（如测试配置、个人脚本）的忽略。\n\n**3. 使用 assume-unchanged 标记（临时“忽略”已跟踪文件）**\n对于已跟踪但临时不想修改的文件（如本地配置），可使用命令 `git update-index --assume-unchanged <file>`。这会让 Git 假定该文件未更改，从而在 `git status` 中隐藏。用 `no-assume-unchanged` 恢复。这并非真正的忽略，而是一种本地视角的“隐藏”，需谨慎使用。\n\n**总结**：**`.gitignore` 用于项目级的规则共享；`.git/info/exclude` 用于个人本地的规则；`assume-unchanged` 用于临时性、不改变文件内容的“忽略”需求。** 核心原则是避免将构建产物、依赖包、环境配置和敏感信息纳入版本控制。"}, {"num": 101, "title": "如何在 Git 中查看和分析提交图？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "在Git中查看和分析提交图，主要使用`git log`命令结合图形化参数。\n\n核心命令是 `git log --graph --oneline --all`。`--graph`以ASCII图形展示分支与合并历史，`--oneline`将每个提交压缩为一行以便概览，`--all`则显示所有分支（含远程跟踪分支）的提交。\n\n分析时重点关注：1) **分支走向**：通过图形线条判断功能开发的独立性与合并时机。2) **提交哈希**：唯一标识每次变更，用于检出、回滚或比较。3) **分支/标签指针**：明确当前各分支所指向的提交。4) **合并节点**：识别整合点，理解代码集成流程。\n\n高级分析可结合 `--decorate` 显示分支/标签名，或使用 `git log --oneline --graph --all --decorate`。若需更直观的图形，可借助第三方工具如`gitk`或Git GUI客户端。\n\n实战中，提交图能快速定位问题引入点、评估合并冲突历史、理解团队协作模式，是代码考古和项目管理的关键工具。"}, {"num": 102, "title": "如何在 Git 中管理依赖项？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "在 Git 中管理依赖项主要有以下几种方式：\n\n**一、使用依赖管理工具**\n\n不同语言有各自的依赖管理工具，如 Node.js 的 npm/yarn、Python 的 pip/Poetry、Java 的 Maven/Gradle 等。建议将依赖声明文件（如 package.json、requirements.txt、pom.xml）纳入版本控制，便于团队成员同步项目依赖。\n\n**二、合理使用 .gitignore**\n\n依赖目录（如 node_modules、vendor、__pycache__）不应提交到仓库，应在 .gitignore 中排除。这些文件体积大、变动频繁，且可通过声明文件重新生成，提交会污染仓库历史。\n\n**三、锁定依赖版本**\n\n提交锁文件（如 package-lock.json、Pomfile.lock）至关重要。它能精确记录每个依赖的实际版本，确保不同环境下的构建一致性，避免\"在我机器上能运行\"的问题。\n\n**四、使用 Git 子模块（Submodules）**\n\n当项目依赖其他 Git 仓库时，可用 `git submodule` 管理。适合引用内部库或需要源码级依赖的场景，命令为 `git submodule add <仓库地址>`。但子模块使用复杂，需谨慎评估。\n\n**五、最佳实践**\n\n- 定期更新依赖，修复安全漏洞\n- 区分开发依赖和生产依赖\n- 使用私有镜像或仓库管理内部依赖\n- 团队统一依赖管理工具和版本策略\n\n合理管理依赖项能提升项目可维护性和协作效率，是工程化开发的重要环节。"}, {"num": 103, "title": "如何在 Git 中管理分支的访问权限？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git协作应用", "answer": "管理Git分支的访问权限主要通过代码托管平台（如GitHub、GitLab）的权限控制功能结合Git原生机制实现，核心是**实现精细化的读写控制与代码质量守护**。具体方法如下：\n\n1.  **仓库与团队权限设置**：在平台层面，为不同团队或成员分配仓库的**只读（Read）、读写（Write）或管理员（Admin）** 权限。这决定了成员能否访问仓库及推送代码。\n\n2.  **分支保护规则**：对核心分支（如 `main`、`release`）设置保护规则。常见配置包括：\n    *   **禁止强制推送**：防止历史记录被篡改。\n    *   **要求拉取请求（PR）**：禁止直接向保护分支推送，必须通过PR合并。\n    *   **要求代码审查**：PR必须获得指定数量或人员的批准才能合并。\n    *   **要求状态检查通过**：PR在合并前必须通过CI/CD流水线等检查。\n\n3.  **使用CODEOWNERS文件**：在仓库根目录创建 `CODEOWNERS` 文件，指定特定文件或目录的“负责人”。当这些文件被修改时，系统会自动请求相应的负责人进行代码审查，实现**基于代码的自动派审**。\n\n4.  **Git钩子与自定义流程**：服务端可使用 `pre-receive` 等服务器端钩子，在代码推送前执行自定义脚本，检查提交是否符合特定规则（如提交信息格式、分支命名规范），从更底层进行控制。\n\n**总结**：有效的分支权限管理是平台权限、分支保护规则、CODEOWNERS及钩子脚本的有机结合，旨在通过流程自动化，在保障核心分支稳定安全的同时，提升团队协作的规范性与效率。"}, {"num": 104, "title": "如何在 Git 中管理贡献者？有哪些好的方法和实践？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git协作应用", "answer": "在 Git 中管理贡献者的核心是建立清晰的协作规范与权限体系。主要方法包括：\n\n1. **精细化的权限控制**：使用 Git 托管平台（如 GitHub、GitLab）的角色系统，按“只读-读写-管理员”分级设置仓库访问权限，遵循最小权限原则。\n\n2. **规范的分支策略**：采用保护分支（如 main/master）强制代码审查，所有修改通过特性分支（feature branches）合并。使用合并请求（Pull Request/Merge Request）流程，要求至少1人审核通过。\n\n3. **明确的贡献流程**：通过 CONTRIBUTING.md 文件说明编码规范、提交信息格式和测试要求。设立标签（如“good first issue”）帮助新贡献者快速入门。\n\n4. **质量保障机制**：配置自动化测试（CI）在合并前运行，要求通过测试才能合并。使用代码审查清单确保代码风格、安全性和逻辑正确性。\n\n5. **协作文化培养**：定期同步分支、及时解决冲突。通过 Issue 跟踪任务，使用模板统一提交格式。保持开放的沟通，及时反馈代码审查意见。\n\n这些实践能平衡协作效率与代码质量，在开源项目或团队开发中尤为重要。"}, {"num": 105, "title": "如何在 Git 中设置 commit hooks 来自动化任务？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "Git 通过 hooks 机制支持在特定事件（如提交、推送）时自动执行脚本。以下是设置方法：\n\n**1. 理解 Hooks 位置**  \nGit hooks 是位于仓库 `.git/hooks` 目录下的脚本。默认提供示例文件（以 `.sample` 结尾），需去除后缀才能生效。\n\n**2. 常用 Hook 类型**  \n- **`pre-commit`**：提交前执行，常用于代码检查（如 ESLint）、格式化。  \n- **`commit-msg`**：检查提交信息格式（如是否符合规范）。  \n- **`pre-push`**：推送前运行测试或部署检查。  \n\n**3. 操作步骤**  \n- 进入 `.git/hooks` 目录，创建或编辑对应 hook 文件（如 `pre-commit`）。  \n- 添加可执行权限（Linux/macOS 使用 `chmod +x pre-commit`）。  \n- 编写脚本逻辑，例如使用 Bash 或 Python。示例：  \n  ```bash\n  #!/bin/sh\n  npm run lint # 运行代码检查\n  ```\n- 提交代码时将自动触发该 hook。\n\n**4. 跨项目同步**  \n若需在团队中共享 hooks，可将脚本存放在项目目录（如 `.hooks`），通过工具（如 Husky）或 Git 配置 `core.hooksPath` 指向该路径。\n\n**5. 注意事项**  \n- Hooks 脚本需具有可执行权限。  \n- 使用 Git 2.36+ 可通过 `git config core.hooksPath <目录>` 自定义 hooks 路径，提升可维护性。\n\n通过此机制，团队可统一代码规范、自动化测试，减少人为疏漏。"}, {"num": 106, "title": "如何在 Git 中设置和使用 pre-commit 钩子？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "Pre-commit 钩子是在 Git 执行 `git commit` 命令前自动触发的脚本，用于对暂存区的代码进行自动化检查，是保障代码质量的第一道关卡。\n\n**设置方法：**\n1.  **定位钩子目录**：在项目的 `.git/hooks/` 目录下，创建一个名为 `pre-commit` 的文件（无后缀）。\n2.  **编写脚本**：该文件是一个可执行的脚本（通常是 Shell 或 Python 脚本）。脚本中，你可以调用任何代码检查工具，例如 `flake8`（检查 Python 代码风格）、`eslint`（检查 JavaScript）或运行单元测试。\n3.  **赋予执行权限**：执行 `chmod +x .git/hooks/pre-commit` 命令，确保脚本可运行。\n4.  **工作流程**：此后，每次执行 `git commit`，Git 会先运行此脚本。如果脚本返回非零退出码（即检查失败），提交将被中止，从而防止不符合规范的代码进入仓库。\n\n**核心要点与面试回答思路：**\n- **核心目的**：实现提交前的自动化质量门禁。\n- **实现关键**：脚本位于 `.git/hooks/pre-commit`，需可执行。\n- **脚本内容**：集成代码风格检查、静态分析、敏感信息扫描、单元测试等任何你希望在提交前强制执行的命令。\n- **优势**：将质量检查前置，确保进入版本库的代码符合基本标准，减轻人工审查负担。\n- **注意**：该钩子仅存在于本地 `.git` 目录，不会随代码提交到远程仓库。团队可通过配置工具（如 `pre-commit` 框架）或提供示例文件进行协作管理。"}, {"num": 107, "title": "如何在 Git 中设置和使用子模块 Submodules？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "**Git子模块**用于将一个Git仓库作为另一个仓库的子目录嵌入，允许你独立管理其版本。\n\n**主要操作：**\n1.  **添加子模块**：使用 `git submodule add <仓库URL> [本地路径]` 命令。这会在项目根目录生成`.gitmodules`文件，记录子模块信息。\n2.  **克隆含子模块的项目**：使用 `git clone --recursive <仓库URL>`，或在克隆后执行 `git submodule init` 和 `git submodule update`。\n3.  **更新子模块**：\n    *   进入子模块目录，执行 `git fetch` 和 `git merge` 更新到最新。\n    *   回到主项目，提交对子模块引用的变更。\n    *   或使用 `git submodule update --remote` 拉取跟踪分支的最新提交。\n\n**常见场景与注意事项：**\n*   **删除子模块**：需手动执行 `git submodule deinit`、`rm -rf` 子模块目录、编辑`.gitmodules`文件并删除相关缓存（如`.git/modules`中的记录），最后提交更改。\n*   **处理路径变更**：若需移动子模块目录，需同步更新`.gitmodules`文件和`.git/config`中的路径配置。\n*   **团队协作**：建议将`.gitmodules`文件纳入版本控制，并在README中说明子模块初始化步骤。\n\n**总结**：子模块能有效管理项目依赖，关键在于理解其“指针”特性——主项目只记录子模块的特定提交版本。操作时需注意主项目与子模块提交的同步，避免产生“分离HEAD”状态或路径不一致的问题。"}, {"num": 108, "title": "如何在 Git 中还原已经 push 并公开的提交？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "在 Git 中还原已公开的提交，本质上是修正已共享的提交历史。根据场景，主要采用两种方法：\n\n**1. 使用 `git revert`（推荐）**\n这是最安全、协作友好的方式。它不会改变原有历史，而是创建一个新的提交，用于撤销目标提交引入的更改。\n- **命令**：`git revert <commit-hash>`\n- **适用场景**：需要撤销更改，但希望保留完整历史记录，尤其适用于公共分支（如 main）。\n\n**2. 使用 `git reset` + 强制推送（慎用）**\n这会直接丢弃或修改历史，强制覆盖远程仓库。\n- **命令**：先 `git reset --hard <commit-hash>` 回退本地，然后 `git push --force`。\n- **适用场景**：仅用于个人分支或刚推送、尚未被他人拉取的错误提交。**在公共分支上此操作极具破坏性，应与团队充分沟通后再执行。**\n\n**核心要点与建议**\n- **首选 `revert`**：因其创建新提交，不会破坏其他协作者的工作流。\n- **避免 `reset --force`**：除非你完全理解后果并已通知所有协作者，否则应避免在共享分支上使用。\n- **沟通**：任何对公开历史的修改，都应及时通知团队。"}, {"num": 109, "title": "如何在 Git 中配置和使用多个远程仓库？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作, Git协作应用", "answer": "在 Git 中配置和使用多个远程仓库主要分为以下步骤：\n\n首先，**查看与添加远程仓库**。使用 `git remote -v` 查看当前配置的远程仓库。若要添加新远程，执行 `git remote add <名称> <仓库URL>`，例如 `git remote add upstream https://github.com/原作者/repo.git`。常用名称 `origin` 指主仓库，`upstream` 常用于上游或原始仓库。\n\n然后，**操作多个远程仓库**。获取更新时，使用 `git fetch <远程名>`。推送时，指定远程和分支，如 `git push origin main`。拉取合并则用 `git pull <远程名> <分支名>`。\n\n另外，可**管理远程仓库**，包括 `git remote rename` 重命名、`git remote remove` 删除，以及用 `git remote set-url` 修改URL。\n\n最后，**应用场景**常见于开源协作：从 `origin`（个人fork）拉取，在本地修改后推送回 `origin`，再通过 Pull Request 提交至 `upstream`（项目主仓库）。也用于将同一代码仓库同步到多个平台（如 GitHub 和 GitLab）作为备份。\n\n总之，Git 通过为远程仓库设置不同别名，实现了灵活的多仓库协作与同步。"}, {"num": 110, "title": "如何在 Git 中防止错误的代码提交？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "为防止错误的代码提交，可采用以下策略：\n\n首先，利用 **Git Hooks**（如 pre-commit），在提交前自动运行代码检查、测试或格式化工具，确保基本质量。\n\n其次，实施严格的 **代码审查** 流程，通过 Pull Request（PR）让团队成员审核代码，讨论改进点，避免个人错误。\n\n第三，采用规范的 **分支策略**，"}, {"num": 111, "title": "如何处理 Git 中的二进制文件？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "Git处理二进制文件的核心挑战在于其版本控制机制对这类文件不友好。主要问题包括：无法像文本文件一样进行差异比较（diff），且每次修改都会存储完整副本，导致仓库体积迅速膨胀。\n\n**处理方案主要分为三个层面：**\n\n1.  **预防性处理**：对于不需要版本控制的二进制文件（如编译产物、日志），应通过`.gitignore`文件将其排除在仓库外。\n2.  **专项存储方案**：对于需要跟踪的大型二进制文件（如图片、数据集、设计稿），推荐使用**Git LFS（大文件存储）**。它将文件内容存储在远程服务器，仓库中只保留轻量级指针，有效解决体积问题。\n3.  **仓库架构优化**：若二进制文件体量极大且变更频繁，可考虑将其剥离至独立仓库，通过`git submodule`或`git subtree`在主项目中进行引用，实现逻辑分离。\n\n**最佳实践**：在项目初期就规划二进制文件的管理策略，优先使用`.gitignore`和Git LFS。对于已存在的大文件历史，可使用`BFG Repo-Cleaner`等工具进行清理，从根本上优化仓库健康状况。这体现了对Git工作流的深入理解和项目架构能力。"}, {"num": 112, "title": "如何将 Git 的多个提交压缩成一个提交？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "将 Git 多个提交压缩成一个提交，常用 **`git rebase -i`**（交互式变基）命令。以下是核心步骤：\n\n1. **确定提交范围**：使用 `git log` 查看提交历史，找到要压缩的第一个提交的父提交的哈希值（或使用 `HEAD~N` 表示最近N次提交）。\n2. **启动交互式变基**：执行 `git rebase -i <父提交哈希>`，编辑器会列出范围内的所有提交。\n3. **修改提交动作**：在编辑器中，保留第一个提交的 `pick`，将其余要压缩的提交的 `pick` 改为 `squash` 或 `s`（合并并保留所有提交信息）或 `fixup` 或 `f`（合并且仅保留第一个提交信息）。\n4. **完成变基**：保存退出后，Git 会依次应用提交。若使用 `squash`，会再次打开编辑器让您编辑合并后的提交信息；若使用 `fixup`，则自动使用第一个提交的信息。\n5. **确认结果**：使用 `git log` 检查提交历史，已压缩为单次提交。\n\n**注意事项**：\n- 此操作会改写提交历史，**不可用于已推送到远程分支的提交**，除非使用 `git push --force`（需谨慎）。\n- 若过程中出现冲突，需手动解决后执行 `git rebase --continue`。\n- 适用场景：整理本地开发记录，使提交历史更清晰、整洁。"}, {"num": 113, "title": "如何找到 Git 特定提交中已更改的文件列表？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "要查看 Git 特定提交中更改了哪些文件，主要使用以下两个命令：\n\n1. **`git show --stat <commit-hash>`**\n   - 这是最常用的方法。`--stat` 参数会显示该提交中被修改、添加或删除的文件列表，并附带简要的行数统计。\n   - 示例：`git show --stat abc1234`\n\n2. **`git diff-tree --no-commit-id -r <commit-hash>`**\n   - 此命令以更结构化的格式列出文件，输出更简洁。`-r` 表示递归处理子目录，`--no-commit-id` 隐藏提交哈希。\n   - 示例：`git diff-tree --no-commit-id -r --name-status abc1234`（加上 `--name-status` 可显示修改类型，如A/D/M）。\n\n此外，若想在查看提交历史时同时看到文件变更，可使用：\n- `git log --stat --oneline -1 <commit-hash>`（`-1` 表示只显示该提交一条记录）。\n\n这些命令可直接用于定位某次提交的影响范围，在代码审查、问题排查时非常实用。"}, {"num": 114, "title": "如何撤销在 Git 中错误提交的文件？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "撤销Git中错误提交的文件，主要有以下几种方式：\n\n**1. 撤销最近一次提交（保留更改）**\n```bash\ngit reset --soft HEAD~1\n```\n此命令会撤销提交，但保留文件在暂存区，适合提交信息写错需要修改的情况。\n\n**2. 撤销提交并取消暂存（保留工作区更改）**\n```bash\ngit reset --mixed HEAD~1\n```\n撤销提交并将文件从暂存区移除，但保留工作区的修改。\n\n**3. 彻底撤销提交（丢弃所有更改）**\n```bash\ngit reset --hard HEAD~1\n```\n彻底回退到上一次提交状态，所有未保存的更改将丢失，需谨慎使用。\n\n**4. 撤销已推送到远程的提交**\n```bash\ngit revert <commit-hash>\n```\n创建一个新的提交来撤销指定提交的更改，不会改变提交历史，适合多人协作场景。\n\n**5. 撤销单个文件的提交**\n```bash\ngit reset HEAD~1 -- <file>   # 撤销指定文件的提交\ngit checkout -- <file>       # 丢弃文件的修改\n```\n\n**核心要点：**\n- `git reset` 会修改提交历史，适合本地未推送的提交\n- `git revert` 不修改历史，生成新的撤销提交，适合已推送的情况\n- 使用 `--hard` 前务必确认，避免数据丢失\n- 提交到远程后，避免使用 `reset` 强制推送，应优先使用 `revert`"}, {"num": 115, "title": "如何检查一个分支是否已经合并到 master 分支？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "### 检查分支是否已合并到 master 的方法\n\n**1. 使用 `git branch` 命令**  \n- 查看哪些分支已合并到当前分支：  \n  `git branch --merged master`  \n  该命令会列出所有已合并到 `master` 的分支。若目标分支在列表中，则表示已合并。\n- 查看未合并的分支：  \n  `git branch --no-merged master`\n\n**2. 使用 `git log` 或 `git merge-base`**  \n- 通过提交历史检查：  \n  `git log master..目标分支名`  \n  若输出为空，则说明目标分支的所有提交已包含在 `master` 中（即已合并）。  \n- 验证共同祖先：  \n  `git merge-base --is-ancestor 目标分支名 master && echo \"已合并\"`  \n  若命令返回成功（exit code 0），则表示已合并。\n\n**注意事项**  \n- 执行前需确保本地仓库已更新：`git fetch origin`  \n- 若分支存在未合并的独立提交（如通过 cherry-pick 或变基），需结合提交哈希或差异对比进一步确认。\n\n**实用建议**  \n在删除分支前，建议始终检查合并状态，避免误删未合并的工作内容。团队协作中，可通过 Pull Request 或代码审查工具辅助管理分支生命周期。"}, {"num": 116, "title": "如何确定一个分支是否已经被合并？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "要确定一个分支是否已被合并，主要通过以下核心命令进行检查：\n\n**1. 查看已合并到当前分支的所有分支**\n最直接的方法是使用 `git branch --merged`。它会列出所有已经合并到当前HEAD所在分支的分支。如果目标分支名出现在列表中，则说明它已被合并。\n\n**2. 检查特定分支是否已合并到另一分支**\n若要检查`feature`分支是否已合并到`main`分支，可以：\n*   切换到`main`分支，执行 `git branch --merged`。\n*   或者，更精确地使用命令：`git merge-base --is-ancestor <feature> <main>`。如果命令返回退出码0（无输出），则表示`feature`是`main`的祖先，即已合并；返回非0则表示未合并。\n\n**3. 反向检查：查看尚未合并的分支**\n使用 `git branch --no-merged` 可以列出所有尚未合并到当前分支的分支，用于筛选需要处理的分支。\n\n**实用提示**：在合并代码并确认无误后，使用上述方法检查并清理已合并的分支（例如 `git branch -d <branch_name>`），是保持仓库整洁的良好实践。"}, {"num": 117, "title": "如何解决 gitignore 失效的问题？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": ".gitignore失效通常有两种情况，解决方案如下：\n\n**1. 文件已被Git追踪**\n若文件之前已被纳入版本控制，即使后续添加到.gitignore也不会自动忽略。\n- **解决方法**：\n  - 从Git缓存中移除文件（但不删除本地文件）：\n    ```bash\n    git rm --cached <文件路径>\n    # 若需忽略整个目录\n    git rm -r --cached <目录路径>\n    ```\n  - 提交更改：`git commit -m \"更新.gitignore\"`\n  - 此后.gitignore规则将对该文件生效。\n\n**2. .gitignore规则书写错误**\n- **排查点**：\n  - 检查路径是否匹配：如`build/`与`/build/`有区别（后者仅匹配根目录）。\n  - 确认规则语法：使用`#`注释、`!`取反等。\n  - 验证通配符：如`*.log`匹配所有.log文件，但`**/logs`匹配所有层级的logs目录。\n\n**快速验证步骤**：\n- 使用`git status`确认文件是否已被追踪。\n- 用`git check-ignore -v <文件路径>`检查文件是否被忽略及具体规则。\n\n**建议**：新建仓库时优先配置.gitignore，对已有项目修改后记得同步团队成员。遇到复杂情况可检查.gitignore层级（项目根目录、子目录等）。"}, {"num": 118, "title": "如果你是团队负责人，你会如何管理项目代码？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git协作应用", "answer": "作为团队负责人，我会从以下几个方面管理项目代码：\n\n**1. 建立统一规范**\n制定团队统一的代码风格、提交规范（如约定式提交）和分支命名规则。所有成员必须遵守，确保代码库的整洁与可读性。\n\n**2. 实施分支策略**\n采用成熟的分支模型，如Git Flow或基于主干的开发。明确主干、开发、功能分支的用途，规定合并流程，确保并行开发有序进行，同时保护主干分支稳定。\n\n**3. 强制代码审查**\n要求所有代码变更必须通过Pull Request/Merge Request合并。设定至少一到两名成员进行代码审查，重点关注设计合理性、潜在错误和规范遵守情况，将审查作为知识共享和质量把关的关键环节。\n\n**4. 强化自动化与协作**\n集成CI/CD流水线，在合并前自动运行测试和代码检查。同时，定期组织代码回顾会议，鼓励成员分享技术方案，持续优化开发流程和工具链。"}, {"num": 119, "title": "如果想在提交前运行代码检查工具，并在测试失败时阻止提交，该如何配置 Git 存储库？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git协作应用", "answer": "要实现提交前自动代码检查并阻止失败提交，核心是配置 Git 的 **pre-commit 钩子**。\n\n**1. 钩子位置与配置**\n进入项目根目录的 `.git/hooks/` 目录，创建或编辑 `pre-commit` 文件（无后缀），并赋予可执行权限：\n```bash\nchmod +x .git/hooks/pre-commit\n```\n\n**2. 编写检查脚本**\n在 `pre-commit` 文件中添加执行检查的命令。例如，使用 ESLint 检查 JavaScript 代码：\n```bash\n#!/bin/bash\n# 执行代码检查\nnpm run lint\n# 如果检查命令（上一条）执行失败（返回非零），则阻止提交\nif [ $? -ne 0 ]; then\n  echo \"❌ 代码检查失败，提交被阻止。\"\n  exit 1\nfi\n```\n该脚本会运行检查工具，若失败（返回非零退出码），将输出提示并退出，Git 随即终止提交。\n\n**3. 更好的团队协作实践**\n为确保钩子在团队内同步，可将脚本放在项目目录（如 `.githooks/pre-commit`），然后告知成员运行以下命令来配置其本地仓库指向该位置：\n```bash\ngit config core.hooksPath .githooks\n```\n\n**补充：服务器端保障**\n在客户端使用钩子的基础上，可在 Git 服务器（如 GitLab、GitHub）设置 **CI/CD 流水线**，在合并请求时强制运行同样的检查。这是阻止不合规代码进入主干的最终保险。\n\n通过客户端 `pre-commit` 钩子提供即时反馈，并结合服务器端检查，即可构建完整的提交质量关卡。"}, {"num": 120, "title": "怎么配置 Git，使得 git push 时不需要重复输入用户名和密码？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "要配置 Git 实现 push 免密，主要有两种常用方法：\n\n**1. 使用 SSH 协议（推荐）**\n生成 SSH 密钥对：在终端执行 `ssh-keygen -t ed25519 -C \"your_email@example.com\"`，按提示完成。将公钥（默认为 `~/.ssh/id_ed25519.pub`）内容复制到 GitHub/GitLab 等平台的账户 SSH 设置中。之后将远程仓库地址改为 SSH 格式（例如：`git remote set-url origin git@github.com:user/repo.git`），此后 push 便无需输入密码。\n\n**2. 使用凭证缓存**\n对于 HTTPS 协议，可通过 Git 凭证助手缓存密码：\n- **临时缓存**（15分钟）：`git config --global credential.helper cache`\n- **永久存储**：`git config --global credential.helper store`（密码明文保存在 `~/.git-credentials` 中）\n\n**建议**：长期开发推荐 SSH 方案，安全性更高；临时使用可选择 credential 缓存。两者均能有效避免重复认证。"}, {"num": 121, "title": "执行 git pull 命令后，如何回滚到之前的版本？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作", "answer": "执行 `git pull` 后，若需回滚到上一个版本，可以使用以下步骤：\n\n1.  **首选方法：使用 `git reflog` 和 `reset`**\n    这是最安全、最精确的方法。执行 `git reflog` 查看所有 HEAD 的移动历史，找到 `pull` 操作之前的那条记录（通常显示为 `HEAD@{1}` 或对应的提交哈希）。然后执行：\n    ```bash\n    git reset --hard HEAD@{1}\n    ```\n    这会将本地仓库强制回退到 `pull` 之前的状态，工作区也会被同步更新。\n\n2.  **补充方案：如果只想放弃本地修改，保留远程版本**\n    如果拉取后产生冲突且你决定采用远程版本，可以执行：\n    ```bash\n    git checkout --theirs .    # 保留远程版本的文件\n    ```\n    或者，若要彻底丢弃所有未提交的本地修改并回到当前分支的最新提交（即pull后的版本）：\n    ```bash\n    git reset --hard HEAD\n    ```\n\n**核心要点与注意事项**：\n*   `git reset --hard` 会丢失未提交的本地修改，请谨慎操作。\n*   `reflog` 是操作的关键，它能追踪所有移动HEAD的操作，是回滚的“后悔药”。\n*   此操作仅影响本地。如果已经将错误的提交推送到远程仓库，则需要后续进行 `git push --force`，但务必与团队协作方沟通。\n\n**总结**：最通用的答案是先查 `reflog`，再使用 `git reset --hard` 回退到指定历史节点。"}, {"num": 122, "title": "用 Git 开发时，为什么建议创建额外的提交而不是修改现有提交？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git操作, Git概念", "answer": "在 Git 开发中，通常建议创建额外的提交（追加提交）而非修改现有提交（如使用 `git commit --amend` 或变基），主要基于以下几个核心考虑：\n\n1.  **保持历史清晰与可追溯性**：每一次提交都应代表一个完整、独立的逻辑变更点。追加提交能真实记录开发的演进过程，便于后续理解“何时、为何”做了哪些改动。修改提交会重写历史，可能隐藏重要的决策过程。\n\n2.  **保障协作安全与简化**：当代码已经推送到远程共享仓库后，修改历史会导致与其他协作者的代码库产生冲突，迫使所有人进行复杂的修复操作。追加提交则完全避免了此类协作风险，使流程更简单。\n\n3.  **便于调试与回滚**：一个清晰、原子化的提交历史像一本详尽的工程日志。当出现问题时，可以轻松使用 `git bisect` 等工具定位引入错误的具体提交。如果提交被修改得过大或混杂，这个过程会变得非常困难。\n\n**核心原则**：将修改提交视为一种谨慎使用的“整理工具”，主要用于清理尚未推送的本地提交，使其更整洁。在团队协作场景或公开历史中，**优先选择追加提交**，这是更安全、更利于团队协作的实践。一个常见的工作流是：在功能分支上自由创建追加提交，待功能完成、合并前，再考虑是否用交互式变基（`rebase -i`）整理提交历史，但仅限本地或与团队充分沟通后进行。"}, {"num": 123, "title": "请介绍 Git 存储库中的主要目录结构，以及对应的功能？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Git, Git进阶, Git概念", "answer": "Git 存储库的核心结构主要分为两大部分：**`.git` 目录**和**工作区**。`.git` 目录是 Git 的“大脑”，存储了所有的版本历史和配置信息；工作区则是用户进行文件编辑的可见目录。\n\n`.git` 目录下的关键子目录及功能如下：\n\n*   **`objects` 目录**：Git 的**核心数据库**，存储所有版本数据，包括文件内容（blob）、目录结构（tree）和提交记录（commit）。每个对象通过SHA-1哈希值唯一标识。\n*   **`refs` 目录**：存储**引用**，即指向特定提交的指针。主要包括`heads/`（本地分支）和`tags/`（标签），其中`heads/`下的文件记录了分支最新提交的哈希值。\n*   **`HEAD` 文件**：指向**当前所在分支**的引用。通常内容为`ref: refs/heads/main`，表明当前在`main`分支上工作。\n*   **`config` 文件**：存储**本地仓库的配置**，如远程仓库地址、用户信息等，优先级高于全局配置。\n*   **`index` 文件**：**暂存区（Stage）的元数据**，记录了下一次将要提交的文件状态和路径信息。\n\n理解此结构有助于掌握 Git 如何追踪变更、管理分支及高效存储数据。"}];