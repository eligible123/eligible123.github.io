const QUESTIONS = [{"num": 1, "title": "编写 SQL，查询所有用户中年龄最大的用户信息", "difficulty": "简单", "difficulty_emoji": "🟢", "tags": "SQL, SQL基础, SQL网站场景", "answer": "要查询所有用户中年龄最大的用户信息，假设数据库中存在用户表（例如 `users`），包含 `id`、`name`、`age` 等字段。核心思路是先找到年龄的最大值，再筛选出年龄等于该值的所有用户。以下是两种常用SQL写法：\n\n1. **使用子查询**：\n   ```sql\n   SELECT * FROM users WHERE age = (SELECT MAX(age) FROM users);\n   ```\n   这种方法适用于所有SQL数据库，子查询 `SELECT MAX(age) FROM users` 计算出最大年龄，外部查询选择年龄等于该值的用户。如果有多名用户年龄相同且都是最大，会全部返回。\n\n2. **使用窗口函数（适用于支持SQL标准的数据库，如PostgreSQL、MySQL 8.0+）**：\n   ```sql\n   SELECT * FROM (\n       SELECT *, RANK() OVER (ORDER BY age DESC) AS rnk FROM users\n   ) ranked WHERE rnk = 1;\n   ```\n   通过 `RANK()` 函数对年龄降序排名，年龄最大的用户排名为1，然后筛选排名等于1的记录。这种方法同样能处理多个最大年龄用户的情况。\n\n在面试中，推荐优先展示子查询方法，因为它更基础且兼容性强。解释时强调：首先通过 `MAX()` 函数获取最大年龄，然后用 `WHERE` 子句精确匹配，确保结果完整。如果面试官追问性能优化，可以提到在 `age` 字段上建立索引以提高查询效率。最后，注意实际应用中需根据数据库系统调整语法，例如"}, {"num": 2, "title": "编写 SQL，查询每个页面的第一次访问日期", "difficulty": "简单", "difficulty_emoji": "🟢", "tags": "SQL, SQL基础, SQL网站场景", "answer": "```sql\nSELECT \n    page,\n    MIN(access_date) AS first_access_date\nFROM page_views\nGROUP BY page;\n```\n\n该查询通过 `GROUP BY page` 将同一页面的所有访问记录分组，然后用聚合函数 `MIN(access_date)` 提取每个组中最早访问的日期。最终返回两列：页面标识及其首次访问日期。若需按访问时间排序，可添加 `ORDER BY first_access_date` 子句。实际场景中需确认表名（如示例中的 `page_views`）和字段名（如 `page`、`access_date`）是否与数据结构匹配。"}, {"num": 3, "title": "编写 SQL，查询用户表中年龄在 25 岁及以下且性别为 '女' 的用户的用户名", "difficulty": "简单", "difficulty_emoji": "🟢", "tags": "SQL, SQL基础, SQL网站场景", "answer": "要查询用户表中年龄在25岁及以下且性别为“女”的用户的用户名，可以使用以下SQL语句：\n\n```sql\nSELECT username\nFROM users\nWHERE age <= 25 AND gender = '女';\n```\n\n**核心要点说明：**\n\n1.  **SELECT 子句**：指定需要查询的列，即 `username`。\n2.  **FROM 子句**：指定数据来源的表，即用户表 `users`。\n3.  **WHERE 子句**：用于设置筛选条件，是本题的关键。\n    *   `age <= 25`：表示年龄小于或等于25岁。\n    *   `gender = '女'`：表示性别为“女”。\n    *   使用 `AND` 逻辑运算符连接两个条件，确保同时满足。\n\n**实际使用时的考虑：**\n*   请根据实际数据库表结构调整表名（`users`）和字段名（`username`, `age`, `gender`）。\n*   如果 `age` 字段允许 `NULL`，此查询会自动排除这些记录。\n*   为提高查询效率，可在 `age` 和 `gender` 字段上建立索引。\n*   在实际应用中，应使用参数化查询以防止SQL注入。"}, {"num": 4, "title": "编写 SQL，查询用户表中年龄在 25 岁及以下的所有用户的用户名和年龄", "difficulty": "简单", "difficulty_emoji": "🟢", "tags": "SQL, SQL基础, SQL网站场景", "answer": "```sql\nSELECT username, age \nFROM users \nWHERE age <= 25;\n```\n\n**核心要点说明：**\n1. **字段选择**：使用 `SELECT` 明确指定 `username` 和 `age` 两个目标字段\n2. **表来源**：`FROM users` 指定数据来源为用户表\n3. **筛选条件**：`WHERE age <= 25` 实现年龄小于等于25岁的精确过滤\n4. **数据类型**：假设 `age` 字段为数值类型，可直接进行数值比较\n5. **性能优化**：若表数据量大，可在 `age` 字段上建立索引提升查询效率\n\n**补充说明：**\n- 如需去重可添加 `DISTINCT`：`SELECT DISTINCT username, age`\n- 如需排序可添加：`ORDER BY age ASC, username ASC`\n- 处理NULL值：若需排除年龄为NULL的记录，可添加 `AND age IS NOT NULL`"}, {"num": 5, "title": "编写 SQL，查询用户表中性别为 '男' 的所有用户的用户名", "difficulty": "简单", "difficulty_emoji": "🟢", "tags": "SQL, SQL基础, SQL网站场景", "answer": "```sql\nSELECT user_name \nFROM users \nWHERE gender = '男';\n```\n\n**答案要点：**  \n1. **选择字段**：使用 `SELECT` 指定查询 `user_name`（用户名字段）。  \n2. **指定表名**：`FROM users` 表示从用户表中读取数据。  \n3. **条件过滤**：`WHERE gender = '男'` 筛选性别为“男”的记录。  \n4. **注意事项**：实际场景中需确保字段名与数据库一致，如性别字段可能为 `sex`，且数据存储可能为“M”/“F”，需根据表结构调整。  \n\n**扩展建议**（面试时可补充）：  \n- 若需去重，可使用 `SELECT DISTINCT user_name`。  \n- 若数据量大，建议在 `gender` 字段建立索引以优化查询性能。"}, {"num": 6, "title": "编写 SQL，查询用户表中每个性别的平均年龄，并按性别分组", "difficulty": "简单", "difficulty_emoji": "🟢", "tags": "SQL, SQL基础, SQL网站场景", "answer": "```sql\nSELECT gender, AVG(age) AS avg_age\nFROM users\nGROUP BY gender;\n```\n\n**答案解析：**\n\n1.  **核心逻辑**：此查询使用 `GROUP BY` 子句按 `gender`（性别）字段对数据进行分组，确保后续的聚合计算针对每一组（即每个性别）独立进行。\n2.  **聚合计算**：`AVG(age)` 聚合函数用于计算每个分组内 `age`（年龄）字段的平均值，并将其命名为 `avg_age`，使结果更易读。\n3.  **结果呈现**：查询将返回两列：性别 (`gender`) 和对应的平均年龄 (`avg_age`)。例如，可能返回 `Male` 和其平均值，以及 `Female` 和其平均值。\n4.  **要点总结**：此SQL语句是典型的分组聚合查询，其结构为`SELECT [分组字段], [聚合函数(计算字段)] FROM [表名] GROUP BY [分组字段]`。它准确地实现了“按性别分组并计算每组平均年龄”的需求。"}, {"num": 7, "title": "编写 SQL，查询用户表中每个性别的用户数量", "difficulty": "简单", "difficulty_emoji": "🟢", "tags": "SQL, SQL基础, SQL网站场景", "answer": "为了查询用户表中每个性别的用户数量，我们可以使用SQL的聚合函数和分组功能。假设用户表名为`users`，其中包含一个表示性别的列`gender`。\n\n核心SQL语句如下：\n\nSELECT gender, COUNT(*) AS user_count\nFROM users\nGROUP BY gender;\n\n在这个查询中：\n- `SELECT gender`：选择性别列作为分组依据，确保结果按性别显示。\n- `COUNT(*)`：统计每组中的记录数，即每个性别的用户数量，使用聚合函数高效计算。\n- `AS user_count`：给计数列起一个别名，使输出结果更清晰"}, {"num": 8, "title": "编写 SQL，查询用户表中注册日期在 2024 年 1 月 1 日之后的用户名和注册日期", "difficulty": "简单", "difficulty_emoji": "🟢", "tags": "SQL, SQL基础, SQL网站场景", "answer": "**SQL语句：**\n```sql\nSELECT username, registration_date\nFROM users\nWHERE registration_date > '2024-01-01';\n```\n\n**核心要点说明：**\n\n1.  **选择字段**：使用 `SELECT` 指定查询目标为 `username`（用户名）和 `registration_date`（注册日期）。\n2.  **指定表**：`FROM users` 指明数据来源是用户表。\n3.  **设置条件**：`WHERE registration_date > '2024-01-01'` 是关键条件，筛选出注册日期严格晚于2024年1月1日0点0分0秒的记录。使用标准日期格式（'YYYY-MM-DD'）确保兼容性与准确性。\n4.  **结果排序（可选但推荐）**：为提升可读性，可添加 `ORDER BY registration_date DESC` 按注册时间降序排列。\n\n**扩展思考（面试时可主动提及）：**\n*   **性能优化**：若用户表数据量大，应在 `registration_date` 字段上建立索引以加速查询。\n*   **边界确认**：需与产品方明确“之后”是否包含‘2024-01-01’当天。若包含，条件应改为 `>=`。\n*   **时区处理**：若系统涉及时区，日期比较需统一为数据库的时区标准。\n\n**示例输出：**\n| username | registration_date |\n|----------|-------------------|\n| 张三     | 2024-02-15        |\n| 李四     | 2024-01-20        |\n\n此答案涵盖了编写SQL的逻辑步骤、关键语法点以及实际开发中需要考虑的潜在问题，体现了扎实的基础和工程化思维。"}, {"num": 9, "title": "编写 SQL，查询用户表中注册日期最早的 3 个用户的用户名和注册日期", "difficulty": "简单", "difficulty_emoji": "🟢", "tags": "SQL, SQL基础, SQL网站场景", "answer": "```sql\nSELECT 用户名, 注册日期\nFROM 用户表\nORDER BY 注册日期 ASC\nLIMIT 3;\n```\n\n**说明**：  \n- `ORDER BY 注册日期 ASC` 按注册日期升序排列（最早的排最前）  \n- `LIMIT 3` 只返回前3条记录  \n- 若数据库为 SQL Server，可将 `LIMIT 3` 替换为 `SELECT TOP 3`  \n- 假设用户表字段为“用户名”“注册日期”，实际需根据表结构调整字段名"}, {"num": 10, "title": "编写 SQL，查询用户表中用户名包含 '张' 字的所有用户", "difficulty": "简单", "difficulty_emoji": "🟢", "tags": "SQL, SQL基础, SQL网站场景", "answer": "# 查询用户名包含'张'字的所有用户\n\n## SQL语句\n\n```sql\nSELECT *\nFROM users\nWHERE username LIKE '%张%';\n```\n\n## 核心知识点\n\n**LIKE 操作符**：用于在 WHERE 子句中进行模糊匹配。\n\n**通配符说明**：\n- `%`：匹配任意数量的字符（包括零个字符）\n- `_`：匹配单个字符\n\n`'%张%'` 表示\"张\"字前后可以有任意字符，能匹配\"张三\"、\"小张\"、\"张伟强\"等。\n\n## 常见变体\n\n```sql\n-- 查询以'张'开头的用户（如\"张三\"）\nSELECT * FROM users WHERE username LIKE '张%';\n\n-- 查询以'张'结尾的用户（如\"小张\"）\nSELECT * FROM users WHERE username LIKE '%张';\n\n-- 精确三个字且中间是'张'\nSELECT * FROM users WHERE username LIKE '_张_';\n```\n\n## 补充要点\n\n1. **性能考虑**：`%张%` 这种前缀带通配符的查询无法使用普通索引，数据量大时会导致全表扫描。可以考虑全文索引优化。\n\n2. **字符集问题**：中文查询需确保数据库和表的字符集为 UTF-8 或支持中文的编码，避免乱码。\n\n3. **SQL 注入风险**：实际开发中应使用参数化查询，而非直接拼接字符串：\n```sql\n-- 使用预编译语句示例\nSELECT * FROM users WHERE username LIKE CONCAT('%', ?, '%');\n```\n\n4. **大小写敏感**：MySQL 默认对中文不区分大小写，但英文字符是否敏感取决于字段的 collation 设置。"}, {"num": 11, "title": "编写 SQL，查询用户表中，注册日期在 2024 年 1 月 1 日和 2024 年 6 月 30 日之间的所有用户", "difficulty": "简单", "difficulty_emoji": "🟢", "tags": "SQL, SQL基础, SQL网站场景", "answer": "```sql\nSELECT *\nFROM users\nWHERE register_date BETWEEN '2024-01-01' AND '2024-06-30';\n```\n\n**核心要点解析：**\n\n1.  **语法选择**：使用 `BETWEEN` 运算符可以简洁地表达连续的区间查询，条件等价于 `register_date >= '2024-01-01' AND register_date <= '2024-06-30'`。`BETWEEN` 包含两端的边界值，符合题目要求。\n\n2.  **日期格式**：SQL标准中日期字面量通常使用 `'YYYY-MM-DD'` 格式，以确保在不同数据库系统中的兼容性和明确性。\n\n3.  **性能考虑**：如果 `register_date` 列已建立索引，此查询能有效利用索引加速。对于大数据量表，应确保该列有索引。\n\n**面试中可补充的延伸点：**\n*   若字段包含时间部分（如`DATETIME`），则 `'2024-06-30'` 可能仅代表当天的00:00:00，需根据业务需求判断是否需包含6月30日整天。更精确的写法是 `<= '2024-06-30 23:59:59'`。\n*   可讨论如何验证查询结果的正确性，例如通过 `COUNT(*)` 聚合或抽样检查。\n*   了解所使用的具体数据库（如MySQL, PostgreSQL）对日期处理是否有细微差异。"}, {"num": 12, "title": "编写 SQL，查询访问记录表中所有页面的页面 ID 和访问日期", "difficulty": "简单", "difficulty_emoji": "🟢", "tags": "SQL, SQL基础, SQL网站场景", "answer": "本题考察基础SQL查询能力，要求从访问记录表中提取页面ID和日期信息。核心是使用SELECT语句指定字段，并通过FROM指定数据源。\n\n标准查询如下：\n```sql\nSELECT page_id, visit_date \nFROM access_log;\n```\n\n**关键点说明：**\n1. **字段选择**：`page_id`和`visit_date`需与实际表结构匹配，若字段名为英文请确认拼写。\n2. **去重处理**：若需去重，可添加`DISTINCT`：`SELECT DISTINCT page_id, visit_date`。\n3. **排序优化**：通常建议按时间降序排列以便查看最新记录：`ORDER BY visit_date DESC`。\n4. **索引建议**：若数据量较大，可为`page_id`和`visit_date`建立索引提升查询性能。\n\n**实际应用示例：**\n```sql\nSELECT DISTINCT page_id, visit_date \nFROM access_log \nWHERE visit_date >= '2023-01-01'  -- 添加日期筛选条件\nORDER BY visit_date DESC \nLIMIT 100;  -- 限制返回条数\n```\n\n面试时需说明：基础查询应简洁明确，实际场景中需结合业务需求添加过滤、排序等条件，并关注执行效率。"}, {"num": 13, "title": "编写 SQL，查询访问记录表中每个用户的总访问次数", "difficulty": "简单", "difficulty_emoji": "🟢", "tags": "SQL, SQL基础, SQL网站场景", "answer": "假设访问记录表为 `visits`，包含字段 `user_id`（用户ID），查询每个用户总访问次数的 SQL 如下：\n\n```sql\nSELECT \n    user_id, \n    COUNT(*) AS total_visits \nFROM \n    visits \nGROUP BY \n    user_id \nORDER BY \n    total_visits DESC;\n```\n\n**核心要点：**\n1. 使用 `GROUP BY user_id` 对数据进行用户分组。\n2. 通过 `COUNT(*)` 统计每组内的记录数，即总访问次数。\n3. 使用 `ORDER BY total_visits DESC` 按访问次数降序排列，便于快速查看高频访问用户。\n\n若需筛选特定条件（如时间范围），可在 `GROUP BY` 前添加 `WHERE` 子句。此写法适用于 MySQL、PostgreSQL 等主流数据库，是面试中常见且基础的分组聚合查询示例。"}, {"num": 14, "title": "编写 SQL，查询访问记录表中每个页面的总访问次数", "difficulty": "简单", "difficulty_emoji": "🟢", "tags": "SQL, SQL基础, SQL网站场景", "answer": "```sql\nSELECT page_url, COUNT(*) AS total_views\nFROM access_log\nGROUP BY page_url;\n```\n\n**核心解释：**\n该查询使用 `GROUP BY page_url` 将访问记录表 `access_log` 按页面地址（`page_url`）进行分组，随后对每一组使用聚合函数 `COUNT(*)` 统计记录数，即为该页面的总访问次数。\n\n**关键要点：**\n1.  **分组聚合**：`GROUP BY` 是处理“每个/各类别”需求的核心，它将数据划分为子集。\n2.  **计数函数**：`COUNT(*)` 统计分组后的行数，是计算总次数的直接方式。\n3.  **结果列命名**：使用 `AS total_views` 为统计列赋予清晰别名，增强结果可读性。\n\n**场景扩展（可提及）：**\n- 若需筛选特定时间段，可在 `GROUP BY` 前添加 `WHERE` 子句。\n- 为提高 `GROUP BY` 在大表上的性能，通常在 `page_url` 字段上建立索引。\n- `COUNT(page_url)` 与 `COUNT(*)` 在无NULL时结果一致，但后者语义更明确。"}, {"num": 15, "title": "编写 SQL，查询访问记录表中用户IP为 '192.168.1.1' 的所有记录", "difficulty": "简单", "difficulty_emoji": "🟢", "tags": "SQL, SQL基础, SQL网站场景", "answer": "```sql\nSELECT * FROM 访问记录表 WHERE 用户IP = '192.168.1.1';\n```\n\n该查询直接、高效，是面试中考察SQL基础的典型问题。其核心要点如下：\n\n1.  **明确查询目标**：我们需要从“访问记录表”中获取**所有记录**，而非统计或聚合，因此使用`SELECT *`。在实际应用中，若表字段较多，建议显式列出所需字段（如`SELECT 访问时间, 请求路径, 用户代理`），以提升可读性和性能。\n\n2.  **精准筛选条件**：使用`WHERE`子句进行条件过滤。`用户IP = '192.168.1.1'` 是一个等值查询，确保仅返回该特定IP地址的所有访问记录。这是该SQL语句正确执行的核心。\n\n3.  **结果与场景**：执行后，数据库将返回用户IP为`192.168.1.1`的完整历史访问明细。在网站运维或数据分析场景中，此查询可用于追踪特定IP的行为、排查问题或分析用户模式。\n\n4.  **扩展思考（可提及以展现深度）**：若表数据量极大，可考虑添加分页（如`LIMIT`子句）或索引（在`用户IP`字段上创建索引）来优化查询性能。"}, {"num": 16, "title": "编写 SQL，查询访问记录表中，每个页面最近一次访问的日期", "difficulty": "简单", "difficulty_emoji": "🟢", "tags": "SQL, SQL基础, SQL网站场景", "answer": "查询每个页面的最近访问日期，可使用聚合函数MAX配合GROUP BY实现。\n\n```sql\nSELECT page_url, MAX(visit_date) AS latest_visit\nFROM visit_records\nGROUP BY page_url;\n```\n\n核心要点说明：\n1. **MAX(visit_date)** 用于获取分组中的最大日期值，即最近访问时间\n2. **GROUP BY page_url** 按页面URL进行分组，确保每个页面独立计算\n3. 如需筛选特定时间段，可增加WHERE条件；如需排序结果，可添加ORDER BY子句\n\n此查询直接返回每个页面及其对应的最新访问日期，满足网站访问分析的常见需求。"}, {"num": 17, "title": "编写 SQL，查询页面表中所有页面的页面 ID，并按页面 ID 升序排序", "difficulty": "简单", "difficulty_emoji": "🟢", "tags": "SQL, SQL基础, SQL网站场景", "answer": "```sql\nSELECT page_id FROM pages ORDER BY page_id ASC;\n```\n\n**核心要点解析：**\n\n1. **基础语法**：使用 `SELECT` 指定查询字段（`page_id`），`FROM` 指定数据源表（假设表名为 `pages`），`ORDER BY` 配合 `ASC` 实现升序排序。\n2. **关键细节**：\n   - 若表名或字段名与实际数据库不一致，需根据实际表结构调整（如 `SELECT id FROM webpage ORDER BY id`）。\n   - 默认排序即为升序，显式写 `ASC` 可增强代码可读性。\n   - 若页面ID可能包含 `NULL`，升序排序时 `NULL` 值通常会被置于结果开头。\n3. **场景适配**：在网站场景中，此查询常用于后台管理页面列表展示、数据导出或与其他表关联查询（如关联页面内容表）。若数据量较大，可结合 `LIMIT` 实现分页，但题目未要求，故保持简单。\n4. **扩展思考**：实际开发中需考虑索引优化（如 `page_id` 是否有索引），以及是否需去重（但页面ID通常为主键，无需 `DISTINCT`）。\n\n此答案满足面试中简洁、准确、可扩展讨论的要求。"}, {"num": 18, "title": "编写 SQL，查询页面表中页面路径为 '/home' 的页面名称", "difficulty": "简单", "difficulty_emoji": "🟢", "tags": "SQL, SQL基础, SQL网站场景", "answer": "SELECT name FROM pages WHERE url = '/home';\n\n上述 SQL 语句用于从 `pages` 表中查询页面路径为 `/home` 的记录，并返回其 `name` 字段。\n\n核心要点在于使用 `WHERE` 子句进行精确匹配过滤。`SELECT name` 指定了查询结果只包含 `name` 列，提高了查询效率并返回所需数据。在网站场景中，这类查询常用于根据 URL 路径快速定位和获取页面标题、菜单名等关键信息，是后端数据获取或管理后台数据展示的基础操作。"}, {"num": 19, "title": "编写 SQL，使用开窗函数查询每个用户最近一次访问的页面路径及其访问日期", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "SQL, SQL进阶, SQL网站场景", "answer": "```sql\nSELECT\n    user_id,\n    page_path AS last_visited_page,\n    visit_date AS last_visit_date\nFROM (\n    SELECT\n        user_id,\n        page_path,\n        visit_date,\n        ROW_NUMBER() OVER (\n            PARTITION BY user_id\n            ORDER BY visit_date DESC\n        ) AS rn\n    FROM user_visit_log\n) t\nWHERE rn = 1;\n```\n\n**核心要点说明：**\n1.  **开窗函数 `ROW_NUMBER()`**：为每个用户（`PARTITION BY user_id`）的访问记录按时间降序（`ORDER BY visit_date DESC`）编号。\n2.  **子查询**：必须将开窗函数置于子查询中，因为 `WHERE` 子句无法直接引用它。\n3.  **主查询过滤**：在外部查询中，通过 `WHERE rn = 1` 筛选出每个用户编号为 1 的记录，即其最近一次访问。\n4.  **场景适用**：此方法效率较高，避免了相关子查询，是获取分组内最新记录的经典范式。"}, {"num": 20, "title": "编写 SQL，使用开窗函数查询每个页面的累计访问次数", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "SQL, SQL进阶, SQL网站场景", "answer": "```sql\nSELECT \n    page_id, \n    visit_time, \n    COUNT(*) OVER (PARTITION BY page_id ORDER BY visit_time) AS cumulative_visits\nFROM visit_records;\n```\n\n**解析**：\n1. **核心逻辑**：使用`COUNT(*)`配合`OVER`子句实现开窗函数。\n2. **分区与排序**：`PARTITION BY page_id`按页面分组，确保每个页面独立计算；`ORDER BY visit_time`按访问时间排序，实现累计计数。\n3. **适用场景**：该查询会返回每条访问记录及其对应的页面累计访问次数，结果集包含`page_id`（页面标识）、`visit_time`（访问时间）和`cumulative_visits`（累计次数）三列。\n\n**扩展说明**：\n- 若需统计每个页面的总访问次数（非累计），可改用`COUNT(*) OVER (PARTITION BY page_id)`。\n- 实际使用时需根据表结构调整字段名（如`visit_records`、`page_id`、`visit_time`等）。"}, {"num": 21, "title": "编写 SQL，查询在 2024 年 7 月 1 日访问了 '/home' 页面但没有访问 '/contact' 页面的用户的用户名（去重）", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "SQL, SQL进阶, SQL网站场景", "answer": "针对此查询，我提供两种常用方法。假设表名为 `visit_log`，包含字段 `user_name` (用户名), `visit_time` (访问时间), `page_path` (页面路径)。\n\n**方法一：使用 `NOT EXISTS` (推荐，逻辑清晰)**\n```sql\nSELECT DISTINCT user_name\nFROM visit_log v1\nWHERE DATE(visit_time) = '2024-07-01'\n  AND page_path = '/home'\n  AND NOT EXISTS (\n    SELECT 1\n    FROM visit_log v2\n    WHERE v2.user_name = v1.user_name\n      AND DATE(v2.visit_time) = '2024-07-01'\n      AND v2.page_path = '/contact'\n  );\n```\n**核心思路：**\n1. **主查询**：筛选出在2024-07-01访问过`/home`的所有用户记录。\n2. **子查询/关联条件**：使用 `NOT EXISTS` 子查询，检查同一位用户在同一天**没有**访问`/contact`的记录。只有当子查询无结果时，主查询的记录才会被保留。\n3. `SELECT DISTINCT` 用于对最终结果用户名进行去重。\n\n**方法二：使用 `LEFT JOIN` + `IS NULL` (性能可能更优)**\n```sql\nSELECT DISTINCT v1.user_name\nFROM visit_log v1\nLEFT JOIN visit_log v2\n  ON v1.user_name = v2.user_name\n  AND DATE(v2.visit_time) = '2024-07-01'\n  AND v2.page_path = '/contact'\nWHERE DATE(v1.visit_time) = '2024-07-01'\n  AND v1.page_path = '/home'\n  AND v2.user_name IS NULL;\n```\n**核心思路：**\n1. 将同一个表根据不同的条件进行自连接。\n2. 连接条件 (`ON`) 同时确保用户相同、日期相同，且右表 (`v2`) 对应访问 `/contact` 的记录。\n3. `WHERE` 子句确保左表 (`v1`) 是访问 `/home` 的记录。\n4. `v2.user_name IS NULL` 是关键，它筛选出在左表中存在（访问了/home），但在右表中没有匹配记录（即未访问/contact）的用户。同样使用 `DISTINCT` 去重。\n\n**两种方法均能正确求解。** 在面试中，首选能清晰表达业务逻辑的 `NOT EXISTS` 写法，并简要说明其“过滤未满足否定条件记录”的逻辑，这能体现你对SQL集合操作的深刻理解。"}, {"num": 22, "title": "编写 SQL，查询在 2024 年 7 月 1 日访问次数最多的页面的页面路径", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "SQL, SQL进阶, SQL网站场景", "answer": "```sql\nSELECT page_path, COUNT(*) AS visit_count\nFROM access_log\nWHERE DATE(access_time) = '2024-07-01'\nGROUP BY page_path\nORDER BY visit_count DESC\nLIMIT 1;\n```\n\n**核心思路：**\n1. **筛选日期**：使用 `WHERE DATE(access_time) = '2024-07-01'` 提取2024年7月1日的数据。\n2. **分组聚合**：按 `page_path` 分组，并用 `COUNT(*)` 统计每个页面的访问次数。\n3. **排序取最大**：通过 `ORDER BY visit_count DESC` 按访问次数降序排序，`LIMIT 1` 取出访问量最高的记录。\n\n**优化与边界考虑：**\n若存在多个页面访问次数并列第一，此查询仅返回一条。如需全部并列记录，可改用窗口函数：\n```sql\nWITH ranked AS (\n  SELECT page_path, \n         RANK() OVER (ORDER BY COUNT(*) DESC) AS rnk\n  FROM access_log\n  WHERE DATE(access_time) = '2024-07-01'\n  GROUP BY page_path\n)\nSELECT page_path, rnk \nFROM ranked WHERE rnk = 1;\n```\n该方案通过 `RANK()` 保留所有并列第一的页面，更严谨地覆盖业务场景。面试中可根据时间复杂度与需求选择表述。"}, {"num": 23, "title": "编写 SQL，查询在 2024 年 7 月 1 日访问过 '/home' 页面且年龄大于 25 岁的用户的用户名（去重）", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "SQL, SQL进阶, SQL网站场景", "answer": "```sql\nSELECT DISTINCT u.username\nFROM users u\nINNER JOIN access_logs a ON u.user_id = a.user_id\nWHERE a.visit_date >= '2024-07-01'\n  AND a.visit_date < '2024-07-02'\n  AND a.page_url = '/home'\n  AND u.age > 25;\n```\n\n**核心要点说明：**\n\n1. **多表关联**：通过`INNER JOIN`连接用户表（users）和访问日志表（access_logs），关联键为用户ID。\n2. **日期过滤**：使用日期范围条件（`>= '2024-07-01'`且`< '2024-07-02'`）精确筛选2024年7月1日的数据，避免时间精度问题。\n3. **条件筛选**：指定页面路径为`/home`，同时限制用户年龄大于25岁。\n4. **结果去重**：使用`DISTINCT`确保用户名唯一，避免同一用户多次访问产生重复记录。\n\n**扩展思考**：若表数据量较大，可考虑在`visit_date`、`page_url`和`age`字段上建立联合索引优化查询性能。此外，如果业务允许，也可使用`GROUP BY u.username`实现去重。"}, {"num": 24, "title": "编写 SQL，查询在 2024 年 7 月 1 日访问过 '/product/123' 页面的所有用户的用户 ID 和访问日期", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "SQL, SQL进阶, SQL网站场景", "answer": "```sql\nSELECT user_id, visit_date\nFROM 访问日志\nWHERE visit_date = '2024-07-01'\n  AND page_url = '/product/123';\n```\n\n**核心要点说明：**\n1. **字段与条件明确**：精准定位目标日期（`2024-07-01`）和页面路径（`/product/123`），避免全表扫描。\n2. **性能优化**：建议在 `visit_date` 和 `page_url` 字段上建立联合索引，提升查询效率。\n3. **扩展性考虑**：\n   - 若日期包含时间部分，需使用范围查询（如 `BETWEEN '2024-07-01 00:00:00' AND '2024-07-01 23:59:59'`）。\n   - 若页面路径存在动态参数（如 `/product/123?ref=abc`），需通过 `LIKE '/product/123%'` 或拆分路径字段处理。\n4. **数据一致性**：实际业务中需确认日期字段的时区、去重需求（是否多次访问同一页面），并考虑使用 `DISTINCT` 或聚合函数。\n\n**场景适配**：此查询适用于基础场景，面试时可进一步讨论索引设计、执行计划分析或分表分库场景下的优化策略。"}, {"num": 25, "title": "编写 SQL，查询在 2024 年 7 月 1 日访问过的所有用户的用户名（不能重复）", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "SQL, SQL进阶, SQL网站场景", "answer": "查询在指定日期访问过的用户名，通常需要关联访问日志表和用户信息表。假设存在以下表结构：\n- 用户表 `users`（含字段 `id`, `username`）\n- 访问日志表 `visits`（含字段 `user_id`, `visit_time`）\n\n核心思路：\n1. **筛选日期**：从访问日志表中筛选 `visit_time` 日期部分为 `2024-07-01` 的记录。\n2. **关联用户**：通过 `user_id` 关联用户表获取用户名。\n3. **去重输出**：使用 `DISTINCT` 确保用户名不重复。\n\nSQL 示例：\n```sql\nSELECT DISTINCT u.username\nFROM users u\nJOIN visits v ON u.id = v.user_id\nWHERE DATE(v.visit_time) = '2024-07-01';\n```\n\n**关键点说明**：\n- `DATE()` 函数用于提取 `visit_time` 的日期部分（若字段已为日期类型可省略）。\n- 使用 `JOIN` 确保只返回有访问记录的用户。\n- `DISTINCT` 直接在最外层保证用户名唯一性。\n\n**扩展考虑**：\n- 若访问日志数据量较大，可对 `visit_time` 建立索引优化查询。\n- 若表结构不同（如用户名直接在日志表中），可简化为单表查询并去重。"}, {"num": 26, "title": "编写 SQL，查询在 2024 年 7 月访问次数最多的 3 个页面的页面名称", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "SQL, SQL进阶, SQL网站场景", "answer": "要在2024年7月找出访问量最高的3个页面，核心思路是：筛选出指定时间范围内的访问记录，按页面分组统计访问次数，然后按次数降序排序并取前三。\n\n假设我们有访问日志表 `page_views`，其中包含页面名称字段 `page_name` 和访问时间字段 `visit_date`。\n\n```sql\nSELECT \n    page_name,\n    COUNT(*) AS visit_count\nFROM \n    page_views\nWHERE \n    visit_date >= '2024-07-01' \n    AND visit_date < '2024-08-01'\nGROUP BY \n    page_name\nORDER BY \n    visit_count DESC\nLIMIT 3;\n```\n\n**代码解释：**\n1.  **`WHERE` 子句**：限定查询的时间范围为2024年7月（使用左闭右开区间，避免边界问题）。\n2.  **`GROUP BY page_name`**：将记录按页面名称分组，以便对每个页面的访问量进行独立计算。\n3.  **`COUNT(*)`**：聚合函数，用于计算每个分组（即每个页面）的访问总次数。\n4.  **`ORDER BY visit_count DESC`**：将统计结果按访问次数从高到低排序。\n5.  **`LIMIT 3`**：取排序后的前3条记录，即为访问量最高的3个页面。\n\n此查询高效、清晰，能直接回答问题，是面试中的标准解法。"}, {"num": 27, "title": "编写 SQL，查询在 2024 年访问过网站的所有用户中，访问次数最多的前五名用户的用户名及其访问次数", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "SQL, SQL进阶, SQL网站场景", "answer": "**查询思路：**\n1. 筛选2024年的访问记录。\n2. 按用户分组统计访问次数。\n3. 按访问次数降序排序，取前五名。\n\n**SQL实现：**\n```sql\nSELECT \n    user_name,\n    COUNT(*) AS visit_count\nFROM website_visits\nWHERE visit_date >= '2024-01-01' AND visit_date < '2025-01-01'\nGROUP BY user_name\nORDER BY visit_count DESC\nLIMIT 5;\n```\n\n**核心要点：**\n- 使用`WHERE`过滤2024年数据，避免全表扫描。\n- `GROUP BY`聚合计算每个用户的访问次数。\n- `ORDER BY`结合`LIMIT`实现Top-N查询。\n- 可考虑为`visit_date`字段添加索引优化查询性能。"}, {"num": 28, "title": "编写 SQL，查询总访问次数大于 2 的每个页面的总访问次数", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "SQL, SQL进阶, SQL网站场景", "answer": "【题目】编写 SQL，查询总访问次数大于 2 的每个页面的总访问次数。\n\n**核心考点**：考察对 `GROUP BY`、聚合函数 `COUNT()` 以及分组后筛选条件 `HAVING` 子句的运用。\n\n**SQL 答案**：\n假设有一张页面访问日志表 `page_visits`，包含 `page_id`（页面ID）和 `visit_id`（访问记录ID）等字段。\n\n```sql\nSELECT page_id, COUNT(visit_id) AS total_visits\nFROM page_visits\nGROUP BY page_id\nHAVING COUNT(visit_id) > 2;\n```\n\n**思路解析**：\n1.  **`GROUP BY page_id`**：首先按页面ID进行分组，将同一页面的所有访问记录归为一组。\n2.  **`COUNT(visit_id) AS total_visits`**：使用聚合函数 `COUNT` 统计每个分组（即每个页面）中的访问次数，并起别名 `total_visits`。\n3.  **`HAVING COUNT(visit_id) > 2`**：`HAVING` 子句用于对 **分组聚合后的结果** 进行筛选，这里筛选出总访问次数大于2的分组。\n\n**关键点强调**：\n- **`WHERE` 与 `HAVING` 的区别**：`WHERE` 在分组前对原始行进行过滤，而 `HAVING` 在分组后对聚合结果进行过滤。本题需要过滤的是“总次数”（聚合结果），因此必须使用 `HAVING`。\n\n**执行顺序总结**：理解此查询有助于体现对SQL执行逻辑的掌握：`FROM`（确定数据源）"}, {"num": 29, "title": "编写 SQL，查询所有在 2024 年 7 月访问过网站的用户的用户名（去重）", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "SQL, SQL进阶, SQL网站场景", "answer": "假设有用户表 `users`（字段：`user_id`, `username`）和访问记录表 `website_visits`（字段：`visit_id`, `user_id`, `visit_time`）。可通过以下 SQL 查询：\n\n```sql\nSELECT DISTINCT u.username\nFROM users u\nJOIN website_visits w ON u.user_id = w.user_id\nWHERE w.visit_time >= '2024-07-01' AND w.visit_time < '2024-08-01';\n```\n\n**核心要点**：\n1. **使用 `JOIN` 连接表**：通过 `user_id` 关联用户表和访问记录表。\n2. **日期范围筛选**：用 `WHERE` 限定 `visit_time` 在 2024 年 7 月内（包含 7 月 1 日，不包含 8 月 1 日）。\n3. **去重处理**：用 `DISTINCT` 确保用户名唯一，避免同一用户多次访问导致的重复。\n\n**补充说明**：若访问记录表直接包含用户名字段，可简化为单表查询，但仍需使用 `DISTINCT` 去重。实际面试中可简述此逻辑，并指出需根据表结构调整细节。"}, {"num": 30, "title": "编写 SQL，查询所有注册日期在 2024 年 6 月之后且访问过 '/contact' 页面的用户的用户名", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "SQL, SQL进阶, SQL网站场景", "answer": "假设数据库中存在两张表：用户表（`users`），包含字段 `user_id`、`username`（用户名）、`registration_date`（注册日期）；以及页面访问表（`page_views`），包含字段 `user_id`、`page_visited`（访问页面）。要查询满足条件的用户名，可以使用 SQL 的连接与子查询。\n\n核心查询如下：\n```sql\nSELECT DISTINCT u.username\nFROM users u\nWHERE u.registration_date > '2024-06-01'\n  AND EXISTS (\n    SELECT 1\n    FROM page_views pv\n    WHERE pv.user_id = u.user_id\n      AND pv.page_visited = '/contact'\n  );\n```\n此查询的逻辑清晰：首先，在 `WHERE` 子句中筛选注册日期在 2024 年 6 月 1 日之后的用户；其次，通过 `EXISTS` 子查询检查该用户是否在 `page_views` 表中访问过 `/contact` 页面。使用 `EXISTS` 可提高效率，避免重复结果。\n\n关键要点包括：日期比较使用具体日期字符串"}, {"num": 31, "title": "编写 SQL，查询所有用户的用户名和注册日期，要求格式化注册日期为 'YYYY-MM' 格式", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "SQL, SQL进阶, SQL网站场景", "answer": "```sql\n-- MySQL 写法\nSELECT \n    username,\n    DATE_FORMAT(registration_date, '%Y-%m') AS formatted_date\nFROM users;\n```\n\n**关键点说明：**\n1. **DATE_FORMAT函数**：MySQL中用于格式化日期，`'%Y-%m'`表示四位年份和两位月份\n2. **AS别名**：为格式化后的列设置可读的别名\n3. **其他数据库写法**：\n   - PostgreSQL：`TO_CHAR(registration_date, 'YYYY-MM')`\n   - SQL Server：`FORMAT(registration_date, 'yyyy-MM')` 或 `CONVERT(VARCHAR(7), registration_date, 120)`\n\n**注意事项：**\n- 若注册日期含时间部分，格式化会自动忽略\n- 可添加`WHERE`条件筛选特定时间范围的用户\n- 考虑空值处理，可用`IFNULL`或`COALESCE`函数"}, {"num": 32, "title": "编写 SQL，查询所有页面中访问次数最多的页面的页面名称", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "SQL, SQL进阶, SQL网站场景", "answer": "可以按以下步骤编写SQL查询：\n\n**思路分析：**\n1. 首先需要对页面访问记录进行统计，计算每个页面的访问次数\n2. 然后找出访问次数的最大值\n3. 最后筛选出访问次数等于最大值的页面名称\n\n**SQL实现：**\n```sql\nSELECT 页面名称\nFROM 页面访问表\nGROUP BY 页面名称\nHAVING COUNT(*) = (\n    SELECT MAX(访问次数) \n    FROM (\n        SELECT 页面名称, COUNT(*) AS 访问次数 \n        FROM 页面访问表 \n        GROUP BY 页面名称\n    ) AS 统计表\n);\n```\n\n**核心要点说明：**\n1. 使用 `GROUP BY` 对页面名称进行分组统计\n2. 通过 `COUNT(*)` 计算每个页面的访问次数\n3. 子查询先获取每个页面的访问次数，外层查询再找出最大访问次数\n4. 最后通过 `HAVING` 筛选出访问次数等于最大值的页面\n\n**优化考虑：**\n如果数据库支持窗口函数，也可用 `RANK()` 或 `DENSE_RANK()` 简化查询，但上述写法兼容性更好。注意处理可能有多个页面并列第一的情况，该查询会返回所有符合条件的页面名称。"}, {"num": 33, "title": "编写 SQL，查询每个用户名的平均访问时长，并且平均访问时长超过 100 秒的用户名", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "SQL, SQL进阶, SQL网站场景", "answer": "假设有用户表`users`（user_id, username）和访问记录表`visits`（visit_id, user_id, visit_duration），SQL如下：\n```sql\nSELECT \n    u.username,\n    AVG(v.visit_duration) AS avg_duration\nFROM \n    users u\nJOIN \n    visits v ON u.user_id = v.user_id\nGROUP BY \n    u.username\nHAVING \n    AVG(v.visit_duration) > 100\nORDER BY \n    avg_duration DESC;\n```\n\n**核心要点说明**：\n1. **连接查询**：通过`JOIN`关联用户表与访问表，获取每个用户的访问记录。\n2. **聚合计算**：使用`AVG()`函数按用户名分组计算平均访问时长。\n3. **分组筛选**：`HAVING`子句用于对聚合结果（平均时长）进行过滤，这是与`WHERE`的关键区别。\n4. **结果优化**：`ORDER BY`对结果排序，便于直观查看访问时长最长的用户。\n\n**补充**：若访问记录直接包含用户名字段，可省略`JOIN`，直接对`visits`表进行分组查询，但通常设计上会使用用户ID关联以确保数据一致性。"}, {"num": 34, "title": "编写 SQL，查询每个用户名访问的不同页面的数量，要求该数量大于 1", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "SQL, SQL进阶, SQL网站场景", "answer": "假设存在一个网站访问日志表 `user_visits`，包含列 `username`（用户名）和 `page_visited`（访问的页面）。要查询每个用户访问的不同页面数量，且该数量大于1，可以使用以下SQL查询：\n\n```sql\nSELECT username, COUNT(DISTINCT page_visited) AS distinct_pages_count\nFROM user_visits\nGROUP BY username\nHAVING COUNT(DISTINCT page_visited) > 1;\n```\n\n**核心要点解释：**\n- `GROUP BY username`：按用户名对记录进行分组，以便为每个用户独立计算。\n- `COUNT(DISTINCT page_visited)`：统计每个用户访问的不同页面数量，使用 `DISTINCT` 确保只计数唯一的页面，避免重复。\n- `HAVING` 子句：用于过滤分组后的结果，仅保留 `distinct_pages_count` 大于1的记录。注意，`WHERE` 子句不能用于聚合函数过滤，因此必须用 `HAVING`。\n\n此查询结构清晰，适用于面试场景，展示了SQL的聚合函数、分组和条件过滤的综合应用。在实际网站分析中，这有助于识别活跃"}, {"num": 35, "title": "编写 SQL，查询每个用户最近一次访问的页面路径及访问日期", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "SQL, SQL进阶, SQL网站场景", "answer": "**核心思路**：使用窗口函数为每个用户的访问记录按日期降序排名，筛选排名第一的记录。\n\n**实现代码**：\n```sql\nSELECT user_id, page_path, visit_date\nFROM (\n    SELECT \n        user_id,\n        page_path,\n        visit_date,\n        ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY visit_date DESC) AS rn\n    FROM user_access_logs\n) ranked\nWHERE rn = 1;\n```\n\n**关键说明**：\n1. **ROW_NUMBER()** 为每个用户的访问记录生成唯一序号\n2. **PARTITION BY user_id** 按用户分组\n3. **ORDER BY visit_date DESC** 按日期降序排列，确保最新记录序号为1\n4. 子查询筛选序号为1的记录，即最近访问\n\n**其他方法**：可使用关联子查询或MAX()函数实现，但窗口函数更简洁高效，适合生产环境。"}, {"num": 36, "title": "编写 SQL，查询每个用户的访问记录，并增加一个序号列，按访问日期排序", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "SQL, SQL进阶, SQL网站场景", "answer": "**核心思路：** 使用窗口函数为每个用户的访问记录按日期排序生成序号。\n\n**答案：**\n\n可以使用窗口函数 `ROW_NUMBER()` 来解决。该函数能为每个分区（即每个用户）内的记录按指定顺序（访问日期）分配一个唯一的连续序号。\n\n**基础查询语句：**\n\n```sql\nSELECT\n    user_id,\n    visit_date,\n    ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY visit_date) AS visit_seq\nFROM\n    user_visits;\n```\n\n**说明：**\n1.  `PARTITION BY user_id`: 将数据按用户ID分组，每个用户是一个独立的“窗口”。\n2.  `ORDER BY visit_date`: 在每个用户窗口内，按访问日期升序排序。\n3.  `ROW_NUMBER()`: 为排序后的每条记录分配从1开始的序号。\n\n**进阶说明：**\n面试中可补充：\n*   **降序排序**：若想显示最近访问为序号1，将`ORDER BY visit_date`改为`ORDER BY visit_date DESC`。\n*   **其他序号函数**：如需相同日期共享序号，可用`RANK()`；若需连续但共享序号，可用`DENSE_RANK()`。"}, {"num": 37, "title": "编写 SQL，查询每个用户访问的不同页面的总数量", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "SQL, SQL进阶, SQL网站场景", "answer": "```sql\nSELECT user_id, COUNT(DISTINCT page) AS unique_page_count\nFROM user_visits\nGROUP BY user_id;\n```\n\n**答案分析：**\n\n1. **问题核心**：统计每个用户访问过的不同页面数量，需要同时处理“用户维度分组”和“页面去重”两个关键点。\n\n2. **SQL实现**：\n   - 使用 `GROUP BY user_id` 按用户分组\n   - 使用 `COUNT(DISTINCT page)` 计算每组内不同的页面数量（自动忽略重复访问）\n   - 假设访问记录表包含 `user_id` 和 `page` 字段\n\n3. **扩展说明**：\n   - 若需处理空值，可改用 `COUNT(DISTINCT COALESCE(page, 'NULL'))`\n   - 大数据量场景下需在 `user_id` 和 `page` 字段建立联合索引优化性能\n   - 若需同时返回总访问次数，可增加 `COUNT(*)` 字段"}, {"num": 38, "title": "编写 SQL，查询每个页面在 2024 年 7 月的总访问次数，并按访问次数降序排列", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "SQL, SQL进阶, SQL网站场景", "answer": "```sql\nSELECT page_id, COUNT(*) AS total_visits\nFROM page_views\nWHERE visit_time >= '2024-07-01' AND visit_time < '2024-08-01'\nGROUP BY page_id\nORDER BY total_visits DESC;\n```\n\n**核心要点说明：**\n1. **日期过滤**：使用范围查询（`>=` 和 `<`）高效筛选2024年7月数据，避免函数转换导致索引失效。\n2. **分组聚合**：通过 `GROUP BY page_id` 对页面分组，`COUNT(*)` 统计每组访问次数。\n3. **排序优化**：直接按聚合结果 `total_visits` 降序排列。\n\n**注意事项：**\n- 若表较大，可在 `visit_time` 字段建立索引提升性能。\n- 若需返回页面名称，可关联页面信息表进行多表查询。\n- 实际字段名需根据表结构调整（如时间字段可能为 `created_at`）。"}, {"num": 39, "title": "编写 SQL，查询每个页面在每个月的总访问次数，并按页面 ID 和月份排序", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "SQL, SQL进阶, SQL网站场景", "answer": "假设有一个访问记录表 `page_views`，其中包含字段：`page_id`（页面ID）和 `visit_time`（访问时间）。查询每个页面每月总访问次数的SQL如下：\n\n```sql\nSELECT \n    page_id,\n    DATE_FORMAT(visit_time, '%Y-%m') AS visit_month,\n    COUNT(*) AS total_views\nFROM page_views\nGROUP BY page_id, visit_month\nORDER BY page_id, visit_month;\n```\n\n**核心要点：**\n1. **日期提取**：使用 `DATE_FORMAT` 函数将时间格式化为“年-月”字符串，便于按月份分组。\n2. **分组聚合**：按 `page_id` 和提取的月份进行分组，使用 `COUNT(*)` 统计每组记录数。\n3. **排序**：最后按页面ID和月份升序排序，结果清晰有序。\n\n**可选扩展：**\n- 若使用MySQL，也可用 `YEAR(visit_time)` 和 `MONTH(visit_time)` 分别提取年月。\n- 可考虑添加 `WHERE` 子句限定时间范围，提高查询效率。"}, {"num": 40, "title": "编写 SQL，查询每个页面路径的独立访问用户数", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "SQL, SQL进阶, SQL网站场景", "answer": "该问题需要按页面路径统计独立用户数，核心是使用`COUNT(DISTINCT user_id)`进行去重计数，并结合`GROUP BY`实现分组聚合。\n\n```sql\nSELECT \n    page_path,\n    COUNT(DISTINCT user_id) AS unique_users\nFROM visit_log\nGROUP BY page_path\nORDER BY unique_users DESC;\n```\n\n**关键要点：**\n1. **去重统计**：`COUNT(DISTINCT user_id)`确保同一用户多次访问只计一次\n2. **分组聚合**：`GROUP BY page_path`将数据按页面路径分组\n3. **结果排序**：`ORDER BY DESC`可优先展示访问量最高的页面\n\n**扩展说明：**\n- 实际表中需包含`user_id`和`page_path`字段，表名根据业务调整\n- 若需统计时间段内的数据，可添加`WHERE visit_time BETWEEN ...`条件\n- 对于大数据量场景，可考虑先对原始数据去重再聚合以优化性能\n\n该查询能直接满足分析各页面受众覆盖广度的需求，是网站流量分析的常见基础操作。"}, {"num": 41, "title": "编写 SQL，查询每个页面路径的访问次数和独立访问用户数，并按页面 ID 升序排序", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "SQL, SQL进阶, SQL网站场景", "answer": "```sql\nSELECT \n    page_path,\n    COUNT(*) AS visit_count,\n    COUNT(DISTINCT user_id) AS unique_users\nFROM page_views\nGROUP BY page_path\nORDER BY MIN(page_id) ASC;\n```\n\n答案说明：\n1. 使用`COUNT(*)`统计每个页面路径的总访问次数；\n2. 使用`COUNT(DISTINCT user_id)`计算独立访问用户数，避免重复计数；\n3. 通过`GROUP BY page_path`按页面路径分组聚合数据；\n4. 最后使用`ORDER BY MIN(page_id) ASC`确保结果按页面ID升序排序（注意需使用MIN/MAX聚合函数获取分组内的page_id排序依据）。\n\n此查询适用于网站流量分析场景，能快速识别热门页面及用户覆盖情况。"}, {"num": 42, "title": "编写 SQL，查询用户表中所有用户的用户名和年龄", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "SQL, SQL进阶, SQL网站场景", "answer": "查询用户表（假设表名为`users`）中所有用户的用户名和年龄，基本的SQL语句如下：\n\n```sql\nSELECT username, age FROM users;\n```\n\n在实际网站场景中，需注意：\n1.  **字段与表名**：`username`和`age`是示例字段名，实际需根据数据库表结构（如可能为`user_name`、`user_age`等）调整。\n2.  **数据权限**：该查询会返回表中所有记录，生产环境中需考虑是否需要添加`WHERE`条件进行筛选，以及是否有执行该查询的权限。\n3.  **性能考虑**：若表数据量巨大，应确保相关字段已建立索引，并避免在高峰期执行全表扫描。\n4.  **规范写法**：建议显式指定字段而非使用`SELECT *`，并保持SQL关键字大写以提高可读性。\n\n一个更完整的示例（含别名）：\n```sql\nSELECT username AS 用户名, age AS 年龄 FROM users;\n```\n\n此答案涵盖了核心语法、场景注意事项及规范建议，适合在面试中简洁清晰地展示知识掌握程度。"}, {"num": 43, "title": "编写 SQL，查询访问次数超过 2 次的所有用户的用户名和访问次数", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "SQL, SQL进阶, SQL网站场景", "answer": "假设存在访问日志表 `visit_log`，包含字段 `user_id`（用户ID）和访问记录。我们需关联用户表 `users`（包含 `user_id` 和 `username`）获取用户名。\n\n**SQL语句：**\n```sql\nSELECT u.username, COUNT(*) AS visit_count\nFROM visit_log v\nJOIN users u ON v.user_id = u.user_id\nGROUP BY v.user_id, u.username\nHAVING COUNT(*) > 2;\n```\n\n**核心要点：**\n1. 使用 `JOIN` 关联用户表，获取用户名。\n2. 按 `user_id` 分组统计访问次数。\n3. 通过 `HAVING` 子句筛选访问次数超过2次的记录。\n4. 输出用户名和对应的访问次数。"}, {"num": 44, "title": "编写 SQL，查询访问记录表中每个页面的访问次数，按访问次数从高到低排序", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "SQL, SQL进阶, SQL网站场景", "answer": "## 解答\n\n### SQL 语句\n\n```sql\nSELECT \n    page_url,\n    COUNT(*) AS visit_count\nFROM \n    visit_records\nGROUP BY \n    page_url\nORDER BY \n    visit_count DESC;\n```\n\n### 核心要点\n\n**1. GROUP BY 分组**：使用 `GROUP BY page_url` 将相同页面的访问记录聚合为一组，这是实现\"每个页面统计\"的关键。\n\n**2. COUNT(*) 计数**：`COUNT(*)` 统计每组的记录数，即该页面的访问次数。\n\n**3. ORDER BY 排序**：`DESC` 关键字实现降序排列，访问量高的页面排在前面。\n\n### 扩展讨论\n\n实际场景中可根据需求优化：\n\n| 场景 | SQL 扩展 |\n|------|----------|\n| 筛选特定日期 | 添加 `WHERE visit_date = '2024-01-01'` |\n| 只看前10页面 | 添加 `LIMIT 10` |\n| 过滤无效访问 | `WHERE page_url IS NOT NULL` |\n| 去重统计用户数 | 用 `COUNT(DISTINCT user_id)` 替代 |\n\n### 面试加分点\n\n- 若表数据量大，可在 `page_url` 字段建立索引提升性能\n- `COUNT(*)` 与 `COUNT(1)` 性能等价，但 `COUNT(1)` 语义更明确\n- 如需统计访问人数而非次数，应使用 `COUNT(DISTINCT user_id)`"}, {"num": 45, "title": "编写 SQL，查询访问记录表中访问次数最多的前两个页面路径", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "SQL, SQL进阶, SQL网站场景", "answer": "**方案一：基础解法（推荐，适用于面试演示）**\n```sql\nSELECT page_path, COUNT(*) AS visit_count\nFROM access_log\nGROUP BY page_path\nORDER BY visit_count DESC\nLIMIT 2;\n```\n此解法通过 `GROUP BY` 统计每个页面的访问次数，然后按访问次数降序排序，使用 `LIMIT 2` 直接取前两条记录。其优点是简洁直观，核心逻辑（聚合、排序、限制）清晰，非常适合在面试中快速书写和讲解。\n\n**方案二：处理并列情况（更严谨，可作为补充说明）**\n若访问量最高或第二高的页面存在并列，`LIMIT 2` 可能无法列出所有符合条件的记录。使用窗口函数可以完美解决：\n```sql\nSELECT page_path, visit_count\nFROM (\n    SELECT page_path, \n           COUNT(*) AS visit_count,\n           DENSE_RANK() OVER (ORDER BY COUNT(*) DESC) AS rnk\n    FROM access_log\n    GROUP BY page_path\n) ranked\nWHERE rnk <= 2;\n```\n这里通过 `DENSE_RANK()` 函数（排名连续）为每个页面分配一个基于访问次数的排名，再筛选出排名前两位的所有页面。它能确保即使访问次数相同，也都能被正确选出。\n\n**总结**：面试时建议先写出方案一，并口头补充说明其潜在局限（并列情况），随后可主动提出方案二作为更健壮的解法，这能充分展现你对问题细节的考量和对SQL进阶功能的掌握。"}];