const QUESTIONS = [{"num": 1, "title": "ArrayList 里有 1 亿条数据，要怎么去重？", "difficulty": "简单", "difficulty_emoji": "🟢", "tags": "后端, 场景题", "answer": "**核心思路：** 数据量极大时，传统的 `new HashSet<>(list)` 会因内存溢出而失败，需采用分治或外部存储策略。\n\n**解决方案：**\n\n1.  **外部排序 + 顺序去重：**\n    *   **步骤：** 将数据写入多个文件，使用 `FileChannel` 等进行外部排序（如多路归并）。排序后，相同元素相邻。最后顺序扫描，仅写入不重复的元素到新文件。\n    *   **优点：** 内存占用低（只需几个缓冲区），适合单机处理。\n    *   **缺点：** 磁盘 I/O 是瓶颈，速度较慢。\n\n2.  **分布式计算（推荐）：**\n    *   **步骤：** 使用 Spark/Flink 等框架。将数据读入分布式数据集（如 Spark RDD）。直接调用 `distinct()` 算子。框架会自动将数据分区，通过哈希或排序在分区内外高效去重。\n    *   **优点：** 能充分利用集群资源，处理速度最快，是生产环境首选。\n    *   **缺点：** 依赖外部大数据组件，架构相对复杂。\n\n**面试回答要点：**\n“对于1亿条数据的去重，直接使用内存中的集合会OOM。我们可以根据资源情况选择方案：若只有单机，可采用外部排序；在企业级开发中，通常会借助Spark等分布式框架，其`distinct`算子能通过数据分片和Shuffle机制，高效、可靠地完成海量数据去重。”"}, {"num": 2, "title": "不能使用大于小于号，如何判断两个正整数的大小？", "difficulty": "简单", "difficulty_emoji": "🟢", "tags": "后端, 场景题", "answer": "在不使用大于小于号的前提下，判断两个正整数A和B的大小，可以采用以下几种算法实现：\n\n**1. 基于减法的方法**  \n计算差值D = A - B（需注意整数溢出问题）。若D为正，则A大于B；若D为负，则A小于B；若D为零，则两者相等。在编程中，可通过符号位判断：例如在C语言中，利用`(A - B) > 0`的隐含逻辑，但需确保使用无符号数或大数处理以防溢出。\n\n**2. 逐位比较法（位运算）**  \n从二进制最高位开始逐位扫描。对于每个位i（从最高位到最低位），提取A和B的对应位：若A的位为1而B的位为0，则A大于B；反之则A小于B；若相同则继续比较下一位。该方法避免了减法溢出，适合底层实现或硬件描述。\n\n**3. 加法迭代法**  \n通过循环累加较小的数来模拟比较。初始化计数器为0，不断对A或B进行加法运算直到相等或超过。例如，若先达到A的值，则B大于或等于A；但此方法效率较低，通常作为理论示例。\n\n**注意事项**：  \n- 在"}, {"num": 3, "title": "可以用几行代码实现一个负载均衡器吗？", "difficulty": "简单", "difficulty_emoji": "🟢", "tags": "后端, 场景题", "answer": "可以。一个最简单的负载均衡器核心是请求分发算法。以下是基于Python Flask和Round Robin（轮询）算法的最小实现：\n\n```python\nfrom flask import Flask, redirect\nimport itertools\n\napp = Flask(__name__)\n# 后端服务器列表\nservers = itertools.cycle(['http://server1:5000', 'http://server2:5000'])\n\n@app.route('/')\ndef load_balance():\n    # 选择下一个服务器并重定向\n    server = next(servers)\n    return redirect(server)\n```\n\n**核心要点：**\n1.  **算法选择**：轮询是最基础、最易实现的策略。其他如随机、加权轮询、最少连接数等算法逻辑类似。\n2.  **实现本质**：负载均衡器是一个代理或路由器，根据策略将请求转发给后端服务。\n3.  **局限性**：此示例仅为核心逻辑，缺乏关键生产特性：\n    *   **单点故障**：本身就是一个单点。\n    *   **健康检查**：无法自动剔除故障服务器。\n    *   **会话保持**：未处理需要会话粘滞的场景。\n    *   **性能**：基于Web框架，转发效率和并发能力有限。\n\n生产环境会使用Nginx、HAProxy等成熟软件，或通过云服务商提供的负载均衡服务，它们内置了健康检查、高可用、丰富的调度算法和强大的性能。此示例旨在阐明其最核心的“路由决策”原理。"}, {"num": 4, "title": "现在需要你设计一个将已登录的用户踢下线的功能，如何实现？", "difficulty": "简单", "difficulty_emoji": "🟢", "tags": "后端, 场景题", "answer": "实现将已登录用户踢下线，核心在于破坏用户的会话状态。用户登录后，后端通常会生成一个会话标识（如JWT Token或Session ID）返回给客户端，后续请求凭此标识进行身份验证。踢下线功能就是要让该标识立即失效。\n\n技术方案主要有两种：一是**主动失效**，当管理员触发踢下线指令时，后端立即将该用户的会话标识加入“黑名单”（如存入Redis并设置过期时间），并在后续请求校验时优先检查黑名单；二是**被动检测**，在用户每次请求时，除了验证标识有效性，还需查询该账户是否已被标记为“需踢下线”，若是则返回错误并销毁会话。\n\n关键设计点：1. **实时性**，使用Redis等高性能存储存放状态变更，确保踢下线指令即时生效。2. **多设备支持**，若需踢掉用户所有设备，可维护用户与活跃会话的映射，批量失效。3. **通知机制**，踢下线后可向客户端发送特定状态码或推送通知，引导用户重新登录。\n\n该功能需集成到用户管理后台，并记录操作日志。对于分布式系统，需确保状态同步，避免因会话缓存导致踢下线延迟。"}, {"num": 5, "title": "1000 个任务，每个任务 0.1s，最大响应时间 1s，线程池参数怎么设置？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, Java, 场景题", "answer": "## 线程池参数设计分析\n\n**核心计算思路：**\n\n要在1秒内完成1000个任务，每个任务耗时0.1秒，所需最小并发线程数为：\n```\n并发数 = 1000 × 0.1s / 1s = 100\n```\n\n**参数配置建议：**\n\n```java\nThreadPoolExecutor executor = new ThreadPoolExecutor(\n    100,                        // 核心线程数\n    200,                        // 最大线程数（留有余量）\n    60L, TimeUnit.SECONDS,      // 空闲线程存活时间\n    new LinkedBlockingQueue<>(100),  // 有界队列\n    new ThreadPoolExecutor.CallerRunsPolicy()  // 拒绝策略\n);\n```\n\n**参数设计考量：**\n\n| 参数 | 值 | 理由 |\n|------|-----|------|\n| 核心线程数 | 100 | 满足最小并发需求 |\n| 最大线程数 | 200 | 预留50%-100%余量应对突发 |\n| 队列容量 | 较小值 | 队列会增加排队等待时间，影响响应时间 |\n| 拒绝策略 | CallerRunsPolicy | 降级处理，保证任务不丢失 |\n\n**补充说明：**\n\n1. **若任务为CPU密集型**：核心线程数建议设为CPU核心数+1，需重新评估响应时间\n2. **若任务为IO密集型**：可适当增加线程数，提高并发能力\n3. **监控调优**：上线后通过监控观察实际耗时、队列积压情况，动态调整\n\n**关键结论**：该场景下队列不宜过大，因为队列排队会直接消耗响应时间预算，应优先通过增加线程数来满足并发需求。"}, {"num": 6, "title": "4C8G和8C16G，这两个配置的服务器你们选哪个？为什么？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "# 服务器配置选择：4C8G vs 8C16G\n\n## 答案\n\n选择哪种配置取决于**业务场景**，不能一概而论。\n\n---\n\n### 选择 4C8G 的场景\n\n| 场景 | 说明 |\n|------|------|\n| 中小型业务 | 访问量有限，负载不高 |\n| IO密集型服务 | 如Web应用、API网关，主要瓶颈在IO而非CPU |\n| 初创项目/测试环境 | 成本敏感，后续可弹性扩容 |\n| 轻量级服务 | 如配置中心、定时任务服务 |\n\n---\n\n### 选择 8C16G 的场景\n\n| 场景 | 说明 |\n|------|------|\n| 大流量生产环境 | 高并发场景需要更多计算资源 |\n| CPU密集型服务 | 如数据处理、视频转码、复杂计算 |\n| 核心服务/主服务 | 稳定性要求高，需预留资源余量 |\n| 多线程应用 | 如线程池较大的Java应用 |\n\n---\n\n### 我的决策思路\n\n**选型时关注三点：**\n\n1. **业务类型** — CPU密集选8C，IO密集可选4C\n2. **成本效益** — 优先满足性能需求，再考虑成本\n3. **弹性能力** — 如果有K8s等弹性伸缩能力，可用4C+水平扩展替代8C\n\n**实际项目中**：生产环境核心服务倾向选 **8C16G**，预留30%资源余量应对流量峰值；非核心服务或可水平扩展的服务选 **4C8G** 更具性价比。"}, {"num": 7, "title": "Agent 死循环问题有遇到过吗？如何解决？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, AI, 大模型, 场景题", "answer": "**Agent 死循环问题的解决方案**\n\n在实际项目中确实遇到过 Agent 死循环问题，主要表现为两种场景：\n\n**一、常见原因**\n\n1. **工具调用重试循环**：Agent 调用工具返回异常，但模型未正确处理错误，反复发起相同调用。\n2. **任务分解死循环**：Agent 将任务拆解为子任务，但子任务间存在相互依赖或条件无法满足，陷入无限分解。\n3. **状态判断失效**：终止条件设计不严谨，Agent 无法判断任务已完成，持续执行。\n\n**二、解决方案**\n\n1. **硬性防护**：设置最大迭代次数（如 max_iterations=10）和单次调用超时时间，超限直接终止并返回当前结果。\n2. **状态去重**：记录每轮执行的动作与结果，检测到重复行为时强制跳出循环。\n3. **循环检测**：使用有向无环图（DAG）记录任务依赖关系，检测环形依赖。\n4. **Prompt 优化**：在系统提示中明确终止条件和异常处理策略，例如\"若连续两次获得相同结果则停止\"。\n5. **降级兜底**：死循环触发后回退到规则引擎或人工介入，保证系统可用性。\n\n**三、监控层面**\n\n上线后通过日志监控迭代次数分布，设置告警阈值，及时发现潜在的死循环 case 并优化策略。\n\n核心思路是**多层防护：前置约束 + 运行时检测 + 事后监控**，确保系统稳定性。"}, {"num": 8, "title": "JDK 序列化问题排查", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 线上问题排查", "answer": "JDK序列化问题排查是线上常见故障，需快速定位根因。核心排查步骤如下：\n\n**1. 确认异常信息**：首先查看完整异常堆栈。常见错误包括`InvalidClassException`（类定义不兼容）、`NotSerializableException`（未实现序列化接口）及字段值异常等。\n\n**2. 检查类定义一致性**：\n   - **serialVersionUID**：序列化和反序列化的类中该字段必须完全一致，否则直接抛出`InvalidClassException`。优先比对类的全限定名和该值。\n   - **Serializable接口**：确认对象类及所有非静态、非transient字段的类型都已实现该接口。\n\n**3. 排查字段变更**：\n   - 检查类结构是否发生不兼容变更，如字段增删、类型改变、继承关系调整。\n   - 注意`transient`和`static`字段不会被序列化，若业务依赖这些字段则需通过自定义`writeObject`/`readObject`方法处理。\n\n**4. 分析环境与依赖**：对比上下游服务的JDK版本、类路径和依赖库版本，某些框架（如Hessian）可能修改序列化行为。\n\n**5. 数据验证**：检查序列化数据的格式、长度是否异常，排查是否因数据污染或网络传输截断导致。\n\n**预防建议**：上线前进行序列化兼容性测试，关键类显式声明`serialVersionUID`，优先考虑使用更中立的协议（如JSON）进行服务间通信。"}, {"num": 9, "title": "Java 写入文件到磁盘会经历哪些过程？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "Java写文件到磁盘主要经历三个层次：应用层处理、操作系统内核交互、物理磁盘写入。\n\n在应用层，Java通过IO流（如FileOutputStream）或NIO（FileChannel）写入数据。写入时先将数据复制到**用户空间缓冲区**。当调用write()方法或缓冲区满时，数据会从用户空间复制到**内核空间的PageCache**（页面缓存），此时数据已被操作系统接管，但可能尚未写入磁盘。\n\n内核层中，文件系统（如ext4、NTFS）会组织PageCache中的数据，管理文件元数据（如inode）并维护**日志**保证崩溃一致性。操作系统通过I/O调度器合并或排序写请求，再通过设备驱动程序将数据发送到硬件。\n\n物理写入阶段，磁盘控制器将数据写入**磁盘缓存**，最终通过磁头寻道（HDD）或电子写入（SSD）持久化到存储介质。整个过程中，**写入时机由OS内核策略决定**，应用层无法强制立即落盘。\n\n实际开发中，可通过`FileChannel.force()`强制刷盘，或调整OS的`vm.dirty_*`参数平衡性能与数据安全。理解此流程对优化IO性能、设计可靠的数据存储方案至关重要。"}, {"num": 10, "title": "Java 应用的内存持续性增长，但是监控显示堆内存没有什么变化，可能的原因有哪些？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "当堆内存稳定但Java应用整体内存持续增长时，通常指向堆外内存或JVM其他区域的问题。主要原因包括：\n\n1.  **堆外内存（Direct Memory）泄漏**：通过`ByteBuffer.allocateDirect()`或NIO框架分配的直接内存未正确释放。此类内存不归GC直接管理，需依赖回收机制或显式清理。\n2.  **Metaspace（元空间）增长**：大量动态类加载（如Groovy脚本、反射代理、CGLIB）会导致元空间持续增长。若未设置上限或未卸载类，内存会膨胀。\n3.  **线程栈内存增加**：每个线程会占用固定栈内存（默认约1MB）。高并发下线程数持续增长会显著消耗内存。\n4.  **本地方法库（Native Memory）泄漏**：JNI调用或第三方本地库（如某些图像、压缩库）分配的内存未释放。\n5.  **监控盲区**：JVM监控工具（如`jstat`）可能无法完整覆盖所有内存区域，需结合操作系统监控（如Linux的`pmap`、`RSS`）排查。\n\n**排查思路**：\n- 使用`jcmd <pid> VM.native_memory detail`分析JVM各区域内存。\n- 检查NIO直接内存的使用和回收情况。\n- 监控线程数变化与`-Xss`参数。\n- 通过`-XX:MaxMetaspaceSize`限制元空间并观察是否溢出。\n- 结合操作系统工具（如`top`、`valgrind`）分析进程级别的内存增长。"}, {"num": 11, "title": "MySQL 里有 2000w 数据，Redis 中只存 20w 的数据，如何保证 Redis 中的数据都是热点数据?", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "保证 Redis 中仅存储热点数据，核心在于采用合理的淘汰策略并辅以应用层逻辑。\n\n首先，将 Redis 的 `maxmemory-policy` 配置为 **LFU** 或 **LRU** 相关策略。推荐使用 `allkeys-lfu`（全局最不经常使用），它能根据键的访问频率自动淘汰低频数据，将内存配额动态分配给访问更频繁的键，从而持续沉淀出 20w 的热点。\n\n其次，在应用层配合，主要采用 **Cache-Aside** 模式：读请求先查 Redis，未命中则回源 MySQL 并将结果写入 Redis；写请求直接更新 MySQL 并删除对应的 Redis 缓存。这确保了只有被访问到的数据才会被缓存，且被淘汰的冷数据不会占用资源。\n\n此外，可结合**访问频率监控**，通过分析日志或使用 Redis 的 `LFU` 统计信息，主动识别并加载热点数据。同时，需设置合理的缓存过期时间作为兜底，并使用**唯一键**缓存完整结果，避免缓存穿透。\n\n综上，通过 Redis 自身的高效淘汰机制与正确的应用读写模式相结合，即可高效地在有限"}, {"num": 12, "title": "MySQL数据库一亿条数据，怎么快速加索引？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "核心原则：避免长时间锁表，保障业务连续性。推荐以下方案，按优先级排序：\n\n1.  **首选方案：使用Online DDL (MySQL 5.6+)**。\n    执行命令时指定 `ALGORITHM=INPLACE` 和 `LOCK=NONE`。这允许在建索引的同时进行数据的增删改，对业务影响最小。需注意：执行时间仍较长，可能占用大量IO和CPU资源，应在低峰期进行，并确保磁盘空间充足。\n\n2.  **主从切换方案**。\n    若数据库存在主从集群，可先在**从库**上执行加索引操作（可使用上述Online DDL或允许锁表的方式）。完成后，通过主从切换将该从库提升为主库，原主库下线。此方案对原主库业务零影响，但过程复杂，需考虑切换期间的读写流量分配和数据一致性。\n\n3.  **分批次处理方案**。\n    对于非严格实时要求的索引，可结合业务逻辑分批次创建。例如，按时间或ID范围分段，使用 `pt-online-schema-change`（Percona工具）或 `gh-ost`（GitHub开源工具）在后台逐步创建新表、同步数据、原子重命名。这些工具能实现无锁表变更，但对数据库负载有一定要求。\n\n**执行前务必**：评估索引必要性、在测试环境验证、选择业务低峰期、监控系统负载、并做好备份。"}, {"num": 13, "title": "Spring Cloud Gateway 500 问题排查", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 线上问题排查", "answer": "**Spring Cloud Gateway 500 问题排查思路：**\n\n1.  **定位日志与异常**：首先查看Gateway应用日志，重点关注 `500 Internal Server Error` 的详细堆栈。这通常会直接指向问题根源，例如下游服务调用失败、过滤器异常或路由配置错误。\n\n2.  **检查路由配置**：确认请求是否按预期路由。检查路由的 `predicates`（断言）和 `filters`（过滤器）配置是否正确，目标服务地址（`uri`）是否可达、稳定。可通过Actuator的 `/actuator/gateway/routes` 端点动态核查路由表。\n\n3.  **排查下游服务**：500错误常由下游微服务自身异常引起。直接访问下游服务的健康检查接口（如 `/actuator/health`）或接口地址，确认其是否正常运行并返回预期结果。网络不通或服务宕机是常见原因。\n\n4.  **检查网关过滤器**：审查全局或特定路由的Filter链。自定义Filter（如权限校验、日志记录）或内置Filter（如 `Hystrix`、`Retry`）的逻辑错误、超时配置或异常处理不当，都可能抛出500错误。\n\n5.  **其他可能原因**：\n    *   **资源与依赖**：网关自身资源耗尽（如连接池满、线程阻塞）或核心组件（如服务发现、配置中心）连接失败。\n    *   **版本与兼容性**：网关与下游服务使用的依赖版本（如序列化库）不兼容。\n    *   **限流熔断**：检查是否触发了限流（`RequestRateLimiter`）或熔断（`CircuitBreaker`），这些通常返回429或特定降级响应，但配置错误也可能导致500。\n\n**总结**：排查遵循从网关自身到下游服务的顺序，优先从日志和异常信息入手，结合路由、过滤器、下游服务状态及网络等多维度进行分析，快速定位故障点。"}, {"num": 14, "title": "WebSocket 知道吗？如果让你设计一个 IM 协议，你会考虑什么？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "WebSocket 是基于 HTTP 升级的全双工通信协议，允许服务端主动向客户端推送数据，解决了传统 HTTP 轮询效率低的问题，非常适合实时性要求高的场景如 IM、实时通知等。\n\n如果设计一个 IM 协议，我会从以下几点考虑：\n\n1. **连接层**：使用 WebSocket 保持长连接，实现双向实时通信。需设计心跳机制维持连接，并检测断线重连。\n2. **协议格式**：定义二进制或 JSON 格式的消息结构，包含消息类型、序列号、时间戳、发送者、接收者、内容等字段。二进制协议更高效，JSON 更易调试。\n3. **可靠性**：实现消息确认机制（ACK）、重传策略（如基于序列号去重）、离线消息存储与推送。\n4. **扩展性**：支持单聊、群聊，群消息可采用扩散写或读扩散模型；可集成已读回执、消息撤回、媒体文件传输（通过独立链接上传）。\n5. **安全与性能**：使用 WSS 加密传输，身份认证（如 Token）。在高并发下考虑连接数管理、消息队列削峰、分库分表存储。\n\n例如，可设计一个简化的自定义协议：消息头（长度+类型）+ 消息体（Protobuf 编码），通过心跳包保持活跃，并在客户端缓存未确认消息以保障不丢失。"}, {"num": 15, "title": "一条 1s 请求的响应，怎么优化成 1ms？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "要将1秒请求优化至1毫秒，核心在于系统性分析瓶颈并分层优化：\n\n**1. 定位瓶颈**\n首先通过监控工具（如APM）定位耗时热点。常见瓶颈包括：数据库查询慢、外部服务调用阻塞、复杂计算、锁竞争或网络延迟。\n\n**2. 数据库优化**\n- 优化SQL与索引：避免全表扫描，减少关联查询。\n- 引入缓存：对热点数据使用Redis或本地缓存，减少DB访问。\n- 读写分离/分库分表：分散数据库压力。\n\n**3. 代码与架构优化**\n- 异步化：将非关键逻辑（如日志、通知）移至消息队列异步处理。\n- 并行调用：对多个无依赖的远程调用使用线程池并行执行。\n- 减少锁粒度：使用无锁结构或局部锁。\n\n**4. 缓存策略**\n- 多级缓存：L1（本地缓存）+L2（分布式缓存），设置合理过期与降级策略。\n- 预热缓存：提前加载热点数据。\n\n**5. 基础设施与网络**\n- 使用连接池：复用数据库、HTTP连接，减少握手开销。\n- 静态资源CDN：卸载静态请求。\n- 数据压缩与序列化优化：如采用Protocol Buffers。\n\n**6. 验证与迭代**\n优化后需进行压测验证，持续监控线上性能，建立报警机制。\n\n最终，性能优化是权衡的艺术，需在业务场景、开发成本和系统复杂度间取得平衡。"}, {"num": 16, "title": "一笔订单，在取消的那一刻用户刚好付款了，怎么办？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "这是一个典型的并发和状态竞态场景，核心是处理“取消”与“支付”两个操作在时间上的重叠冲突。我的解决方案如下：\n\n**1. 问题本质与核心原则**\n本质是“取消”请求和“支付成功”通知在极短时间内并发到达后端。处理原则是**优先保障资金正确性**，并**确保数据最终一致性**。通常应优先采纳“支付成功”这一已发生的事实。\n\n**2. 具体处理流程**\n- **服务端收到取消请求时**：检查订单当前状态。若已是“已支付”或“支付中”，则应拒绝取消操作，并向用户返回明确提示（如“订单已支付，无法取消”）。\n- **处理支付回调时**：若支付成功回调到达，发现订单状态为“已取消”，**不应简单丢弃支付结果**。正确做法是：将订单状态**强制变更为“已支付”**（或一个中间态如“支付后取消中”），然后触发后续的自动退款流程。同时，记录详细的日志和监控告警。\n- **关键设计——幂等性**：支付回调接口必须设计为幂等，避免因网络重试导致重复处理。通常使用支付流水号作为唯一标识来去重。\n\n**3. 架构与设计保障**\n- **引入状态机**：严格管理订单状态流转，避免非法状态跳变。\n- **分布式事务或消息队列**：保证“订单状态更新”和“资金退款操作”的最终一致性。例如，支付回调成功后发送退款消息到队列，由消费者可靠地处理退款。\n- **清晰的用户告知**：无论结果如何，都要通过站内信、短信等方式清晰告知用户处理结果（如“取消失败，已为您办理原路退款”）。\n\n总之，这要求系统具备健壮的并发控制、明确的状态机模型和完善的补偿机制，以应对这种“临界状态”，确保资金流与订单流的最终一致。"}, {"num": 17, "title": "主线程如何得知线程池中多个并行任务的完成并收集它们的结果？​", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "**核心答案：**\n\n主线程通过 `Future` 对象与线程池中的任务交互。提交任务后会返回 `Future`，主线程可通过其 `get()` 方法阻塞等待单个任务完成并获取结果。\n\n为高效处理多个任务完成通知，推荐使用 `ExecutorCompletionService`。它将任务结果封装为 `Future` 并放入一个阻塞队列。主线程通过调用 `take()` 或 `poll()` 方法，可以按任务完成的先后顺序获取结果，实现“谁先完成谁先处理”，避免了简单使用 `Future` 时需轮询或盲目等待的问题。\n\n**方案选择：**\n1.  **需要所有结果**：可使用 `Future` 列表，依次调用 `get()`。\n2.  **需处理最先完成的结果**：首选 `CompletionService`，效率更高。\n3.  **需回调通知**：可为任务附带 `FutureCallback`，但需自行管理线程安全。\n\n**示例代码片段：**\n```java\nExecutorService pool = Executors.newFixedThreadPool(3);\nCompletionService<Result> cs = new ExecutorCompletionService<>(pool);\nList<Future<Result>> futures = new ArrayList<>();\n\nfor (Task task : tasks) {\n    futures.add(cs.submit(task));\n}\n\n// 按完成顺序收集结果\nfor (int i = 0; i < tasks.size(); i++) {\n    Future<Result> f = cs.take(); // 阻塞直到有任务完成\n    Result result = f.get();     // 获取结果\n    // 处理 result...\n}\n```\n\n**关键点：** `Future.get()` 是阻塞的；`CompletionService` 通过分离提交与完成，提升了结果处理的吞吐量。"}, {"num": 18, "title": "从网关再到各个后端服务，如何设置 RPC 的超时时间，要考虑哪些问题？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "**超时时间设置方案：**\n1. **网关层设置全局超时**：网关作为入口，需设置合理的全局超时时间（如3-5秒），防止上游请求长时间阻塞。该时间应略大于核心后端服务超时之和，预留网络和中间件处理开销。\n\n2. **服务间独立设置超时**：各后端服务根据自身业务复杂度、依赖资源（如数据库、第三方接口）设置独立超时。例如：\n   - 轻量级查询服务：100-300ms\n   - 复杂计算或聚合服务：1-2s\n   - 涉及外部调用的服务：按外部接口SLA设定，通常≤外部超时时间。\n\n3. **考虑关键因素**：\n   - **超时传递**：避免设置不一致导致“超时风暴”，如网关超时5s，但底层服务设置10s，会导致网关超时后资源仍被占用。\n   - **重试与幂等**：超时后是否需重试？需确保操作幂等性，并设置最大重试次数与退避策略。\n   - **熔断降级**：结合熔断器（如Sentinel、Hystrix），在超时率高时自动熔断，防止雪崩。\n   - **监控调整**：基于P99延迟、错误率等指标动态调整超时，并通过分布式链路追踪分析耗时分布。\n\n**示例架构**：\n用户请求 → 网关（总超时5s） → 服务A（超时1s） → 服务B（超时500ms） → 数据库（超时200ms）  \n若服务B超时，需在服务A层捕获异常并快速失败，避免阻塞网关队列。"}, {"num": 19, "title": "会员表中有500万条会员数据，有什么方式可以对即将过期的会员提前7天进行提醒？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "针对会员表中500万条数据的提醒需求，核心方案是采用**定时任务结合优化查询**的方式。具体实现可分为以下步骤：\n\n首先，设置一个每日定时任务（如Cron Job），在业务低峰期（如凌晨）执行。任务的核心是查询过期日期在当前时间加7天内的会员。为提升查询效率，必须在会员表的“过期日期”字段上建立索引，并考虑使用复合索引以覆盖其他常用查询条件。\n\n其次，由于数据量大，为避免一次性加载导致内存压力，应采用**分批处理**策略。例如，每次查询处理1000条记录，通过游标或分页方式循环获取数据。查询结果可直接用于发送提醒，或先暂存到临时表或缓存中。\n\n再者，提醒发送应设计为**异步操作**，例如通过消息队列（如RabbitMQ、Kafka）将会员信息推送到下游服务进行发送，避免阻塞扫描任务。这能提高系统容错性，并支持重试机制。\n\n最后，需考虑监控与日志：记录任务执行状态、失败会员信息等，便于排查问题。同时，为防止重复提醒，可在会员数据中添加“已提醒”标志或使用独立表记录提醒历史。\n\n此方案平衡了效率与可靠性，确保在大规模数据下稳定运行。"}, {"num": 20, "title": "你说你项目上用了分布式锁，加锁后并发度不就降低了吗？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "是的，从表面上看，加锁确实会降低对特定资源的并发访问能力。但这正是分布式锁要解决的核心矛盾：**在需要保证数据强一致性的关键路径上，用可控的并发度下降来避免数据错乱**。\n\n在我们的项目中，并非所有操作都加锁。分布式锁只用于保护那些**无法通过数据库事务、唯一索引等机制保证原子性的“临界区”**，比如：扣减库存的最后一步、生成全局唯一订单号、修改关键状态机等。这些操作一旦并发出错，代价远高于性能损失。\n\n因此，关键在于**精细化控制锁的粒度和范围**。我们只锁必须保护的最小代码块和最细粒度的资源（如锁定某个商品ID的库存），并严格控制超时时间，从而将并发影响降到最低。\n\n此外，并发度的整体优化更多依赖于**架构设计**：通过缓存、队列削峰、数据库读写分离和分库分表来提升系统整体吞吐量。在需要强一致的节点使用分布式锁，而在大量非关键路径上依然保持高并发。这本质上是**在一致性与可用性/性能之间做出的合理权衡**，而不是简单地“一锁了之”。"}, {"num": 21, "title": "你项目用的 JWT，那如何保证安全性？别人拿到你这个 Token 不就可以直接登录你的账号了吗？怎么办？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "JWT的安全性需从多个层面共同保障，核心思路是“传输防窃取 + 使用控范围 + 事后可作废”。\n\n**1. 传输与存储安全**\n*   **强制使用HTTPS**：防止Token在传输过程中被中间人窃听或篡改。\n*   **前端安全存储**：将JWT存储在`HttpOnly`、`Secure`和`SameSite=Strict`（或`Lax`）的Cookie中，可以有效防止XSS攻击读取Token。避免使用localStorage。\n\n**2. Token本身的安全设计**\n*   **缩短有效期**：Access Token应设置较短的有效期（如15-30分钟），降低泄露后的危害窗口。\n*   **使用Refresh Token**：配合一个长期有效的Refresh Token，专门用于安全地获取新的Access Token，两者独立存储。\n*   **避免敏感信息**：JWT payload仅为可解码的Base64，不应存放密码、手机号等敏感数据。\n\n**3. 主动防御与管理机制**\n*   **Token黑名单/登出机制**：虽然JWT本身是无状态的，但可以在服务端维护一个轻量级的“黑名单”（如使用Redis），记录已注销或可疑的Token JTI（唯一标识），在每次请求时校验。\n*   **频繁变更的签名密钥**：定期轮换用于签名JWT的密钥，可使旧Token批量失效。\n*   **绑定客户端信息**：可将Token与客户端的IP或User-Agent等信息进行轻度绑定，增加仿冒难度。\n\n**4. 最小权限与监控**\n遵循最小权限原则，Token中只包含必要的Claims。同时，建立日志与监控，及时发现异常的Token使用模式。\n\n综上，没有绝对安全，但通过以上组合策略，可以极大提升JWT应用的安全性，使其即便在部分泄露的情况下，也能有效控制影响范围并具备快速响应能力。"}, {"num": 22, "title": "使用 LIMIT OFFSET 进行分页同步时，发现数据丢失了，可能是什么原因？如何解决？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "使用 `LIMIT OFFSET` 分页同步时出现数据丢失，通常由以下原因导致：\n\n**核心原因**：\n1.  **数据并发写入**：分页查询期间，有新的数据插入或旧数据被更新，导致数据位置偏移。后续页面可能重复或遗漏数据。\n2.  **查询条件变化**：同步过程中查询条件（如过滤字段值）发生变更，导致已同步的数据不再符合后续页面的查询条件。\n3.  **数据删除**：查询期间有数据被删除，导致后续页面的 `OFFSET` 位置计算错误，跳过本应被查询到的数据。\n\n**解决方案**：\n1.  **采用游标分页（推荐）**：基于一个稳定、有序且唯一的字段（如自增ID、时间戳）进行分页。例如，每次记录上次查询的最后一条记录的ID，下次查询时使用 `WHERE id > 上次最大ID LIMIT N`。这避免了 `OFFSET` 带来的位置漂移问题。\n2.  **业务层保证**：在同步任务开始时，确保查询条件（如时间范围）在整个同步周期内保持不变，或对数据状态进行快照。\n3.  **使用业务主键或唯一键**：分页时，除使用 `LIMIT/OFFSET` 外，在 `ORDER BY` 子句中包含一个唯一的业务主键，以保证排序的稳定性。\n4.  **降低同步频率或批处理**：对于变化剧烈的数据，可适当降低同步频率，或采用基于时间窗口的批处理（如每小时同步一次前一小时的数据），减少并发影响。\n\n**总结**：问题的根源在于 `OFFSET` 基于动态变化的总数据集进行定位。最佳实践是摒弃 `OFFSET`，改用基于游标的查询方式，确保分页的稳定性和可靠性。"}, {"num": 23, "title": "使用消息队列实现数据同步时，发现订单状态更新的顺序错乱了，如何解决？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "订单状态更新顺序错乱，通常由消息队列的异步性、消费者并发处理或网络延迟导致。解决方法如下：\n\n首先，确保消息队列支持顺序消息。例如，使用Kafka时，通过消息键（如订单ID）将同一订单的消息路由到同一分区，保证分区内顺序。\n\n其次，优化消费者设计。采用分区消费者模型，每个分区由单一线程顺序消费；或使用分布式锁，避免并发冲突。若需高吞吐，可设计为按订单哈希分组处理。\n\n此外，实现幂等性。消息处理应支持重复消费，通过唯一标识符（如消息ID）去重，防止因重试导致状态覆盖。\n\n最后，加强监控与日志。记录消息生产和消费时间戳，实时检测顺序异常；引入重试队列，处理失败消息时保持原顺序。\n\n综上，结合顺序分区、键路由、幂等消费和监控，可有效解决顺序错乱问题，确保数据同步一致性。"}, {"num": 24, "title": "公司目前有一些海外业务，从国内将数据发送到海外延迟比较大，如何处理这个问题？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "处理海外业务数据传输延迟问题，需从网络、数据、应用多层面综合优化。核心思路是“加速传输”与“减少传输”。\n\n首先，优化网络架构。可在业务集中的海外区域部署节点或利用云服务商的全球骨干网、专线服务，构建低延迟专用通道。同时，使用CDN加速静态资源分发，将热点数据缓存至边缘节点。\n\n其次，优化数据传输。对传输数据进行压缩、精简，减少体积；采用更高效的序列化协议（如Protobuf）；合理设置数据同步策略，非核心数据可异步或分批传输。\n\n再次，优化应用架构。采用异步化设计，将非实时性操作转为消息队列异步处理，提升响应速度。在前端或海外节点引入本地缓存，减少对国内源站的实时请求。\n\n最后，建立监控与容灾。持续监控链路质量，动态选择最优路由。设计降级与缓存机制，在网络异常时保障基础服务可用。\n\n综上，需结合业务场景，通过网络加速、数据压缩、架构解耦等组合策略，在成本与体验间取得平衡。"}, {"num": 25, "title": "双十一期间订单量暴增，实时数据同步系统出现消息堆积，如何应对？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "当面临双十一高峰期实时同步系统消息堆积时，可按“快速止损”和“长期优化”两阶段应对：\n\n**一、快速止损**\n1. **监控与告警**：立即确认堆积来源（如Kafka、RocketMQ）及影响范围，设置消费延迟阈值告警。\n2. **服务降级**：非核心业务（如积分、通知）暂停同步，保障交易、支付等主链路稳定。\n3. **临时扩容**：对消费端服务进行水平扩容，提高并发处理能力；同时检查数据库写入瓶颈，必要时分库分表。\n\n**二、长期优化**\n1. **队列隔离**：将核心业务消息与非核心业务消息分离，避免相互阻塞。\n2. **异步解耦**：将非实时性任务（如报表生成）改为异步延迟处理，降低实时系统压力。\n3. **流量控制**：在接入层或MQ层面设置限流，避免突发流量冲垮下游服务。\n4. **幂等设计**：确保消费端具备幂等性，防止重试导致数据错误。\n\n最终需复盘容量评估与压测机制，优化系统弹性设计。"}, {"num": 26, "title": "在 Java 中，不使用锁如何实现一个线程安全的单例？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "Java, 后端, 场景题", "answer": "## 不使用锁实现线程安全单例的方法\n\n主要有以下三种方式：\n\n### 1. 枚举单例（推荐）\n\n```java\npublic enum Singleton {\n    INSTANCE;\n    // 可添加业务方法\n}\n```\n\n利用 JVM 类加载机制保证线程安全，且天然防御反射攻击和序列化破坏，是最简洁优雅的实现。\n\n### 2. 静态内部类\n\n```java\npublic class Singleton {\n    private Singleton() {}\n    \n    private static class Holder {\n        private static final Singleton INSTANCE = new Singleton();\n    }\n    \n    public static Singleton getInstance() {\n        return Holder.INSTANCE;\n    }\n}\n```\n\n利用 JVM 类加载的初始化阶段是线程安全的特性，实现懒加载与线程安全的兼顾。只有调用 `getInstance()` 时才会触发内部类加载，避免了饿汉式的资源浪费问题。\n\n### 3. 饿汉式\n\n```java\npublic class Singleton {\n    private static final Singleton INSTANCE = new Singleton();\n    private Singleton() {}\n    \n    public static Singleton getInstance() {\n        return INSTANCE;\n    }\n}\n```\n\n在类加载阶段就完成实例化，JVM 保证类初始化过程线程安全。缺点是无法懒加载。\n\n### 核心原理\n\n这三种方式的本质都是**依赖 JVM 的类加载机制**——类的初始化由 JVM 保证线程安全，无需开发者手动加锁。\n\n### 选择建议\n\n- 一般场景推荐**静态内部类**（懒加载 + 线程安全）\n- 追求简洁且无特殊需求可用**枚举单例**\n- 饿汉式适用于实例占用资源少、启动就需要使用的场景"}, {"num": 27, "title": "在 Linux 命令行敲下一行命令，会进行哪些事情？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "当在Linux终端敲下一行命令（如 `ls -l`）后，系统会依次完成以下核心步骤：\n\n**1. Shell解析与执行**\n首先，Shell（如bash）会读取该命令行，进行解析：识别命令名（`ls`）、参数（`-l`），并处理环境变量、通配符扩展等。Shell还会根据分号、管道符等判断是否包含多条命令。\n\n**2. 查找可执行程序**\nShell会按顺序在`PATH`环境变量指定的目录列表中查找名为`ls`的可执行文件。若找到，则通过`fork()`系统调用创建一个子进程。\n\n**3. 执行程序**\n子进程通过`exec()`系统调用将自身替换为`ls`程序映像。内核将程序从磁盘加载到内存，为其分配地址空间，并设置执行上下文。\n\n**4. 内核处理与I/O**\n程序执行时，通过系统调用（如`readdir`）与内核交互，访问文件系统数据。最终，程序将格式化后的输出通过标准输出流（stdout）写回终端，终端渲染并显示结果。\n\n**5. 进程终止与信号处理**\n命令执行完毕后，子进程退出，Shell回收其资源，并显示提示符等待下一条命令。期间信号（如`Ctrl+C`）可能中断该流程。\n\n整个过程涉及Shell解析、进程管理、文件系统操作、系统调用及终端交互，体现了Linux“一切皆文件”的设计哲学和分层协作的机制。"}, {"num": 28, "title": "如何快速定位到五分钟内重复登录了两次的QQ号，用什么数据结构？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "定位五分钟内重复登录的QQ号，核心在于高效管理带时间窗口的记录并快速查询。建议采用**哈希表（字典）结合双向链表**的方案。\n\n**具体实现：**\n1.  **数据结构**：使用哈希表，Key为QQ号。每个Key对应的Value维护一个**双向链表**，链表节点存储该QQ号的登录时间戳，并按时间顺序排列。\n2.  **处理新登录**：当用户`QQ1`在时间`T`登录时：\n    *   在哈希表中查找`QQ1`。若不存在，创建新链表并插入`T`。\n    *   若存在，沿链表**从尾部向头部**遍历，移除所有早于`(T - 5分钟)`的过期时间戳节点。\n    *   随后将`T`插入链表头部，并检查此时链表长度。若**长度 >= 2**，则判定`QQ1`在五分钟内重复登录。\n3.  **优势**：该结构使得**移除过期记录（O(k)，k为过期数）和查询最新记录（O(1)）** 非常高效。哈希表保证了按QQ号的快速查找（O(1)）。\n\n**关键细节**：链表按时间有序，过期记录集中在尾部，可批量快速清除，确保了滑动时间窗口管理的高性能，适合高并发场景。"}, {"num": 29, "title": "如何用消息队列实现延迟任务？比如订单30分钟未支付自动取消？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题, 消息队列, 延迟消息, 定时任务", "answer": "消息队列实现延迟任务的核心方案是利用其**延迟投递机制**。以订单30分钟超时取消为例：\n\n**方案实现（以RocketMQ为例）：**\n1.  **生产者发送延迟消息**：创建订单时，发送一条延迟消息到MQ，延迟等级设为30分钟（如RocketMQ的level=6）。\n2.  **消息存储与定时投递**：MQ服务器收到消息后，将其持久化并暂存于**内部延迟队列/定时任务服务**。到达设定时间后，将消息投递到目标主题。\n3.  **消费者处理**：消费者监听目标主题，收到消息后执行业务逻辑：**查询订单状态，若未支付则调用取消订单接口**（需做幂等和并发控制）。\n\n**关键要点与考量：**\n- **可靠性**：依赖MQ消息持久化与重试机制，保障消息不丢失。消费失败需有重试和死信队列兜底。\n- **幂等性**：消费者处理时需校验订单当前状态，防止因网络抖动等原因重复取消。\n- **时间精度**：消息队列延迟通常为秒级，满足大部分业务。更高精度或复杂时间逻辑需配合定时任务扫描。\n- **扩展性**：天然解耦，可轻松应对流量高峰。支持分布式部署，避免单点问题。\n- **对比方案**：相比定时任务轮询数据库，消息队列方案**资源占用少、实时性高、系统压力分散**。\n\n**总结**：消息队列延迟消息是解决此类定时任务的优选方案，通过时间轮等算法实现高效调度，在保证可靠性的同时提供了良好的扩展性。"}, {"num": 30, "title": "如何设计一个 OAuth2.0 授权服务？Token 如何管理？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题, 系统设计", "answer": "OAuth2.0 授权服务的设计核心是构建一个安全的、标准化的代理层，负责在用户、客户端应用和资源服务器之间进行身份认证与授权。\n\n**授权服务设计要点如下：**\n1.  **核心组件**：需实现授权服务器，负责颁发令牌；需与资源服务器协同，验证令牌并保护资源。\n2.  **授权模式支持**：根据应用场景（如Web应用、移动端、服务端间调用）支持不同的授权模式，例如**授权码模式**（最安全）、客户端模式、密码模式（仅限可信客户端）等。\n3.  **端点实现**：必须实现标准端点，包括**授权端点**（用户授权）、**令牌端点**（客户端交换令牌）和可能的**令牌撤销端点**。\n4.  **安全机制**：强制使用HTTPS传输；对所有端点进行严格的客户端身份验证（如Client ID/Secret）；实施CORS策略防止CSRF攻击。\n\n**Token 管理策略如下：**\n1.  **颁发策略**：颁发**访问令牌**（Access Token）和**刷新令牌**（Refresh Token）。访问令牌生命周期短（如30-60分钟），用于访问资源；刷新令牌生命周期长，用于更换访问令牌。\n2.  **令牌存储**：**访问令牌**通常无需在授权服务器端持久化存储（JWT自包含即可），但**刷新令牌**因其敏感性，必须在授权服务器端安全存储（如数据库），并与客户端、用户、范围等信息绑定。\n3.  **令牌验证**：资源服务器通过验证令牌签名（JWT）或向授权服务器查询（introspection端点）来校验令牌有效性。\n4.  **续期与撤销**：通过有效的刷新令牌，客户端可无感换取新的访问令牌，提升用户体验。同时，需提供机制让用户或管理员能主动撤销令牌（如用户登出时撤销刷新令牌）。\n\n设计目标是实现一个安全、可扩展且符合标准的授权中心。"}, {"num": 31, "title": "如何避免用户重复下单（多次下单未支付，占用库存）", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "## 如何避免用户重复下单（多次下单未支付，占用库存）\n\n### 一、前端防重复\n- 用户点击下单后**立即禁用按钮**，防止重复提交\n- 表单提交后设置防抖，避免网络延迟导致多次点击\n\n### 二、后端幂等性设计\n- **唯一订单号**：基于用户ID+商品ID+时间戳生成唯一幂等键\n- **去重校验**：下单前查询是否存在相同幂等键的未支付订单，若存在则直接返回原订单\n- **数据库唯一索引**：对幂等键字段建立唯一约束，从数据库层面兜底\n\n### 三、库存与订单状态管理\n- **预扣库存**：下单时使用Redis原子操作（Lua脚本）扣减库存，成功后再创建订单\n- **状态流转**：订单状态设置为\"待支付\"，库存处于\"锁定\"而非\"扣减\"\n- **乐观锁控制**：数据库层面使用版本号防止并发超卖\n\n### 四、超时释放机制\n- 设置**支付超时时间**（如15-30分钟）\n- 通过**定时任务**或**延迟队列**（RabbitMQ死信队列/Redis过期监听）扫描超时订单\n- 超时后自动取消订单，释放锁定库存回Redis\n\n### 五、整体流程\n```\n下单请求 → 幂等校验 → Redis预扣库存 → 创建待支付订单 → 等待支付\n                                                        ↓ (超时)\n                                              取消订单 + 释放库存\n```\n\n**核心思想**：前端防抖 + 后端幂等 + 库存预扣 + 超时释放，四层防护确保不重复占用库存。"}, {"num": 32, "title": "如果一个GPU集群的LLM处理能力为1000tokenss，那1000个用户同时并发访问，响应给每个用户的性能只有1 tokens吗？怎么分析性能瓶颈", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题, 大模型", "answer": "不完全是。将1000 tokens/s的总吞吐量简单除以1000用户，得到1 token/s/用户，这是一个常见误解。性能瓶颈需从以下维度分析：\n\n**1. 核心概念澄清：**\n- **吞吐量（Throughput）**：1000 tokens/s是集群每秒能处理的**总**token数，衡量整体产能。\n- **单用户延迟（Latency）**：单个用户从发起请求到接收完整响应的时间，是用户体验的关键。它取决于请求的输出长度和集群的并行处理能力。\n\n**2. 性能瓶颈分析点：**\n- **硬件资源**：GPU计算核心/显存是否饱和？显存带宽是否成瓶颈？CPU、内存、网络IO（如模型分片通信）是否受限？\n- **软件与模型**：\n  - **模型本身**：模型参数大小、计算复杂度。\n  - **推理引擎**：是否采用高效算子、批处理（Batching）策略。动态批处理能显著提升吞吐。\n  - **并发管理**：请求队列管理策略（如先来先服务、优先级）、上下文切换开销。\n- **系统层面**：负载均衡是否均匀、输入/输出序列化开销、缓存机制（如KV Cache）是否生效。\n\n**3. 优化方向：**\n- **纵向优化**：模型量化、蒸馏；选用更快的推理引擎（如vLLM）；优化批处理"}, {"num": 33, "title": "如果一笔订单，用户在微信和支付宝同时支付，会怎么样？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "如果一笔订单在微信和支付宝同时发起支付，本质上是用户对同一笔交易的重复支付请求。从系统设计角度，必须确保**支付一致性**，避免重复扣款或资金损失。具体处理逻辑如下：\n\n1. **唯一订单号与幂等设计**：每笔订单生成唯一的订单号（如`order_id`），支付接口需实现**幂等性**。无论用户发起多少次支付请求，系统均依据同一订单号处理，确保只生成一笔有效支付记录。\n\n2. **并发锁控制**：在支付请求入口处，对订单号加**分布式锁**（如Redis锁）。当第一个支付请求到达时加锁，后续相同订单号的请求直接返回“支付处理中”或提示重复操作，避免并发导致状态错乱。\n\n3. **支付状态同步与核销**：若两个渠道均返回成功（概率极低，因支付平台通常会拦截重复），系统需通过**异步对账**和**主动查询接口**核对实际扣款情况。若发现重复支付，应自动触发退款流程，并记录日志供人工核查。\n\n4. **用户体验提示**：前端页面在用户支付时应明确提示“请勿重复提交”，支付完成后跳转至统一结果页，避免用户因等待焦虑而重复操作。\n\n综上，核心是通过**唯一标识、幂等设计、并发控制**三重机制保障交易唯一性。最终由后端系统负责状态校验和资金一致性维护，确保即使用户误操作，也不会影响资金安全。"}, {"num": 34, "title": "如果现在有 500G 数据需要排序，但是你只有 4G 内存，如何实现？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "采用外部排序（External Sorting）算法，主要分为三个阶段：\n\n1. **分块排序（Generate Initial Runs）**\n   - 将500GB文件按内存容量（如4GB）分割成约125个有序的临时文件。\n   - 每次读入4GB数据，在内存中使用快速排序等算法排序后写回磁盘。\n\n2. **多路归并（Merge Sort）**\n   - 使用最小堆维护每个临时文件的首元素，每次取出最小值写入结果文件。\n   - 取出元素后，从对应临时文件读取下一个数据重新调整堆。\n   - 可通过设置读写缓冲区（如每块1MB）减少磁盘I/O次数。\n\n3. **工程优化**\n   - 并行处理：多线程读取不同临时文件提升I/O吞吐。\n   - 压缩排序：若数据可压缩，可减少磁盘存储和传输量。\n   - 分布式扩展：若单机资源仍不足，可采用MapReduce框架将任务分发到多台机器。\n\n此方法的时间复杂度为O(N log N)，主要瓶颈在磁盘I/O，实际性能取决于排序数据的分布和存储介质速度。"}, {"num": 35, "title": "如果线上接口被恶意刷流量了，要如何解决？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "## 线上接口被恶意刷流量的解决方案\n\n### 一、紧急止损\n\n**立即限流**：通过网关（如Nginx、Spring Cloud Gateway）对异常IP进行频率限制，设置QPS阈值触发熔断，必要时临时封禁恶意IP段。\n\n**服务降级**：对非核心接口暂停服务或返回静态兜底数据，保障核心链路可用。\n\n### 二、流量识别\n\n- **特征分析**：通过日志识别异常流量特征，如高频访问、固定UA、无Referer、请求参数规律性强等\n- **人机验证**：接入滑块验证码或行为验证（如阿里云人机验证），区分真实用户与机器流量\n- **Token机制**：请求前需先获取动态Token，增加攻击成本\n\n### 三、架构防护\n\n- **接入WAF**：使用云厂商WAF或开源方案（如ModSecurity）拦截恶意请求\n- **多层限流**：网关层+应用层双重限流，采用令牌桶或滑动窗口算法，支持分布式限流（如Sentinel、Redis+Lua）\n- **CDN缓存**：对静态资源和可缓存接口启用CDN，减轻源站压力\n\n### 四、监控与预防\n\n- **实时告警**：建立QPS、响应时间、错误率的监控告警体系\n- **黑白名单**：维护IP信誉库，结合风控系统动态调整\n- **事后复盘**：分析攻击来源和手法，持续完善防护策略，必要时联系运营商或报警处理"}, {"num": 36, "title": "如果要实现一个抢红包的功能，红包金额是如何计算的？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "**抢红包金额计算方案**\n\n红包金额计算的核心原则是**公平随机且总和固定**。常用算法是**二倍均值法**：\n\n1.  **算法原理**：假设剩余红包金额为 M，剩余人数为 N，则每人抢到的金额为 `random(0.01, 2 * (M / N))` 元。最后一个人领取剩余所有金额。该方法能保证期望值公平，且每个人都可能抢到最大金额。\n\n2.  **边界与精度**：\n    *   设置每人**最小金额为 0.01 元**，防止金额归零。\n    *   使用**整数单位（如分）** 进行计算，最后再转换为元，避免浮点数精度问题。\n    *   需要处理并发，**锁定红包记录**，保证同一时间仅一人操作，更新剩余金额和人数。\n\n3.  **实际应用补充**：\n    *   红包记录和领取明细需持久化存储。\n    *   可配合随机种子或更复杂的算法防止规律被摸透。\n    *   高并发场景下，常通过**预拆分金额**或**异步入账**提升性能。\n\n此方案在保证随机性的同时，兼顾了公平性与实现效率。"}, {"num": 37, "title": "如果项目需要你实现敏感词过滤功能，如何实现？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "敏感词过滤功能的实现需要结合准确性、性能和可维护性进行综合设计。核心方案可分为以下步骤：\n\n**1. 词库构建与预处理**  \n建立基础敏感词库，可来源业务反馈、公开词库或法律文本。为提升效率，需预处理数据结构。**首选AC自动机**，它基于Trie树实现多模式串并行匹配，一次遍历即可识别所有敏感词，时间复杂度为O(n)，远优于逐个关键词匹配。\n\n**2. 匹配策略分层设计**  \n- **基础层**：实现精确匹配和部分屏蔽（如“法*功”）。\n- **进阶层**：结合拼音转换、同音字替换（如“赌博”与“毒波”）、简繁体转换，应对变形输入。\n- **上下文层**：可利用简单规则或轻量NLP模型，减少“北京杀人胡同”这类正常词误判。\n\n**3. 架构与性能优化**  \n- **缓存预热**：服务启动时加载词库并编译AC自动机状态机，避免重复计算。\n- **动态更新**：提供管理后台或API，支持词库热更新。采用双缓冲或版本号机制，确保更新时服务无阻塞。\n- **分布式一致性**：通过消息队列（如Kafka）广播词库变更事件，各节点异步同步，避免压力。\n\n**4. 工程实践考量**  \n- **降级与监控**：敏感词服务故障时可降级为放行或基于核心词库匹配，并设置拦截率报警。\n- **日志与追溯**：记录命中关键词及上下文，用于词库优化和误判分析。\n- **性能权衡**：对超长文本可分块处理，高并发场景下考虑将过滤逻辑下沉到数据写入层（如CMS审核）。\n\n**总结**：以AC自动机为核心匹配引擎，通过分层匹配策略提升准确率，结合分布式架构与热更新机制保障系统健壮性，最终形成可扩展、易维护的解决方案。"}, {"num": 38, "title": "当前有个本地操作 A，远程操作 B，我需要保证 A 和 B 事务的一致性，你会如何实现？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "分布式事务是保证跨系统数据一致性的核心问题。针对本地A与远程B的操作，我会根据业务场景、性能要求和实现复杂度选择以下方案：\n\n**1. 两阶段提交（2PC）**  \n通过协调者统一管理。第一阶段协调者询问所有参与者是否就绪；第二阶段根据反馈统一提交或回滚。该方案强一致性，但存在同步阻塞、单点故障问题，适合数据库层面的事务。\n\n**2. TCC（Try-Confirm-Cancel）**  \n将业务拆分为三个阶段：Try（预留资源）、Confirm（确认提交）、Cancel（撤销）。开发者需自行实现三个接口，更适合高并发的长事务场景，但对业务侵入性强。\n\n**3. 本地消息表/事务消息**  \n将A操作与一条消息写入本地数据库同一事务，再通过定时任务或MQ将消息发送给B处理。B成功消费后需确认，保证最终一致性。此方案可靠性高，是实现解耦的常用选择，尤其搭配RocketMQ事务消息时可简化流程。\n\n**4. Saga模式**  \n将事务拆分为多个本地事务，每个事务有对应的补偿操作。若某一步失败，则逆向执行补偿。适用于长流程业务，如订单与库存服务。\n\n**方案选择建议**：  \n- 要求强一致且参与方少，可选2PC或TCC。  \n- 更关注最终一致性和解耦，首选本地消息表或MQ事务消息。  \n- 业务流程长、服务多，Saga模式更灵活。\n\n实际中需结合CAP理论权衡，并在幂等、重试、补偿等方面做好设计，以确保系统在异常情况下仍能保持数据一致。"}, {"num": 39, "title": "当客户端有重连机制，当服务意外宕机重新部署启动时，怎么避免流量洪峰？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "服务重启时避免流量洪峰的关键在于实现“有序接入”与“平滑过渡”，可从服务端与客户端协同控制入手：\n\n**1. 服务端优雅启动（关键）**\n- **分阶段健康检查**：启动初期返回非就绪状态，待核心模块（如数据库连接池、缓存预热）初始化完成后再开放流量。\n- **流量切换机制**：在负载均衡层面实现“连接排空”，新实例启动后仅接受少量测试流量，验证无误后再逐步切流。\n- **预热设计**：提前加载热点数据或执行预热任务，避免冷启动时的突发计算压力。\n\n**2. 客户端重连策略优化**\n- **指数退避与随机延迟**：客户端断线后应采用随机化的退避重连策略（如在1s/2s/4s基础上增加±20%抖动），避免所有客户端同时重试。\n- **连接状态检测**：客户端应监测服务端健康状态接口，在服务明确就绪后再发起业务请求。\n\n**3. 流量控制兜底**\n- **服务端限流**：在网关或应用层设置启动初期的限流策略（如令牌桶算法），逐步提升流量阈值。\n- **客户端排队机制**：针对非实时性场景，客户端可本地缓存请求并在服务恢复后有序上报。\n\n**4. 架构层面保障**\n- **无状态服务设计**：结合负载均衡器的慢启动配置，逐步增加新实例权重。\n- **监控与自动干预**：设置重连风暴告警，触发自动扩容或流量熔断。\n\n核心思路是将“全量客户端同时冲击单点服务”转化为“可控、分散的流量注入”，通过技术手段实现服务启动阶段的自然过渡。"}, {"num": 40, "title": "微服务中远程调用的超时时间应该设置为多少合适？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "微服务远程调用的超时时间没有固定值，核心原则是：**在保证业务成功的前提下尽可能短**，避免因局部故障拖垮整体系统。\n\n具体设置可参考以下步骤：\n1. **基于调用链路计算**：分析关键接口的最大允许耗时，将总超时按调用层级（如A→B→C）合理分配，确保端到端响应时间。\n2. **区分服务类型**：\n   - 同步核心链路：建议1-3秒\n   - 非实时查询/异步处理：可放宽至5-10秒\n   - 依赖外部服务（如支付）：需匹配第三方SLA，通常3-5秒\n3. **动态调整与兜底**：结合熔断机制（如Sentinel、Hystrix），在响应缓慢时自动降级；通过监控（如Prometheus）持续观察P99/P999延迟，定期优化阈值。\n\n**关键提醒**：超时时间应略高于正常P99延迟，但必须低于业务容忍阈值。例如下游服务P99为200ms，超时可设500ms-1s，并配置快速失败重试策略。"}, {"num": 41, "title": "我们线上软件安装包地址，这几天访问量暴增，现在 CDN 流量一直异常，这是什么原因？如何解决？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "**原因分析：**\n访问量暴增导致CDN流量异常，核心原因通常有三点：\n1.  **流量激增来源**：可能是新产品发布、营销活动带来的正常增长，但更需警惕是否遭受了**DDoS攻击或恶意刷流量**（如爬虫、刷包）。\n2.  **CDN配置问题**：缓存策略（如TTL）设置过短或未生效，导致大量请求穿透CDN直接回源，消耗源站带宽与CDN回源流量。\n3.  **源站负载过高**：源站服务器性能或带宽不足，无法及时响应CDN回源请求，导致CDN节点频繁重试或降级，推高整体流量。\n\n**解决方案：**\n1.  **立即应急处置**：\n    *   **安全防护**：通过CDN或云防火墙**紧急启用“DDoS防护”和“CC防护”**，封禁高频恶意IP，并分析访问日志识别异常User-Agent。\n    *   **优化缓存**：立即检查并**延长安装包等静态资源的缓存时间（如设置Cache-Control max-age）**，确保高频访问从CDN边缘节点响应。\n    *   **流量调度**：若源站承压，可暂时通过CDN配置进行**限流（Rate Limiting）**，或引导部分流量至备用下载地址。\n\n2.  **中期优化与监控**：\n    *   **源站扩容**：评估业务增长，对源站进行**水平扩展（增加服务器）或垂直扩展（提升带宽/性能）**。\n    *   **启用CDN高级功能**：如**防盗链、IP限速、UA限速**，从源头遏制恶意流量。\n    *   **建立监控告警**：设置CDN带宽、QPS、回源率的**实时监控与阈值告警**，便于快速响应。\n\n3.  **长期架构审视**：\n    *   **考虑多源站与负载均衡**，提升整体可用性。\n    *   对于安装包等大文件，评估是否使用**对象存储（如S3）作为源站**，其更擅长处理高并发静态资源请求。\n\n**总结**：核心是快速区分正常增长与恶意攻击，通过“安全拦截+优化缓存”紧急止血，再从源站、监控、架构层面做系统性加固。"}, {"num": 42, "title": "把单体项目进行了多机部署，多台服务器是如何共享用户登录信息的？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "单体项目多机部署时，用户登录状态的共享本质上是分布式会话管理问题。核心解决方案有以下几种：\n\n1. **集中式Session存储**（最常用方案）  \n   将各服务器的Session统一存储至Redis或Memcached等分布式缓存中。用户登录后，服务器生成SessionID存入缓存，后续请求均通过该ID从缓存获取会话信息，实现状态共享。\n\n2. **基于Token的无状态方案**  \n   使用JWT（JSON Web Token）等自包含令牌。登录成功后，服务器生成包含用户信息的加密Token返回客户端，后续请求携带Token，各服务器通过解密验证即可识别用户，无需集中存储。\n\n3. **会话粘滞（Session Sticky）**  \n   通过负载均衡器将同一用户的请求始终路由到同一服务器。此方案实现简单，但可能导致负载不均，且服务器故障时会话丢失，通常作为辅助方案。\n\n**实际生产建议**：  \n优先采用**集中式Session存储+Redis**的方案，兼顾扩展性与可靠性。同时设置合理的Session超时与淘汰策略，并注意Redis的高可用部署。若采用JWT，需注意令牌刷新机制及敏感信息的加密存储。"}, {"num": 43, "title": "接口变慢了应该如何排查？导致接口变慢的原因有哪些？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "接口变慢的排查可遵循从外到内、从宏观到微观的思路：\n\n**一、排查步骤**\n1.  **监控定位**：通过APM工具或日志，定位是整个服务变慢还是单个接口，并查看响应时间分布。\n2.  **网络层**：检查客户端到服务端的网络延迟、带宽以及是否存在丢包。\n3.  **服务端**：\n    *   **代码与逻辑**：检查接口内是否有耗时操作（如复杂计算、同步IO）、代码性能瓶颈或死锁。\n    *   **依赖分析**：查看数据库、缓存、第三方服务的调用耗时是否异常。\n    *   **日志与监控**：分析服务端日志、GC日志，并监控CPU、内存、磁盘IO和网络IO等系统资源使用率。\n4.  **资源层**：确认服务器（或容器）的资源（CPU、内存、连接数）是否达到瓶颈，是否存在线程池满等情况。\n\n**二、常见原因**\n1.  **代码与设计**：算法复杂度过高、存在循环嵌套、锁竞争激烈或同步阻塞。\n2.  **数据库**：SQL语句缺失索引或查询效率低、数据库连接池耗尽、慢查询、锁表。\n3.  **缓存**：缓存击穿、缓存雪崩、缓存与数据库数据不一致导致回源。\n4.  **第三方服务**：依赖的微服务、RPC接口或外部API响应变慢。\n5.  **基础设施**：网络带宽不足、服务器资源（CPU/内存）过高、磁盘读写IO瓶颈。\n6.  **并发与压力**：突发流量超过系统承载能力，导致排队和资源争抢。\n\n总之，需结合监控数据，逐步定位性能瓶颈点，再针对性优化。"}, {"num": 44, "title": "数据同步任务执行到一半失败了，如何保证数据的一致性？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "数据同步失败时保证一致性，需根据业务场景选择合适策略：\n\n**1. 重试与幂等**：同步任务需设计为幂等操作，失败后可通过安全重试或补偿机制修复，避免重复处理导致数据错误。\n\n**2. 分布式事务**：采用两阶段提交（2PC）、TCC或Saga模式。例如Saga将大事务拆分为多个本地事务，通过补偿操作回滚失败步骤，保障最终一致性。\n\n**3. 消息队列可靠性**：利用消息中间件的持久化、确认重试机制（如RocketMQ事务消息），确保数据变更事件不丢失。\n\n**4. 对账与修复**：通过定期对账脚本比对源端与目标端数据，自动或人工修复差异，作为兜底方案。\n\n实践中常组合使用：关键业务用分布式事务保证强一致；非关键场景用消息队列+幂等+对账实现最终一致，兼顾性能与可靠性。"}, {"num": 45, "title": "数据同步过程中出现 OOM 内存溢出，应该如何排查和解决？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "**排查步骤：**\n1.  **确认与定位：** 首先通过监控或日志确认OOM发生的时间点和具体进程。检查JVM的GC日志，观察是否频繁Full GC且内存回收效果不佳，这常是内存泄漏或分配不足的信号。\n2.  **分析堆转储：** 在JVM启动参数中添加 `-XX:+HeapDumpOnOutOfMemoryError`，使其在OOM时自动生成堆转储文件。使用MAT、VisualVM等工具分析该文件，重点关注占用内存最大的对象集合及它们的引用链，这是定位问题根源的关键。\n3.  **审查同步代码：** 重点检查数据同步作业的代码。常见问题包括：一次性将海量数据从源头全部加载到内存；使用了不恰当的集合类型或缓存策略导致对象堆积；存在未关闭的资源（如数据库连接、文件流）引发的内存泄漏。\n\n**解决方案：**\n1.  **调整JVM参数：** 根据应用实际需求和服务器资源，合理增加 `-Xmx`（最大堆内存）和 `-Xms`（初始堆内存）。注意，这不是根本解决方法，需配合代码优化。\n2.  **优化代码与架构：**\n    *   **分批/流式处理：** 将大批量数据同步改为分页查询或使用游标、流式读取，避免一次性加载。\n    *   **优化数据结构：** 选用内存效率更高的集合类，及时清理不再使用的对象引用。\n    *   **引入中间件：** 对于大规模同步，可引入消息队列（如Kafka）进行生产消费解耦，实现背压和流量削峰，从根本上降低同步过程的瞬时内存压力。\n3.  **监控与预警：** 建立完善的内存使用监控和预警机制，在内存使用率持续高位时提前介入，避免问题恶化。"}, {"num": 46, "title": "数据库表上新增一个字段，如果这个表正在进行读写操作，如何处理才能不影响现有读写操作？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "为了在不停止现有读写操作的情况下为数据库表新增字段，可以采用在线模式变更策略，核心是避免表锁定和保证数据一致性。具体步骤如下：\n\n首先，使用数据库的在线DDL功能或第三方工具（如MySQL的Online DDL、PostgreSQL的ALTER TABLE，或pt-online-schema-change、gh-ost）。这些工具通过创建临时表、同步数据并原子切换，实现在低延迟下添加字段，而不阻塞读写操作。\n\n其次，在变更时，为新字段设置默认值或允许NULL，以避免全表更新带来的性能影响。这样，应用程序代码可以逐步适配新字段，例如先部署支持新字段的代码版本，再执行数据库变更。\n\n此外，务必在测试环境中验证变更，监控生产环境性能，确保索引优化和数据备份。如果涉及大量数据，可考虑分批次迁移，以减少对业务的影响。\n\n最终，通过在线工具、默认值设置和分阶段部署，可以在不影响现有操作的前提下安全完成字段新增。"}, {"num": 47, "title": "新项目要上线了，你会关注哪些指标，为什么？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "新项目上线后，我会重点关注三个层面的指标：\n\n**1. 系统性能与稳定性（核心）**\n*   **吞吐量与响应延迟：** 监控API的每秒请求数和平均/ P99延迟，这是衡量系统处理能力和用户体验的最直接指标。\n*   **错误率：** 密切关注HTTP 5xx和4xx错误率，能快速发现代码缺陷、依赖服务故障或流量异常。\n*   **资源使用率：** 监控CPU、内存、磁盘I/O和网络带宽，防止资源耗尽引发雪崩，并为容量规划提供依据。\n\n**2. 核心业务指标（价值导向）**\n*   **核心链路成功率：** 针对项目的关键业务流程（如下单、支付），监控其端到端的成功率。这比单纯的系统错误率更能反映真实的用户价值。\n*   **关键业务转化率：** 如注册完成率、下单转化率等，用于衡量产品功能是否达到预期目标。\n\n**3. 基础设施与成本**\n*   **第三方依赖健康度：** 监控数据库、缓存、消息队列及外部API的调用延迟与成功率，确保依赖稳定。\n*   **服务实例数与成本：** 关注自动扩缩容情况及产生的云资源费用，确保服务在可控成本下平稳运行。\n\n**为何关注这些？** 因为它们共同构成了一个从**技术健康**、**业务价值**到**运营成本**的完整观测体系。在上线初期，前两者是快速发现并解决问题的“眼睛”；随着项目稳定，所有指标共同为系统的持续优化和决策提供数据支持。"}, {"num": 48, "title": "暂时先不考虑分库分表，如果单表的数据量很大，如何优化？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "针对单表数据量大的情况，可以从三个层面进行优化：\n\n首先，**数据库层优化**是基础。检查并优化表结构，确保字段类型合理；对高频查询字段添加合适的索引，并利用覆盖索引减少回表；启用查询缓存（需评估数据更新频率）。\n\n其次，**查询优化**至关重要。避免使用`SELECT *`，只查询必要字段；对大结果集进行分页，并谨慎使用`OFFSET`；优化JOIN查询，确保关联字段有索引；将复杂的大查询拆分为多个小查询。\n\n最后，**适度使用索引与缓存**。索引能加速查询，但会增加写开销和存储，需权衡。对于热点数据，可引入应用层缓存（如Redis）来减轻数据库压力。\n\n此外，定期监控慢查询日志，进行针对性优化。若优化后仍无法满足需求，再考虑分库分表。核心思路是：先深度优化单机性能，再考虑分布式方案。"}, {"num": 49, "title": "有一张表里面有三个字段，分别是（id，开始时间，结束时间），表中数据量为 5000W，如何统计流量最大的时候有多少条数据？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "场景题", "answer": "## 解题思路\n\n**核心问题转化**：统计流量最大的时刻，本质是求时间区间的**最大重叠数**（即某时刻最多有多少条记录同时\"活跃\"）。\n\n---\n\n**方案一：扫描线算法（推荐）**\n\n将每条记录拆成两个事件点：\n- 开始时间 → `+1`\n- 结束时间 → `-1`\n\n将所有事件点按时间排序，顺序扫描并累加计数器，过程中取最大值即可。\n\n时间复杂度：O(NlogN)，空间复杂度：O(N)\n\n**方案二：数据量大的优化处理**\n\n5000W 数据可能无法一次性加载到内存，可以：\n1. **分片处理**：按时间范围分段，每段独立计算后合并结果\n2. **外部排序**：借助磁盘排序后流式处理\n\n---\n\n**方案三：SQL 解法（适用于离线分析）**\n\n```sql\n-- 思路：将时间离散到最小粒度，统计每个时刻的活跃数\nSELECT time_slot, COUNT(*) AS active_count\nFROM (\n    SELECT generate_series(start_time, end_time, interval '1 min') AS time_slot\n    FROM table_name\n) t\nGROUP BY time_slot\nORDER BY active_count DESC\nLIMIT 1;\n```\n\n---\n\n**总结**：5000W 量级推荐用**扫描线算法**，核心是事件拆分 + 排序 + 线性扫描，时间复杂度 O(NlogN)，工程实现简单高效。"}, {"num": 50, "title": "有人恶意攻击我们的注册功能，伪造手机号大量下发验证码，除了限流之外，如何解决这个问题？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "针对恶意刷验证码的攻击，可从以下几个维度进行防护：\n\n**1. 前置人机验证**\n在发送短信验证码前，要求用户完成图形验证码或滑块验证，有效拦截机器脚本攻击，这是最基础且有效的手段。\n\n**2. 手机号校验与过滤**\n对手机号进行多重校验：验证格式合法性、号段真实性，接入第三方服务识别虚拟号、接码平台号码，对异常号段进行拦截。\n\n**3. 多维度限流策略**\n除简单的IP限流外，还需结合设备指纹、浏览器指纹、账号维度进行限流。设置单IP/单设备每日发送上限，短时间内同号段的发送频率限制等。\n\n**4. 黑名单与风控体系**\n建立IP黑名单、手机号黑名单，对频繁触发验证码的账户或设备标记为风险用户。可接入风控系统，对异常行为（如批量注册、模拟器访问）实时识别并拦截。\n\n**5. 产品策略优化**\n- 注册流程增加邮箱验证等备选方案\n- 验证码发送后页面不返回明确提示（避免被用于探测活跃号码）\n- 短信模板中加入免责声明，降低投诉风险\n\n**6. 短信服务商配合**\n与短信服务商对接，开启发送频率限制，利用其平台的防刷能力，并实时监控短信发送量，异常激增时触发熔断机制。\n\n综合运用以上手段，形成多层防护体系，既能有效抵御恶意攻击，又能保障正常用户的使用体验。"}, {"num": 51, "title": "横跨十几个分布式服务的慢请求要如何排查？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "排查跨服务慢请求需结合分布式追踪、日志分析和监控告警。核心思路是 **“由全局到局部，由链路到节点”**。\n\n首先，使用**分布式链路追踪工具（如SkyWalking、Jaeger）** 构建完整调用链。通过Trace ID串联各服务日志，快速定位**耗时异常的关键服务或数据库节点**。\n\n其次，对可疑节点深入排查：\n1.  **资源层面**：检查CPU、内存、磁盘I/O及网络带宽，排查资源争抢或配置瓶颈。\n2.  **代码与中间件**：分析慢SQL、线程池阻塞、缓存失效、外部服务响应慢等问题。\n3.  **应用日志**：通过关键字检索关联错误堆栈或超时告警。\n\n同时，需**监控各服务P99延迟、错误率及系统指标**，并设置动态基线告警。优化时可考虑：优化查询、引入缓存、异步化调用、调整超时重试策略及扩容瓶颈服务。\n\n总结：遵循“追踪定位 → 监控佐证 → 根因分析 → 针对性优化”的闭环流程，结合自动化工具可显著提升排查效率。"}, {"num": 52, "title": "每次进入订单列表页面都会触发全量同步？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 线程问题排查", "answer": "订单列表页面频繁触发全量同步通常源于设计缺陷或配置不当。主要原因包括：前端缺少本地缓存机制，每次加载都向后端发起全量数据请求；后端未实现增量同步策略，或同步标志位管理混乱导致误触发；线程安全问题可能引发同步任务异常重复执行。\n\n排查时可从三方面入手：检查前端请求逻辑，确认是否携带合理的缓存标识（如时间戳、版本号）；审查后端同步接口的触发条件和线程池配置，观察是否存在任务重复提交；通过日志追踪同步任务的执行频次和触发源头。\n\n解决方案包括：优化前端请求策略，采用缓存失效机制（如ETag或Last-Modified）；后端实施增量同步，基于数据变更时间戳或版本号仅更新差异部分；对于强制全量同步场景，增加同步状态锁和防重入校验，避免并发任务堆积；引入消息队列解耦同步过程，提升系统健壮性。\n\n最佳实践是结合业务场景设计多级缓存与智能同步策略，确保数据一致性的同时减少无效网络传输和资源消耗。"}, {"num": 53, "title": "每秒 200 笔订单请求到支付服务，支付服务需调用第三方支付，限流 100 笔每秒。请你设计支付服务，要求分布式的，并且是 FIFO，不超过第三方限流量，但是要用满第三方100 笔每秒的量，请问如何设计，需要考虑哪些方面？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "**设计方案：**\n\n**核心思路：** 采用分布式消息队列（如Kafka）作为异步缓冲和排序层，结合精确限流的生产者和消费者模型，实现分布式FIFO与流量控制。\n\n**关键组件与流程：**\n\n1.  **接入层与分布式排序：**\n    *   200 QPS的请求首先写入一个**分布式消息队列**（例如Kafka）。使用订单ID作为消息的Key，通过分区（Partition）机制，相同订单的请求会顺序地进入同一分区，从而在**消费侧**天然实现分布式环境下的**FIFO**。\n    *   接入层迅速响应客户端“已接收”，保证系统解耦和高吞吐。\n\n2.  **限流与消费控制层（核心）：**\n    *   部署多个**支付网关微服务**作为消费者，共同消费消息队列。\n    *   **集中式/分布式限流：** 使用限流组件（如Sentinel或自建）在消费端进行**精确的滑动窗口限流**，严格控制所有消费者实例的总发送速率≤100笔/秒。\n    *   **流量回收与用满机制：**\n        *   **动态批次：** 消费者以微批次（如每10-20毫秒拉取一批）处理消息，批次大小动态调整，避免突发。\n        *   **令牌桶/漏桶算法：** 限流组件按100个/秒的速率释放令牌，有令牌的请求才能调用第三方，精确保证“用满但不超”。\n        *   **快速失败与重试：** 第三方失败或限流触发时，消息可暂回队列或移入延迟队列，由消费者稍后重试，确保吞吐量不浪费。\n\n3.  **异步处理与监控：**\n    *   支付结果通过回调或消息通知接入层，更新订单状态。\n    *   监控消息队列积压量、限流触发次数、第三方成功率，动态调整消费实例数和批次参数。\n\n**总结：** 此设计通过消息队列实现分布式解耦与FIFO，通过消费端的精确分布式限流算法（如滑动窗口）严格遵守第三方配额，并通过动态批次和快速重试机制最大化利用100 QPS的额度。"}, {"num": 54, "title": "灰度发布过程中，如何保证新旧版本数据的一致性？如果新版本有数据库结构变更怎么办？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 系统设计, 场景题, 数据库, 灰度发布", "answer": "灰度发布保证数据一致性的核心策略是**向前兼容设计**与**分阶段迁移**。具体分为以下要点：\n\n1. **数据兼容性设计**：新版本代码需同时兼容新旧数据结构。例如，数据库新增字段应设置默认值，或采用可空字段；删除字段前先确保业务逻辑不再依赖。通过ORM映射层或数据访问层屏蔽差异。\n\n2. **双读/双写与数据同步**：对于关键数据，灰度期间采用双写（同时写入新旧表）或双读（新版本读取时做数据转换），通过异步任务进行数据校验与修正，确保最终一致性。\n\n3. **渐进式数据库变更**：\n   - **预发布阶段**：先上线兼容性数据库变更（如加字段、索引），不影响旧版本运行。\n   - **灰度阶段**：通过特性开关控制新逻辑是否触发新字段写入，逐步扩大流量比例。\n   - **数据迁移**：后台任务逐步回填历史数据，必要时采用分表分库平滑迁移。\n\n4. **回滚与监控**：保留旧版本数据库回滚能力，通过数据版本标记（如增加schema版本字段）区分数据结构，同时加强一致性监控与报警。\n\n若存在数据库结构变更，应遵循**仅增不改、先增后删**原则，通过中间版本过渡，避免直接破坏旧版本依赖。"}, {"num": 55, "title": "现在我让你把单体服务拆成多个微服务，这些服务之间如何自动发现彼此？具体原理是什么？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "在微服务架构中，服务自动发现通常依赖一个核心组件：**服务注册中心**（如Nacos、Consul、Eureka）。其原理可分为两步：\n\n**1. 服务注册（服务端）**\n每个微服务启动时，会作为客户端，主动将自己的网络地址（如IP、端口）与元数据（如服务名、版本）上报到注册中心。此后，服务会通过**心跳机制**定期向注册中心报告“存活”状态。若服务下线或故障，心跳停止，注册中心便会将其剔除。\n\n**2. 服务发现与调用（客户端）**\n当服务A需要调用服务B时，其内置的客户端（如Spring Cloud的LoadBalancer、Feign）会向注册中心发起查询：“服务B的实例地址在哪里？”。注册中心返回一个可用的服务B实例地址列表。客户端随后会采用**负载均衡**策略（如轮询、随机）从列表中选择一个实例发起调用，从而实现动态的、去中心化的服务发现与通信。\n\n**核心价值**：此机制解耦了服务间的硬编码地址依赖，使得系统具备**弹性伸缩**和**故障自愈**能力，是微服务高可用的基础。"}, {"num": 56, "title": "现在手头上有一个单体项目，系统的整体 QPS 到了 1 万了，要微服务化拆分吗？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "QPS达到1万是系统面临挑战的一个重要信号，但**是否进行微服务化拆分，核心依据不是单一指标，而是系统的具体痛点**。拆分决策应基于以下综合评估：\n\n首先，**分析性能瓶颈**。如果瓶颈在于单体应用的某个模块（如复杂计算、慢SQL），可先尝试在单体内部优化或针对该模块进行垂直拆分。若数据库成为主要压力源，应优先考虑读写分离、分库分表或引入缓存，而非立即拆解应用。\n\n其次，**审视业务与团队现状**。如果业务模块耦合严重，变更频繁且相互影响，或团队规模扩大导致协同效率低下，拆分能提升开发迭代速度。反之，若业务逻辑简单、团队紧凑，拆分反而会增加复杂性。\n\n最后，**评估非功能性需求与未来预期**。若系统要求极高的可用性、需要独立伸缩特定服务、或业务增长迅猛，微服务架构的优势将更为明显。\n\n**建议决策路径**：优先在单体内部进行**垂直模块划分**（逻辑解耦），并优化数据库与缓存。若仍无法满足扩展性、可靠性或团队效率要求，再基于清晰的边界进行**渐进式服务拆分**。切忌为了技术潮流而盲目拆分，应让架构演进紧跟业务实质需求。"}, {"num": 57, "title": "现在有 40 亿个 QQ 号，给你 1G 的内存，如何实现去重？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "在面对40亿QQ号去重且仅有1GB内存"}, {"num": 58, "title": "现在有一天内的大量日志，每条日志记录了用户id, 登陆时间,登出时间 {userid, login_time, logout_time}, 时间单位是秒。怎么求出一天内的最大在线人数？以及维持最大在线人数的最长持续时间？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "# 求一天内最大在线人数及最长持续时间\n\n## 核心思路：事件点扫描法（扫描线）\n\n将每条日志 `{userid, login_time, logout_time}` 拆分为两个事件：\n- **登录事件**：`login_time` 时刻，人数 +1\n- **登出事件**：`logout_time` 时刻，人数 -1\n\n## 算法步骤\n\n**第一步：生成事件列表**\n```python\nevents = []\nfor user in logs:\n    events.append((login_time, +1))\n    events.append((logout_time, -1))\n```\n\n**第二步：排序**\n按时间升序排序；时间相同时，**登出事件优先于登录事件**（避免重复计数）。\n\n**第三步：扫描统计**\n```python\ncurrent_online = 0\nmax_online = 0\nmax_duration = 0\nlast_time = 0\n\nfor time, delta in sorted(events):\n    if current_online == max_online and max_online > 0:\n        max_duration += time - last_time  # 累计最大在线时长\n    \n    current_online += delta\n    \n    if current_online > max_online:\n        max_online = current_online\n        max_duration = 0  # 重置时长\n        last_time = time  # 更新起点\n    elif current_online == max_online:\n        last_time = time  # 更新起点\n```\n\n## 关键细节\n\n| 问题 | 处理方式 |\n|------|---------|\n| 时间相同 | 按 \"先登出后登录\" 排序，避免虚增 |\n| 区间开闭 | `[login, logout)` 左闭右开，登出时刻不计入在线 |\n| 连续时段 | 同一最大人数的连续区间合并计算 |\n\n## 复杂度分析\n\n- **时间复杂度**：O(n log n)，主要消耗在排序\n- **空间复杂度**：O(n)，存储事件列表\n\n## 示例验证\n\n```\n用户A: [10, 30]  用户B: [20, 50]  用户C: [25, 40]\n\n事件排序：(10,+1)(20,+1)(25,+1)(30,-1)(40,-1)(50,-1)\n\nt=10: 1人  t=20: 2人  t=25: 3人(最大)\nt=30: 2人  t=40: 1人  t=50: 0人\n\n最大在线人数：3人，持续时间：30-25 = 5秒\n```\n\n这道题本质是**区间重叠计数**问题，扫描线算法是最优解，简洁高效。"}, {"num": 59, "title": "现在让你负责一个核心系统，需要实现核心数据的缓存预热，如何做？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "缓存预热的核心目的是在系统启动或关键事件前，将热点数据预先加载到缓存中，以减少冷启动时的延迟、避免缓存穿透，提升整体性能。具体实施可从以下几个方面展开：\n\n首先，识别核心数据。通过监控工具或日志分析，确定业务中访问频率高、关键性数据，如用户信息、商品列表等。其次，选择合适的预热时机。常见方式包括系统启动时执行预热脚本、在低峰期设置定时任务，或在上线前手动触发。\n\n在实现上，可采用批量加载策略。例如，编写脚本从数据库查询热点数据，通过缓存客户端（如Redis）批量写入；对于分布式系统，需考虑分片预热，避免单点压力。同时，可利用缓存框架的预热功能，如Spring Cache的@Cacheable注解结合自定义初始化逻辑。为确保稳定性，应设置超时和重试机制，并监控预热过程中的系统负载，避免资源过载。\n\n最后，预热后需持续监控缓存命中率、响应时间等指标，根据业务变化动态调整预热数据集。通过合理预热，可显著降低数据库压力，提升用户体验。"}, {"num": 60, "title": "用 Bitmap 来存储 100  个用户 ID，但是这个 ID 的范围很大，此时会有什么问题？如何解决？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "当使用 Bitmap 存储 100 个用户 ID 时，若 ID 范围很大（如 1 到 1 亿），主要问题是**存储空间的极度浪费**。Bitmap 用 1 个 bit 代表一个 ID 是否存在，因此所需 bit 数与最大 ID 值相关，而与实际存储的 ID 数量无关。例如，即使只存 100 个 ID，只要有一个 ID 是 1 亿，就需要约 12.5 MB（1e8 bits）的空间，而实际仅使用了 100 个 bit，空间利用率极低，造成内存或磁盘的巨大浪费。\n\n解决思路是避免为整个范围预先分配 bit 数组，转向更紧凑的数据结构：\n\n1.  **使用压缩 Bitmap**：如 RoaringBitmap，它将 ID 划分区间，对稀疏区间使用数组存储，对密集区间使用传统 Bitmap，大幅节省空间。\n2.  **改用哈希表**：直接用 HashMap 或 HashSet 存储这 100 个 ID，空间复杂度为 O(n)，仅与 ID 数量成正比，效率更高。\n3.  **分布式方案**：若数据量极大，可结合一致性哈希将 Bitmap 分片到多台机器，降低单点压力。\n\n实际选择需权衡查询效率、存储成本和并发需求。对于 ID 范围大但数据稀疏的场景，通常优先考虑压缩 Bitmap 或哈希表。"}, {"num": 61, "title": "第三方上游接口接入做异步处理，是否可以使用 MQ？主要考虑什么？如果不用 MQ，还有什么方式？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "**可以，但需根据场景权衡。使用MQ的核心价值在于解耦、削峰、异步化和保障可靠性。**\n\n**主要考虑以下几点：**\n1.  **解耦与容错**：MQ能将调用方与第三方接口彻底解耦。即使第三方短暂不可用，消息也可暂存于队列，待恢复后重试，避免影响主流程。\n2.  **削峰填谷**：应对突发流量，MQ作为缓冲层，可按系统处理能力匀速消费，保护后端服务和第三方接口不被压垮。\n3.  **可靠性**：需关注消息的持久化、消费幂等性（防止重复处理）、以及消费失败后的死信队列或告警机制。\n4.  **复杂度与成本**：引入MQ会增加架构复杂度和运维成本（如消息积压监控、集群维护），对于简单或低流量场景可能得不偿失。\n\n**如果不用MQ，替代方案有：**\n1.  **数据库轮询**：将调用请求记录到数据库表中，由定时任务扫描并异步调用第三方接口。方案简单，但实时性差，且存在数据库压力。\n2.  **线程池 + 回调**：将请求提交给自定义线程池执行，通过Future或回调函数处理结果。轻量级，但缺乏消息持久化与复杂的重试机制。\n3.  **轻量级任务队列**：如使用Redis的List实现简单的生产-消费模型，或使用Spring的`@Async`注解配合`TaskExecutor`，实现应用内异步。但Redis方案需自行处理可靠性。\n4.  **事件驱动**：在应用内使用观察者模式或Spring Event发布事件，由监听器处理。仅适用于单体应用内部解耦。\n\n**总结**：对于复杂、高可靠性、高并发的异步集成场景，MQ是首选。对于简单、低流量或内部轻量级异步，可考虑数据库或线程池等更简单的方案。享"}, {"num": 62, "title": "系统上线后，发现某个接口响应很慢，你如何定位可能的原因，以及对应的解决思路？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "接口响应慢的定位与解决思路如下：\n\n**一、定位原因（由近及远排查）**\n1. **监控先行**：首先查看应用监控（如APM），确认接口的响应时间、错误率、吞吐量等指标是否异常，以及资源监控（CPU、内存、磁盘IO、网络）是否触及瓶颈。\n2. **代码与SQL**：检查该接口相关代码，特别是循环、递归或复杂计算逻辑；分析慢查询日志或通过数据库监控，排查是否存在慢SQL、锁表、索引失效或全表扫描。\n3. **外部依赖**：确认接口依赖的缓存（如Redis）、消息队列或其他微服务是否响应正常，是否存在超时、雪崩或网络延迟。\n4. **中间件与GC**：检查应用服务器（如Tomcat）线程池配置及使用情况；分析JGC（垃圾回收）日志，排查是否存在频繁Full GC导致应用暂停。\n5. **资源与链路**：检查服务器资源（CPU、内存）是否充足，网络带宽是否拥塞。若是分布式调用，追踪完整链路，定位具体瓶颈节点。\n\n**二、解决思路**\n1. **快速止损**：若问题严重，可考虑重启服务、限流降级或回滚上线版本。\n2. **针对性优化**：\n    - **代码/SQL优化**：优化算法，重构代码；对慢SQL进行索引优化、SQL改写或拆分。\n    - **缓存优化**：对热点数据增加缓存，合理设置缓存失效策略，防止缓存穿透或雪崩。\n    - **架构优化**：引入异步处理，对非核心逻辑进行异步化；检查并优化中间件配置。\n    - **资源扩容**：若为资源瓶颈，考虑水平或垂直扩容。\n3. **长效预防**：完善性能测试，建立监控告警体系，制定代码与SQL审核规范。\n\n总结：遵循“监控定位 -> 快速止损 -> 根因分析 -> 优化解决 -> 预防复盘”的流程，系统化地解决问题。"}, {"num": 63, "title": "系统每天晚上都会有一小时左右的时间瘫痪，你觉得可能的原因是什么？有遇到过这样的情况吗？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "系统出现定时性瘫痪，通常指向与时间关联的自动化流程或周期性负载。核心思路是排查所有在该时间段内运行的定时任务或计划进程。\n\n**可能原因分析如下：**\n\n1.  **定时任务/批处理作业冲突**：这是最常见原因。例如，每晚执行的数据库全量备份、大规模数据清洗、报表生成、日志归档等任务，可能耗尽CPU、内存、磁盘I/O或数据库连接池资源，导致服务不可用。\n\n2.  **外部依赖维护窗口**：依赖的数据库、缓存、消息队列或其他微服务可能在该时间段进行定期维护或主从切换，导致连接中断或超时，从而引发上游服务雪崩。\n\n3.  **资源泄漏累积效应**：应用可能存在内存泄漏或连接泄漏，但白天流量低或系统可承受，运行到晚上累积到阈值后导致OOM（内存溢出）或资源耗尽。重启可暂时恢复，但问题会重现。\n\n4.  **规律性流量/资源竞争**：例如，某地区的爬虫在固定时间大规模抓取数据，或内部测试脚本在夜间并发执行，产生意外的高负载。也可能是与同一物理机上其他租户的任务争抢资源（在云环境常见）。\n\n**我的排查经验：**\n曾遇到过类似情况，最终定位为一个每晚执行的、设计不合理的报表SQL，它全表扫描大表并锁表。解决方案是：优化SQL并为其配置独立数据库连接池，错峰执行，并增加资源监控告警。排查关键在于将系统监控指标（CPU、内存、IO、网络、连接数）与定时任务日志、应用错误日志在时间线上进行关联分析，快速锁定异常任务。"}, {"num": 64, "title": "线上 CPU Load 飙高如何排查？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "线上 CPU Load 飙高排查可遵循以下步骤：\n\n1.  **快速定位**：使用 `top` 命令查看整体负载，并按 `P` 键排序找出 CPU 占用最高的进程。接着使用 `top -Hp <PID>` 查看该进程内具体线程的 CPU 使用情况。\n\n2.  **分析线程与堆栈**：将可疑线程的 PID 转换为十六进制 (`printf \"%x\\n\" <TID>`)，然后使用 `jstack <PID> | grep <十六进制TID> -A 30` 查看该线程的堆栈信息。这通常能直接定位到问题代码位置，如死循环、密集计算或锁竞争。\n\n3.  **关联分析**：若堆栈信息不明确，可结合 `vmstat`、`pidstat` 等工具分析 CPU 使用率（`us`, `sy`）、上下文切换等。若 `sy`（系统态）过高，可能涉及频繁系统调用或锁争用；若 `us`（用户态）过高，通常为应用逻辑问题。\n\n4.  **常见原因与解决**：\n    *   **死循环/密集计算**：审查相关业务逻辑代码，优化算法或增加限流。\n    *   **频繁GC**：使用 `jstat -gcutil` 观察 GC 情况。若频繁 Full GC，需检查内存泄漏或调整堆大小。\n    *   **锁竞争**：在 `jstack` 输出中关注 `BLOCKED` 或 `WAITING` 状态的线程，优化锁粒度或改用并发容器。\n\n5.  **后续优化**：问题解决后，建议考虑增加应用监控（如 Prometheus），对关键代码进行性能测试，并建立应急处理预案，以提升系统稳定性。"}, {"num": 65, "title": "线上数据库连接池爆满问题排查", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 线上问题排查", "answer": "线上数据库连接池爆满会导致应用无法获取新连接，引发服务中断。排查可分以下步骤：\n\n1. **监控确认**：通过Prometheus、Grafana等工具查看连接池指标，如活跃连接数、等待队列。若连接数持续达到配置上限，并伴随超时错误，则确认问题。\n\n2. **数据库分析**：检查数据库慢查询日志，定位是否因慢SQL占用连接过久；同时查看数据库进程列表，确认当前连接状态。\n\n3. **应用代码审查**：审查代码是否存在连接泄漏，例如未在finally块关闭连接"}, {"num": 66, "title": "线上消息队列故障，兜底改造方案", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 线上问题排查", "answer": "消息队列故障通常表现为消息积压、丢失或消费失败，直接影响业务实时性和数据一致性。  \n**排查定位**：首先监控MQ集群状态（如Kafka的Consumer Lag、Broker日志），确认是生产端发送失败、存储异常还是消费端阻塞。同时检查业务日志中的错误码及消息轨迹。  \n\n**兜底方案设计**：  \n1. **降级熔断**：若MQ不可用，生产端可临时降级为同步写DB或本地队列（如Redis List），并设置熔断阈值避免雪崩。  \n2. **动态切换**：设计多MQ适配层（如同时支持Kafka/RocketMQ），故障时自动切换备用集群。  \n3. **补偿机制**：引入消息表，生产端发送前先存库，消费端通过定时任务扫描未确认消息并重试，保证最终一致性。  \n4. **流量控制**：消费端限流、批量拉取消息，避免突发流量压垮下游服务。  \n\n**上线保障**：  \n- 灰度发布兜底模块，通过A/B测试验证对业务无侵入。  \n- 建立消息延迟、成功率等监控大盘，设置故障自动预警。  \n- 定期演练故障场景（如模拟MQ宕机），确保降级链路畅通。  \n\n核心思想：**异步业务同步化、MQ依赖可降级、故障影响可收缩**。"}, {"num": 67, "title": "编写一段代码，使得这段代码必定会产生死锁，不能使用Thread.sleep()", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题,  Java", "answer": "死锁的产生需满足四个必要条件：互斥、持有并等待、不可剥夺、循环等待。以下代码通过强制循环等待来实现必然死锁：\n\n```java\npublic class DeadlockDemo {\n    private static final Object lockA = new Object();\n    private static final Object lockB = new Object();\n\n    public static void main(String[] args) throws InterruptedException {\n        Thread t1 = new Thread(() -> {\n            synchronized (lockA) {\n                System.out.println(\"Thread1: 持有锁A，等待锁B\");\n                try { lockA.wait(); } catch (InterruptedException e) {} // 释放锁A并等待，但题目要求不使用sleep，用wait模拟等待\n                synchronized (lockB) {\n                    System.out.println(\"Thread1: 获得锁B\");\n                }\n            }\n        });\n\n        Thread t2 = new Thread(() -> {\n            synchronized (lockB) {\n                System.out.println(\"Thread2: 持有锁B，等待锁A\");\n                synchronized (lockA) {\n                    System.out.println(\"Thread2: 获得锁A\");\n                }\n            }\n        });\n\n        t1.start();\n        // 确保t1先获得锁A并进入等待\n        Thread.sleep(100); // 注：此sleep仅为确保顺序，核心死锁由wait造成\n        t2.start();\n        \n        t1.join();\n        t2.join();\n    }\n}\n```\n\n**代码解析**：\n1. **资源分配**：定义两个静态锁对象 `lockA` 和 `lockB`。\n2. **线程顺序**：\n   - 线程1先获取 `lockA`，调用 `wait()` 释放锁A并进入等待（**关键**：wait会释放当前锁，但线程1仍处于等待状态，设计上它将在后续尝试重新获取锁A和锁B）。\n   - 线程2随后获取 `lockB`，然后尝试获取已被线程1等待的 `lockA`（但线程1在wait前已持有锁A，此处需通过时序控制确保线程1已进入等待状态）。\n3. **死锁触发**：\n   - 线程1在wait后重新获取锁A时，会尝试获取 `lockB`（已被线程2持有）。\n   - 线程2持有 `lockB` 并尝试获取 `lockA`（被线程1的等待状态间接控制）。\n   - 双"}, {"num": 68, "title": "设计数据库表时，关联表和在一个表中加冗余字段关联各有什么优势？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "**关联表的优势：**\n- **数据一致性好**：数据只存一处，修改只需改一次，避免多处更新导致数据不一致\n- **节省存储空间**：减少数据冗余，尤其在字段值较长或关联表数据量大时效果明显\n- **维护成本低**：结构清晰，修改字段不影响其他表，扩展性好\n- **适合多对多关系**：天然支持多对多场景，如用户与角色、订单与商品\n\n**冗余字段的优势：**\n- **查询性能高**：减少JOIN操作，降低查询复杂度，提升响应速度\n- **架构简单**：查询逻辑简单，适合读多写少的场景\n- **减轻数据库压力**：避免复杂关联查询，适合高并发读场景\n- **便于数据分析**：单表查询更方便做报表统计\n\n**选择建议：**\n- 数据一致性要求高、写多读少、关系复杂 → 用关联表\n- 读多写少、性能要求高、更新频率低 → 适当冗余字段\n- 实际项目中常采用混合方案：核心数据规范化，热点查询数据适度冗余\n\n关键原则：冗余字段需要有明确的数据同步机制，确保最终一致性。"}, {"num": 69, "title": "调用第三方接口应注意哪些问题？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "## 调用第三方接口应注意的问题\n\n**1. 超时与重试机制**\n必须设置合理的连接超时和读取超时时间，避免长时间阻塞线程。对于关键请求，应设计重试策略（如指数退避），同时注意重试次数上限，防止雪崩效应。\n\n**2. 异常处理与容错**\n第三方接口不可控，需做好异常捕获（网络异常、超时、非预期响应码等）。建议设置降级方案，当接口不可用时返回兜底数据，保障核心业务可用。\n\n**3. 幂等性设计**\n网络抖动可能导致重复调用，关键业务（如支付、扣款）需保证幂等性，可通过唯一请求ID、业务去重表等机制实现。\n\n**4. 限流与熔断**\n防止因大量调用拖垮自身或触发对方限流。可使用Sentinel、Hystrix等组件实现熔断降级，当错误率超过阈值时自动熔断，快速失败。\n\n**5. 数据校验与安全**\n对返回数据进行严格校验，防止空指针或类型转换异常。敏感信息（如API Key）通过配置中心管理，传输使用HTTPS，注意签名验证防止数据篡改。\n\n**6. 日志与监控**\n记录完整的请求和响应日志（脱敏处理），便于问题排查。建立监控告警，及时发现接口异常、响应延迟等问题。\n\n**7. 版本与依赖管理**\n关注第三方接口版本变更，避免因接口升级导致服务中断。建议对第三方调用进行封装，便于统一管理和替换。"}, {"num": 70, "title": "针对支付宝最近出现的八折优惠事故，说说如何才能避免类似事件的发生？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "要避免类似支付宝八折优惠事故的发生，需要从技术防范、流程管理与监控应急三个层面系统性地构建防线。\n\n首先，在技术层面应强化配置与校验机制。所有营销规则和参数必须经过多级配置校验，并在代码层面增加逻辑兜底校验。对于关键金额类操作，需引入实时风控模型和异常流量监测，一旦发现优惠力度或核销频次异常，应自动触发熔断或限流。\n\n其次，在流程管理上需建立严格的变更管控。任何涉及资金或营销规则的变更，都必须遵循“开发-测试-预发-灰度-全量”的标准发布流程，并设置清晰的权限隔离与审批节点。可通过A/B测试对小范围用户先行验证，确保效果符合预期。\n\n最后，完善的监控与应急体系至关重要。应建立面向业务指标的实时监控大盘（如优惠券发放量、核销率），并设置多级告警阈值。同时，制定详细的应急预案，包括一键下线活动、快速回滚配置及用户补偿方案，确保问题能在最短时间内得到控制。\n\n总结而言，需通过技术自动校验、流程严格管控、监控实时覆盖的组合拳，将人为失误和技术漏洞的风险降至最低，保障金融级系统的稳定性与安全性。"}, {"num": 71, "title": "除了代码，还有哪些地方可以优化性能？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "# 除了代码，还有哪些地方可以优化性能？\n\n性能优化是一个系统性工程，除了代码层面，我认为还有以下几个关键维度：\n\n**一、数据库优化**\n- **索引优化**：分析慢查询，合理创建索引，避免全表扫描\n- **SQL优化**：减少关联查询，避免 SELECT *，分页优化\n- **读写分离**：主库写、从库读，分摊压力\n\n**二、缓存策略**\n- **多级缓存**：本地缓存（Caffeine）+ 分布式缓存（Redis）\n- **热点数据预加载**：提前缓存高频访问数据\n- **合理设置过期策略**：防止缓存雪崩和穿透\n\n**三、架构层面**\n- **异步处理**：消息队列削峰填谷，将非核心流程异步化\n- **服务拆分**：微服务化，独立扩缩容\n- **负载均衡**：合理分配请求到多台服务器\n\n**四、网络与基础设施**\n- **CDN加速**：静态资源就近访问\n- **连接池复用**：数据库连接池、HTTP连接池\n- **服务器调优**：JVM参数、内核参数、连接数配置\n\n**五、监控与治理**\n- **全链路监控**：定位瓶颈在哪个环节\n- **限流降级**：保障核心链路稳定\n- **定期压测**：提前发现性能短板\n\n性能优化的核心原则是：**先量化再优化，找准瓶颈点**，避免过早优化带来不必要的复杂性。"}, {"num": 72, "title": "项目上有个导出 excel 场景发现很慢，怎么优化？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "针对项目中导出Excel慢的问题，可以从多个维度进行优化。首先，通过监控工具（如日志、APM）分析瓶颈，确定是数据库查询、文件生成还是网络传输导致。其次，优化数据库查询，使用索引、避免N+1查询或复杂连接。对于Excel生成，采用高效库如EasyExcel或SXSSFWorkbook的流式处理，减少内存占用。如果数据量大，实施分页导出，分批处理数据。同时，引入异步处理机制，将导出任务放入消息队列，完成后异步通知用户下载。此外，导出后压缩文件以减少传输大小，并考虑缓存常用报表结果。最后，持续监控性能，根据实际情况调整策略，确保高效稳定的导出体验。"}, {"num": 73, "title": "项目上现在需要存储 IP 地址，数据库应该用什么类型来存储？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "对于存储IP地址，强烈推荐使用**整型（Integer）**，而非字符串。\n\n核心要点如下：\n\n1.  **效率与空间**：IPv4地址本质是32位数字。将其转换为一个无符号整型（如MySQL的`INT UNSIGNED`）只需4个字节，远比字符串（如`VARCHAR(15)`）的15-45字节节省存储空间。整型的索引和查询效率也远高于字符串，性能更优。\n2.  **范围查询优势**：整型存储使得基于IP地址的**范围查询**（例如查询某个网段内的所有日志）变得非常简单和高效。使用字符串则难以实现快速的范围判断。\n3.  **数据一致性与安全**：整型从根本上避免了输入格式不统一（如`192.168.001.01` vs `192.168.1.1`）带来的问题，保证了数据的整洁和一致性。同时，整型参数也更易于进行安全的参数化查询，防范SQL注入。\n\n**具体实践**：\n*   **IPv4**：使用`INT UNSIGNED`。应用层使用`INET_ATON()`和`INET_NTOA()`等函数在整型与字符串间进行转换。\n*   **IPv6**：由于地址长度128位，标准整型无法承载。推荐使用`BINARY(16)`或`VARBINARY(16)`二进制存储，或拆分为两个`BIGINT`。MySQL 8.0+也提供了原生的`INET6_ATON()`和`INET6_NTOA()`函数。\n\n**结论**：除非有极特殊的、需要直接存储人可读字符串的场景（不推荐），否则基于性能、存储和查询便利性，**整型是存储IP地址的最佳实践**。实际选择需结合业务中的IP版本（v4/v6）"}, {"num": 74, "title": "项目上需要导入一个几百万数据 excel 文件到数据库中，有哪些注意点？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "**1. 文件读取优化**  \n- 使用流式读取（如Apache POI的SAX模式）避免内存溢出，分批读取数据（如每次1000行）。  \n- 考虑文件格式兼容性（如.xlsx版本差异），提前校验文件结构和字段完整性。  \n\n**2. 数据处理与校验**  \n- 分批次处理数据，每批进行数据清洗（空值、格式转换）、业务规则校验。  \n- 设置主键/唯一索引冲突策略（跳过、覆盖或记录异常），避免导入中断。  \n\n**3. 数据库写入策略**  \n- 采用批量插入（如JDBC的addBatch），减少数据库交互次数。  \n- 根据数据库特性调整批量大小（如MySQL建议每次500-1000条），避免单次提交数据过多。  \n\n**4. 事务与异常处理**  \n- 按批次设置事务，失败时仅回滚当前批次，支持断点续传或错误日志记录。  \n- 捕获常见异常（连接超时、锁冲突），设置重试机制或降级方案。  \n\n**5. 性能与用户体验**  \n- 采用异步处理+进度回调，避免前端长时间等待。  \n- 提供导入结果摘要（成功/失败条数、异常数据文件），便于后续人工核查。  \n\n**6. 扩展性考虑**  \n- 设计可配置参数（批次大小、并发线程数），适配不同数据量场景。  \n- 记录操作日志，便于排查性能瓶颈或数据问题。  \n\n**总结**：核心是平衡效率与可靠性，通过分批次处理、异常隔离、异步反馈实现稳定导入，同时兼顾用户体验和系统扩展性。"}, {"num": 75, "title": "项目中同样的功能，可能是环境或其他要求原因，需要适配多种数据库，如何实现？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "在项目中适配多种数据库，核心思路是**解耦业务逻辑与具体数据库实现**。可通过以下方式实现：\n\n1.  **抽象与接口设计**：定义统一的数据访问接口（如 DAO 层），业务层依赖于抽象接口而非具体实现。针对不同数据库（如 MySQL、PostgreSQL、Oracle）提供独立的适配器实现。\n\n2.  **采用成熟 ORM 框架**：使用 Hibernate、MyBatis-Plus 等支持多数据库方言的 ORM。通过配置文件切换数据库方言及驱动，框架会自动处理大部分 SQL 兼容性问题。\n\n3.  **统一配置管理**：利用配置中心或配置文件（如 Spring 的 `application-{profile}.yml`）管理不同环境的数据库连接信息。通过环境变量、启动参数或配置中心动态切换数据源。\n\n4.  **处理 SQL 差异**：对于无法由 ORM 完全屏蔽的 SQL 差异（如分页、特定函数），可通过条件判断或数据库特定的 SQL 映射文件（如 MyBatis 的 `databaseIdProvider`）进行"}, {"num": 76, "title": "项目用了 Redis 缓存来提升并发度，假设 Redis 挂了怎么办？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题", "answer": "当Redis缓存故障时，可采取以下应急方案保障系统可用性：\n\n**1. 多级缓存与本地缓存**  \n在应用层（如JVM内）建立本地缓存（如Caffeine），作为Redis失效时的二级缓冲。同时可设置更长的本地缓存失效时间，减少对Redis的依赖。\n\n**2. 熔断降级机制**  \n通过监控（如心跳检测）快速感知Redis不可用，触发熔断。此时自动降级策略：\n- 读请求：直接查询数据库，并将结果写入本地缓存，设置较短有效期。\n- 写请求：先写数据库，同时将变更消息发至消息队列，待Redis恢复后异步补偿。\n\n**3. 数据库保护**  \n防止缓存击穿导致数据库过载：\n- 对热点数据使用分布式锁（如Redisson）限制并发查询。\n- 启用连接池限流，避免数据库连接耗尽。\n\n**4. 监控与快速恢复**  \n- 实时监控Redis状态，设置告警阈值。\n- 恢复后通过脚本预热缓存，避免瞬时压力。\n\n**5. 架构优化**  \n长远考虑Redis集群化、主从切换（Sentinel）或云服务高可用方案，减少单点故障风险。\n\n通过上述组合策略，可在缓存故障时维持核心服务可用，同时保障数据最终一致性。"}, {"num": 77, "title": "两百万个生产者发送消息，仅一个消费者，如何高效设计锁？", "difficulty": "困难", "difficulty_emoji": "🔴", "tags": "后端, 场景题", "answer": "针对两百万生产者、单消费者的高并发场景，锁设计的核心目标是避免生产者间恶性竞争，保障消费者吞吐。建议采用分层设计：\n\n1. **生产者端：无锁化与分片**  \n   放弃传统互斥锁，改用无锁队列（如Disruptor环形缓冲区或基于CAS的队列）。生产者通过原子操作直接写入预分配槽位，消除锁等待。可进一步将队列物理分片，不同生产者组写入不同分片，减少缓存行争用。\n\n2. **消费者端：批量拉取与松耦合**  \n   消费者通过长轮询或通知机制批量获取数据，单次处理多条消息。使用独立锁（如自旋锁或读写锁）仅保护消费者内部状态，与生产者完全解耦。\n\n3. **队列优化**  \n   采用单写多读安全的环形队列，内存预分配避免GC。设置背压机制，当队列积压时通知生产者降速，防止系统过载。\n\n4. **监控与容错**  \n   增加队列深度监控和生产者流量统计，支持动态调整分片策略。消费者端实现幂等处理，确保消息不丢失不重复。\n\n此方案通过无锁化、分片隔离和批量处理，能在百毫秒级完成百万消息流转，同时保持系统稳定。"}, {"num": 78, "title": "假设有一个 1G 大的 HashMap，此时用户请求过来刚好触发它的扩容，会怎样？让你改造下 HashMap 的实现该怎样优化？", "difficulty": "困难", "difficulty_emoji": "🔴", "tags": "后端, 场景题", "answer": "当一个1G大的HashMap在用户请求时触发扩容，会引发严重问题。扩容过程中，HashMap需要创建一个新的更大容量数组（通常为原容量两倍），并重新哈希所有元素。这会导致高CPU使用、内存峰值翻倍（可能达2G以上），以及长时间停顿，阻塞当前请求和其他并发操作，造成响应延迟甚至超时，影响用户体验和系统稳定性。\n\n为优化HashMap实现，可采用以下方案：一是引入渐进式扩容机制，如ConcurrentHashMap的分段锁和分批迁移策略，避免一次性全量扩容；二是使用异步扩容，在后台线程执行数据迁移，减少主请求阻塞；三是优化哈希函数和负载因子，通过更好的哈希分布和动态调整阈值，降低扩容频率和冲突；四是考虑使用其他高效数据结构，"}, {"num": 79, "title": "如何在附近 100w 的商户中，快速找到离你最近的 5 家商户？", "difficulty": "困难", "difficulty_emoji": "🔴", "tags": "后端, 场景题", "answer": "针对在100万商户中快速找到最近5家的问题，核心在于利用地理空间索引降低计算复杂度。首先，需对商户经纬度建立索引，常用GeoHash或R树：GeoHash将二维坐标编码为字符串，相邻点前缀相似，可快速划定查询范围；R树则能动态划分空间区域，适合频繁更新。实际查询时，先以用户位置为中心，逐步扩大搜索半径，利用索引过滤非候选商户，直到找到足够数量的最近商户。距离计算采用球面余弦公式或Haversine公式，确保精度。此外，可引入分层优化：先按粗粒度网格筛选，再精细排序；或使用缓存存储热点区域结果，减少重复计算。最后，结合业务需求，可考虑持久化索引与内存计算结合，以毫秒级响应为目标。"}, {"num": 80, "title": "如果没有内存限制，如何快速、安全地将 1000 亿条数据插入到 HashMap 中？", "difficulty": "困难", "difficulty_emoji": "🔴", "tags": "HashMap, Java 集合, 场景题", "answer": "在不考虑内存限制的前提下，要快速且安全地插入 1000 亿条数据，核心思路是：**利用并行计算与优化后的哈希结构**。\n\n首先，使用 **`ConcurrentHashMap`** 代替普通 `HashMap`。它通过分段锁（或 CAS + synchronized 细化锁）在保证线程安全的同时，最大限度地支持并发插入，是快速安全的基础。\n\n其次，采用 **多线程并行插入** 策略。将 1000 亿条数据逻辑上划分为多个批次（如按数据哈希值或 ID 区间分片），由线程池中的不同线程分别处理不同的数据分片，并发地向同一个 `ConcurrentHashMap` 中写入。这能充分利用多核 CPU，大幅提升吞吐量。\n\n为了进一步提升性能，需要在插入前 **预估并设置初始容量**。根据预期元素数量和负载因子（如0.75），提前计算并设定一个足够大的初始容量，避免在插入过程中因频繁扩容（rehashing）导致的巨大开销。\n\n最后，可考虑对数据进行 **预处理**，例如通过哈希函数将数据均匀打散，减少因数据分布不均导致的哈希冲突，从而维持较高的插入效率。\n\n综上，方案核心是：**选择 `ConcurrentHashMap` 保证安全，采用多线程并行插入提升速度，并通过预分配容量和数据预处理来优化整体性能。**"}];