const QUESTIONS = [{"num": 1, "title": "API 网关如何实现接口版本管理？如果要同时支持 v1、v2、v3 多个版本的接口，你会怎么设计？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 系统设计, 场景题, API网关", "answer": "API 网关实现接口版本管理主要有三种常见方式：\n\n**1. 版本标识传递方式**\n- **URL路径**：在路径中包含版本号，如 `/api/v1/users`。最直观，易于理解和路由。\n- **请求头**：在自定义Header（如 `X-API-Version`）或标准头（如 `Accept`）中指定版本。URL更简洁，但对客户端不够透明。\n- **查询参数**：使用查询参数，如 `/api/users?version=1`。灵活，但可能污染参数。\n\n**2. 多版本共存设计**\n- **路由分发**：网关根据版本标识，将请求精确路由到对应版本的后端服务实例或不同路径的服务。\n- **流量控制**：可基于灰度规则，逐步将v1流量切换到v2，并实现新旧版本并行。\n- **版本降级与下线**：设置版本生命周期，通过配置管理平滑下线旧版本，对已废弃版本进行重定向或返回提示。\n\n**3. 设计要点**\n- 核心是**路由与解耦**：网关负责解析版本，后端服务专注于业务逻辑。\n- 支持**多版本并行**，需确保各版本后端服务独立部署或兼容。\n- 版本策略应清晰，并考虑文档、监控和客户端迁移引导。\n\n总之，通过网关集中管理版本路由，结合清晰的版本策略和流量控制，能优雅地支持多版本共存与平滑迭代。"}, {"num": 2, "title": "HashMap 是不是线程安全的？如果让你来实现一个线程安全的 HashMap 你要怎么设计？如果不用加锁你要怎么设计？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 系统设计, 场景题", "answer": "HashMap 本身不是线程安全的，多线程并发写入可能导致数据覆盖、死循环（JDK7 链表头插法）或数据丢失。\n\n**如果使用锁实现线程安全：** 最直接的方式是采用全局锁，例如用 `synchronized` 或 `ReentrantLock` 包裹所有公开方法（如 `put`、`get`）。但这样锁的粒度太大，高并发时会成为性能瓶颈。\n\n**如果不用锁（或优化锁）设计：** 我会参考 `ConcurrentHashMap` 的设计，核心思路是**减小锁粒度**和**利用无锁技术**。\n\n1.  **分段锁/锁分段：** 将数据数组分成多个段（Segment），每个段拥有独立的锁。线程操作不同段的数据可以并行，只在段内加锁。\n2.  **CAS + 细粒度锁：** 针对单个桶（数组节点）操作，首先尝试使用 CAS（Compare-And-Swap）无锁写入。如果失败（如发生哈希冲突或扩容），再退化为对当前桶或更小范围加锁。\n3.  **数据结构优化：** 采用数组+链表+红黑树（如Java8的ConcurrentHashMap）。链表过长时转为红黑树，降低冲突时间复杂度，同时树节点的锁操作比链表更细粒度。\n4.  **辅助设计：** 使用 `volatile` 保证可见性；扩容时采用多线程协作分摊迁移数据压力；读操作在符合内存模型的前提下，大部分情况无需加锁。\n\n这样设计能在保证线程安全的同时，最大限度提升并发读写性能。"}, {"num": 3, "title": "SSO 系统中，如果认证中心挂了怎么办？如何保证已登录用户不受影响？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 系统设计, 场景题, 单点登录", "answer": "当认证中心（SSO Server）宕机时，系统需通过“去中心化验证”和“状态缓存”保证已登录用户（访问业务系统）不受影响。核心思路是**将认证状态的验证能力从集中式认证中心下放到各业务服务**。具体设计如下：\n\n1.  **令牌本地化验证（核心）**：\n    用户首次登录后，认证中心颁发一个**加密、自包含的令牌（如JWT）**，其中包含用户ID、权限和过期时间。该令牌由业务服务在本地通过**预置的公钥或共享密钥**独立验证和解密。只要令牌未过期且签名有效，业务系统即可直接信任该用户身份，**无需每次请求都回调认证中心**。这实现了认证中心的“无状态验证”。\n\n2.  **合理的令牌生命周期管理**：\n    *   **有效期控制**：设置较短的访问令牌（Access Token）有效期（如15-30分钟）和较长的刷新令牌（Refresh Token）有效期。短期令牌降低泄露风险。\n    *   **主动刷新**：业务服务可在用户活动时，使用有效的旧令牌向认证中心**静默请求新令牌**，实现无缝续期。此操作可设置降级策略，失败时允许当前会话继续。\n\n3.  **认证中心的高可用架构**：\n    *   **集群化与负载均衡**：认证中心本身应以集群模式部署，前端通过负载均衡器（如Nginx）虚拟为单一地址，避免单点故障。\n    *   **多级缓存与降级**：在认证中心内部或网关层缓存热点用户的会话状态。当认证中心完全不可用时，业务服务可依赖本地缓存的公钥和令牌验证逻辑，继续提供服务，直至所有令牌过期。\n\n**总结**：通过JWT等无状态令牌实现本地验证，确保了在网络层面对认证中心的弱依赖；通过令牌刷新机制和认证中心自身的集群部署，保障了系统的长期可用性和安全性。最终，即使认证中心暂时不可用，已登录用户凭借本地有效的令牌，业务访问完全不受影响。"}, {"num": 4, "title": "什么是死信队列？在什么场景下会用到？如何设计死信队列的处理机制？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 系统设计, 场景题, 消息队列", "answer": "死信队列是消息中间件中用于存储处理失败消息的特殊队列。当消息被消费者多次重试后仍无法成功处理时，该消息将被路由到死信队列中，从而避免其阻塞正常消息的消费流程。\n\n在以下场景中会使用死信队列：\n1. 消息处理反复失败，如业务逻辑异常、依赖服务不可用；\n2. 消息格式错误或内容不符合预期，导致解析失败；\n3. 消息超过最大重试次数或处理超时；\n4. 消息需要人工介入排查或修复。\n\n设计死信队列的处理机制时，应考虑：\n首先，配置合理的死信策略，如设置最大重试次数、重试间隔和死信路由规则。\n其次，为死信队列设计监控告警机制，及时发现异常堆积。\n最后，建立一套标准化的处理流程：对死信消息进行持久化存储和记录；提供人工管理界面供排查、重发或删除；并能将修复后的消息重新投递至原队列重新处理。关键是要确保整个链路可监控、可干预、可恢复。"}, {"num": 5, "title": "分布式缓存中，如何实现一致性 Hash 算法？相比普通 Hash，它解决了什么问题？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 系统设计, 场景题, 一致性Hash, 分布式缓存, 负载均衡", "answer": "一致性Hash算法通过构建一个虚拟的环形哈希空间（0到2^32-1）来实现。首先，将缓存服务器节点通过哈希函数映射到环上的一个位置。然后，当需要存储或访问数据时，对数据的Key进行哈希，得到其在环上的位置，并沿环顺时针查找，找到的第一个服务器节点即为该数据的目标节点。\n\n为了解决节点较少时可能产生的数据分布不均问题，算法引入了**虚拟节点**。即每个物理节点对应环上的多个虚拟位置，从而分散负载，使数据分布更均匀。\n\n相比普通Hash（如直接用Key的哈希值对服务器数量取模），一致性Hash主要解决了**节点动态增减时的数据迁移问题**。在普通Hash中，当服务器数量变化时，几乎所有数据的映射关系都会失效，需要全量重新分配，成本极高。而在一致性Hash中，当节点增减时，仅影响该节点在环上逆时针方向相邻区间内的数据，其他数据的映射关系保持不变，实现了增量迁移，大大提升了系统的可扩展性和稳定性。"}, {"num": 6, "title": "加密后的数据怎么支持模糊搜索？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 系统设计, 场景题", "answer": "加密后的数据支持模糊搜索是一个典型的系统设计挑战，核心在于平衡数据安全性与搜索功能。直接对密文进行模糊匹配（如部分关键词、相似性搜索）不可行，因为加密破坏了原始数据的可读性。以下是常见解决方案：\n\n首先，采用可搜索加密（Searchable Encryption）技术，这是一种允许在加密数据上执行搜索的密码学方案。例如，使用对称或非对称加密生成加密索引，搜索时通过陷门（trapdoor）在密文索引上匹配，返回加密结果后解密。这能支持精确或简单模糊搜索，如通配符查询。\n\n其次，结合模糊搜索算法预处理。在加密前，对原始数据生成模糊索引，如基于n-gram、编辑距离或拼音相似度的哈希值，并将这些索引加密存储。搜索时，对查询词进行同样模糊处理后加密，再与加密索引匹配。例如，中文模糊搜索可先提取拼音或笔画特征，建立加密特征库。\n\n此外，考虑同态加密或安全多方计算，允许在密文上直接执行计算（如相似度评估），但计算开销大，适用于小数据或高安全场景。\n\n实施时需注意：选择加密算法要兼顾安全与效率（如AES结合哈希）；索引设计需避免信息泄露，防止通过搜索模式推断数据内容；系统可分层，将高频模糊搜索数据部分解密或使用代理重加密优化性能。\n\n总之，通过可搜索加密与预处理索引的"}, {"num": 7, "title": "即时通讯项目中怎么实现历史消息的下拉分页加载？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 系统设计, 场景题", "answer": "在即时通讯系统中实现历史消息的下拉分页加载，核心在于高效地从数据库中检索并返回有序的消息序列。具体设计如下：\n\n首先，**接口设计**。客户端每次请求应传入三个关键参数：`limit`（每页数量）、`timestamp`（上一次最后一条消息的时间戳）和 `direction`（查询方向，如向前）。服务端根据 `timestamp` 和方向，从数据库中查询在此时间戳之前或之后的 `limit` 条消息，并返回新的消息列表及更新后的最新时间戳，形成游标式分页。\n\n其次，**数据库优化**。必须为消息表建立复合索引，例如 `(conversation_id, create_time, id)`。`conversation_id` 用于定位会话，`create_time` 用于快速时间范围排序，`id` 作为唯一键保障排序稳定性。查询时使用 `WHERE conversation_id = ? AND create_time < ?` 配合 `ORDER BY create_time DESC, id DESC LIMIT ?` 的语句，充分利用索引避免全表扫描。\n\n最后，**缓存与性能**。对于热点会话，可将最近的消息缓存在 Redis 等内存数据库中，分页查询优先命中缓存。同时，需考虑数据一致性，在消息入库时更新缓存。对于极端场景（如用户快速连续下拉），需在服务端做限流和并发控制，避免数据库压力过大。整个方案需保证**时效性**（低延迟）和**稳定性**（高可用），并设计降级策略，例如缓存失效时直接查询数据库。"}, {"num": 8, "title": "商家想要知道自己店铺卖的最好的 top 50 商品，如何实现这个功能？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 系统设计, 场景题", "answer": "对于“查询店铺内销量最高的50件商品”这一需求，核心在于实现一个高效的“Top K”排序计算。实现方案需兼顾数据实时性与查询性能，主要思路如下：\n\n首先，将订单数据作为原始数据源，存储在数据库（如MySQL）中，确保数据的持久化和一致性。\n\n其次，引入缓存层，使用Redis的有序集合（Sorted Set）来实时维护商品销量排行。将商品ID作为成员（member），累计销量作为分数（score）。每当一笔订单支付成功，通过消息队列（如Kafka）发送消息，由消费服务原子性地执行`ZINCRBY`指令，对应商品的分数增加其销量。\n\n最后，查询时，接口直接调用Redis的`ZREVRANGE`命令，即可在毫秒级内获得销量前50的商品ID及分数。如需商品详情，可批量查询数据库或进一步使用缓存。\n\n此架构的优点在于：1）查询性能极高；2）通过异步消息处理，降低了计算压力，实现准实时更新。缺点是引入了中间件，增加了系统复杂度。为保证最终一致性，可定期将Redis数据同步回数据库进行校对。若商品不足50个，则按实际数量返回。"}, {"num": 9, "title": "如何实现一个单点登录（SSO）系统？如果有多个子系统，用户只需登录一次即可访问所有系统", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 系统设计, 场景题, 单点登录", "answer": "实现一个单点登录（SSO）系统的核心是**集中化认证**与**分布式会话管理**，目标是让用户通过一次登录，即可访问多个互信的子系统。其关键设计如下：\n\n1.  **核心架构**：设立一个独立的**中央认证服务**（CAS）。该服务负责统一的身份验证和会话管理。\n2.  **认证流程**（以CAS协议为例）：\n    *   用户访问子系统A，若未登录，子系统A将其**重定向**至中央认证服务，URL中携带子系统A的标识。\n    *   用户在认证服务完成登录，认证服务创建全局会话，并生成一个**临时票据**。\n    *   认证服务将用户**重定向**回子系统A，URL中携带该票据。\n    *   子系统A用该票据向认证服务发起**后端验证**。验证通过后，认证服务返回用户身份信息。\n    *   子系统A为用户创建本地会话，用户即被视为已登录。\n3.  **跨系统访问**：当用户访问子系统B时，流程类似。由于在认证服务的全局会话已存在，用户通常无需再次输入密码，认证服务会直接签发票据给子系统B。\n4.  **技术实现**：\n    *   **JWT（JSON Web Token）**：认证服务可将用户信息编码为JWT，作为票据或Token。子系统通过验证签名来确认身份，实现了**无状态**验证，易于横向扩展。\n    *   **OAuth 2.0**：适用于为第三方应用授权访问资源的场景，其“授权码”模式也常被用于构建SSO。\n5.  **关键点**：需统一用户信息源（如LDAP），保障票据传输的安全性（HTTPS），并妥善管理中央服务的会话与"}, {"num": 10, "title": "如何实现一个滑动验证码功能？如何防止被机器识别破解？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 系统设计, 场景题, 安全, 人机识别, 验证码", "answer": "实现滑动验证码需前端交互与后端验证协同。前端生成包含滑块和背景的图片，通过Canvas或CSS定位实现拼图效果；用户拖动滑块后，前端采集滑动轨迹（包括坐标、时间戳、速度、加速度等）和最终位置，加密后提交给后端。\n\n后端验证是核心：首先比对滑动位置是否与预设的正确缺口坐标匹配（允许合理误差）；其次分析轨迹行为，机器操作通常匀速直线，人类则有自然抖动和速度变化。可引入轨迹行为模型（如贝叶斯分类器）进行二次验证。\n\n为防机器破解，可采用多重策略：\n1. **动态难度**：随机生成缺口形状、背景复杂度，甚至采用扭曲拼图。\n2. **行为深度分析**：监测鼠标移动路径、浏览器环境（Canvas指纹、WebGL信息）、操作时长分布，通过机器学习模型识别异常模式。\n3. **服务端限流与风控**：对IP/设备设置验证频率阈值，结合黑名单机制。\n4. **多因素融合**：将滑动验证与设备指纹、IP信誉库等关联，形成风险评分。\n5. **持续迭代**：定期更新算法和UI组件，对抗自动化工具的模板匹配。\n\n总结：滑动验证码是“人机交互”问题，需结合前端体验设计、后端算法分析及动态风控，形成多层防御体系。"}, {"num": 11, "title": "如何设计一个接口签名验证机制？防止参数被篡改和重放攻击？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 系统设计, 场景题, 安全", "answer": "设计一个安全的接口签名验证机制，核心在于**防篡改**与**防重放**，可通过以下步骤实现：\n\n**1. 防篡改：签名生成与验证**\n客户端和服务器共享一个安全密钥（Secret Key）。签名生成流程为：将业务参数（如`userId`, `amount`）按字段名ASCII码排序后，拼接为字符串，并附加一个关键的动态因子——**时间戳（timestamp）** 和一个**随机数（nonce）**。然后，使用如**HMAC-SHA256**算法，以Secret Key为密钥，对该字符串进行加密，生成签名（signature）。\n客户端将原始参数、时间戳、随机数及签名一并发送。服务端采用相同算法和密钥重新计算签名，并与客户端签名比对。若一致，说明参数在传输中未被篡改。\n\n**2. 防重放攻击：时效性与唯一性校验**\n- **时间戳校验**：服务端验证请求中的时间戳是否在允许的时间窗口内（如前后5分钟）。过期的请求直接拒绝，从而限制攻击者利用已窃取请求的时间。\n- **随机数（Nonce）校验**：服务端在接收到请求后，检查该Nonce是否已被使用过（例如存入Redis并设置过期时间）。若已存在，则判定为重放攻击并拒绝。这确保了每个合法请求只能被服务端处理一次。\n\n**总结**：该机制通过**动态签名**保证数据完整性，通过**时间戳+Nonce**组合拳确保请求的时效性与唯一性，二者结合能有效防御参数篡改和重放攻击。密钥的安全存储与传输（如使用HTTPS）是此机制生效的基础前提。"}, {"num": 12, "title": "如何设计一个高可用的数据同步系统？需要考虑哪些容错机制？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题, 系统设计题", "answer": "设计高可用数据同步系统的核心目标是保障数据最终一致性与服务连续性，主要考虑以下方面：\n\n**1. 同步模式选择**  \n采用「双写异步+最终一致性」模型，写入时同时向主库与同步队列写入，通过异步任务队列保障吞吐量。关键业务可升级为同步双写，但需权衡性能。\n\n**2. 容错机制设计**  \n- **故障检测**：使用心跳机制监控节点健康状态，超过阈值自动标记为不可用。  \n- **重试与幂等**：同步失败后按指数退避策略重试，同步操作设计为幂等性（如通过唯一事件ID去重）。  \n- **数据校验**：定期对账（如每日校验主从表数据MD5），发现差异触发补偿同步。  \n- **熔断降级**：同步队列积压时启动熔断，暂停非核心同步任务，核心链路降级为缓存临时替代。\n\n**3. 架构高可用保障**  \n- 主节点故障时，通过选主算法（如Raft）自动切换至从节点。  \n- 同步服务无状态部署，依赖Redis等分布式锁管理同步任务分配。  \n- 引入消息队列（如Kafka）作为缓冲层，解耦生产与消费，应对流量峰值。\n\n**4. 数据一致性保障**  \n- 采用版本号或时间戳解决冲突，以最新数据为准或人工介入策略。  \n- 关键操作记录事务日志，支持断点续传与数据回溯。\n\n**总结**：系统需通过冗余设计、自动化故障转移、异步解耦及持续校验，在高吞吐下实现数据可靠同步，平衡一致性与可用性需求。"}, {"num": 13, "title": "如何防止接口被恶意刷量？除了限流，还有哪些防护手段？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 系统设计, 场景题, 安全", "answer": "防止接口恶意刷量需构建多层次防御体系，限流仅是基础措施，还应包括以下关键手段：\n\n1. **身份认证与权限控制**：强制要求API Key、Token等凭证访问，并对不同用户分配差异化权限，从源头限制未授权访问。\n\n2. **行为特征分析**：通过设备指纹、User-Agent、请求频率异常等维度，结合AI模型实时识别机器人行为，对可疑请求进行拦截或二次验证。\n\n3. **参数合法性校验**：严格校验请求参数的类型、范围、业务逻辑合理性，防止脚本伪造数据（如恶意遍历ID、注入攻击）。\n\n4. **动态风控策略**：基于业务场景设置动态规则，例如同一IP/用户短时间高频查询时触发验证码、短信确认或临时禁封。\n\n5. **数据层面防护**：对敏感接口结果进行差异化缓存（如电商库存延迟显示），避免数据被批量爬取；关键操作需记录日志并关联业务上下文审计。\n\n6. **基础设施协同**：结合WAF（Web应用防火墙）过滤恶意流量，使用CDN隐藏源站IP，通过服务网格实现熔断降级保护核心业务。\n\n总结而言，有效防护需融合身份核验、行为监测、业务规则与基础设施能力，形成“事前预防-事中拦截-事后溯源”的闭环机制。"}, {"num": 14, "title": "如果让你实现一个分布式事务的补偿机制，应该注意哪些问题？补偿失败了怎么办？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 系统设计, 场景题, 分布式事务", "answer": "实现分布式事务补偿机制需关注以下核心问题：\n\n**一、机制设计要点：**\n1.  **状态记录**：为每个事务创建唯一事务ID，并在各参与方记录操作前后的状态快照或变更日志，为补偿提供数据基准。\n2.  **幂等设计**：补偿操作必须是幂等的。无论因网络重试或人为触发，多次执行应产生相同结果，避免造成数据二次错误。\n3.  **逻辑完备**：补偿操作需严格覆盖正向操作的所有副作用。例如，若正向操作创建了订单并扣减了库存，补偿则需取消订单并**等量**归还库存。\n4.  **时序与依赖**：补偿执行顺序需与正向操作相反，以处理数据依赖。例如，先创建的记录可能后被补偿删除，需考虑中间状态查询的容错。\n\n**二、补偿失败处理：**\n1.  **自动重试**：对于网络抖动、服务临时不可用等瞬时故障，应采用带退避策略的自动重试机制。\n2.  **告警与人工介入**：设定重试次数上限，超过后触发高级别告警。系统需提供管理界面，允许运维人员查看失败事务详情，并支持手动触发补偿或进行数据订正。\n3.  **最终一致性保障**：若自动与手动均无法解决，应将该事务挂起并进入“数据不一致”队列，由线下人工核对流程或定时巡检任务进行最终修复，确保系统具备“自愈”能力。\n\n**核心思想**：补偿机制是分布式系统的“安全网”，其设计原则是**通过严密的业务逻辑来保障数据最终一致性，而非强求实时ACID**。关键在于对业务规则的深刻理解与防御性编程。"}, {"num": 15, "title": "如果让你统计每个接口每分钟调用次数怎么统计？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 系统设计, 场景题", "answer": "统计接口每分钟调用次数是一个典型的**实时监控与聚合**问题，核心在于高效的数据采集、存储与计算。我可以提供一个清晰的实现思路：\n\n**1. 数据采集层：**\n*   **方案A（推荐）：** 在API网关或统一拦截器中，为每次请求记录一条结构化日志（如：接口ID、请求时间戳），并直接发送到消息队列（如Kafka）。\n*   **方案B：** 在每个接口业务代码中埋点，异步上报数据到日志服务或时序数据库。\n\n**2. 数据存储与计算层：**\n*   **流处理方案（适合实时要求高）：** 使用Flink或Spark Streaming消费消息队列中的数据流。通过设置**1分钟的滚动窗口**，对窗口内按接口ID分组的记录进行计数，结果可实时输出到Redis或时序数据库（如InfluxDB）。\n*   **离线/准实时方案（适合更复杂的分析）：** 将日志数据导入数据仓库（如ClickHouse、Elasticsearch）。利用其强大的聚合查询能力，编写类似`SELECT interface_id, count(*) FROM logs GROUP BY interface_id, toStartMinute(event_time)`的语句，通过定时任务（每分钟）计算并存储结果。\n\n**3. 查询与展示：**\n*   将计算结果（如 `{interface_id: /api/user, count: 1520, time: \"2023-10-01 12:05\"}`）存储在Redis或数据库中。\n*   提供API或直接对接监控面板（如Grafana），按时间范围查询并展示趋势图。\n\n**技术选型关键点在于：**\n*   **实时性要求**：高则选流处理（Flink+Kafka），低则选定时批处理（数据库聚合）。\n*   **数据量与查询需求**：海量日志和复杂下钻分析，适合ES或ClickHouse；简单计数，Redis+时序数据库即可。\n*   **实现复杂度**：网关拦截+日志服务的方案侵入性低，更易于维护。\n\n总之，核心是**异步化、管道化**，将数据采集与业务解耦，并利用合适的技术栈实现高并发下的实时统计。"}, {"num": 16, "title": "如果配置中心挂了，各个微服务应该如何保证可用性？你会设计什么降级策略？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 系统设计, 场景题, 微服务, 配置中心", "answer": "当配置中心出现故障时，为保证微服务的可用性，我会设计如下降级策略：\n\n**1. 本地配置缓存保障基础可用**\n- 每个微服务启动或配置变更时，将完整配置缓存至本地文件或内存。\n- 服务运行期间，即使配置中心不可用，也能从本地缓存读取配置，维持基本功能。这是保障高可用的第一道防线。\n\n**2. 多层次降级策略**\n- **开关与功能降级**：将非核心功能开关的默认值设置为“关闭”。当无法获取新配置时，自动降级这些非核心功能（如推荐、统计等），确保核心交易链路稳定。\n- **动态与静态降级**：对于动态配置（如限流阈值），若获取失败则沿用最后一次成功获取的值或启动时预设的保守值。\n- **配置下发与拉取结合**：除主动拉取外，增加配置变更的推送通道（如通过消息队列），在拉取通道故障时作为备选方案。\n\n**3. 快速恢复与监控**\n- 建立完善的配置中心健康检查与告警机制，确保第一时间感知故障。\n- 配置中心恢复后，各服务应能自动重新连接，并安全地同步最新配置，避免因瞬间大量请求冲垮中心。\n\n总结而言，核心思想是 **“本地缓存为主，降级为辅，主动兜底”** ，通过分层保障，确保配置中心故障不会引发级联失效，最大限度保证业务连续性。"}, {"num": 17, "title": "微信扫码登录是如何实现的？请描述一下技术方案", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题, 系统设计", "answer": "微信扫码登录基于OAuth 2.0协议实现，主要流程如下：\n\n1.  **生成二维码**：用户选择微信登录时，前端向业务后端发起请求。后端生成一个**唯一的、有时效性的状态码**，并将其与微信授权回调地址拼接，生成二维码。同时，后端将该状态码与用户临时会话（如Session）关联。\n\n2.  **轮询状态**：前端展示二维码并开始定时轮询业务后端，查询该状态码对应的授权状态（如“未扫描”、“已扫码待确认”、“已授权”、“已失效”）。\n\n3.  **用户授权**：用户使用微信App扫描二维码，微信客户端向微信服务器发起请求。用户确认授权后，微信服务器将携带**授权码**重定向至业务后端预先设置的回调地址。\n\n4.  **换取令牌**：业务后端收到回调请求，使用**AppID、AppSecret和授权码**，调用微信接口换取`access_token`和用户的`openid`（微信用户唯一标识）。\n\n5.  **获取信息与登录**：后端使用`access_token`和`openid`调用微信接口获取用户基本信息（如头像、昵称）。随后，业务后端根据`openid`在本地系统完成用户绑定或创建，最终生成一个**业务系统的登录凭证**（如JWT），返回给前端，完成登录。此时，二维码状态更新为“已授权”，前端轮询得到凭证后跳转至主页面。\n\n**核心要点**：关键在于状态码的临时性与轮询机制，以及授权码到业务令牌的安全转换。整个过程实现了**用户身份的第三方验证**，避免了密码的直接传输。"}, {"num": 18, "title": "怎么设计电商系统的订单数据同步方案（同步到数仓）？要求数据准确、性能高", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题, 系统设计题", "answer": "电商订单数据同步至数仓，需设计高准确、高性能的方案，核心在于保障数据一致性并减少对业务库的影响。建议方案如下：\n\n**1. 明确同步原则与目标**\n   *   **原则**：保证最终一致性，允许分钟级延迟，但数据必须准确、完整。\n   *   **目标**：支持T+1离线分析及准实时（如5分钟内）的运营看板需求。\n\n**2. 整体架构流程**\n   采用“**增量抽取、实时/准实时传输、幂等写入**”的流水线模式：\n   *   **数据源**：从业务数据库**从库**或**Binlog**读取，避免直接查询主库。\n   *   **抽取**：通过监听订单表`update_time`字段或解析Binlog，**增量**抓取新增及变更的订单数据。\n   *   **传输**：将数据发送至**Kafka**等消息队列进行缓冲和解耦，实现削峰填谷。\n   *   **计算与写入**：使用**Flink**等流计算引擎消费数据，进行字段清洗、维度补全（如关联商品、用户信息），最终**幂等**地写入数仓ODS层。\n\n**3. 保障数据准确性的关键设计**\n   *   **对账机制**：建立独立对账作业，每日对比业务库与数仓的订单条数、金额总额等关键指标，发现差异则触发数据修补流程。\n   *   **幂等写入**：利用订单ID或数据库更新日志的位点（如Binlog Position）作为幂等键，确保数据重复投递时不会产生重复记录。\n\n**4. 保障高性能的核心优化**\n   *   **增量抽取**：只同步变化数据，大幅降低数据吞吐量。\n   *   **批量处理**：在Flink中设置合理的窗口（如每分钟），将数据批量写入数仓，减少小文件和写入压力。\n   *   **资源隔离**：同步任务使用专用集群和独立的数仓计算队列，避免影响在线业务及分析查询。\n\n通过以上设计，可构建一个准确、高性能且可运维的订单数据同步管道，满足电商数据分析的时效性与可靠性要求。"}, {"num": 19, "title": "接口中的敏感数据（如身份证号、手机号）应该如何加密传输和存储？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 系统设计, 场景题, 安全", "answer": "接口敏感数据的安全需贯穿传输与存储全链路，核心措施如下：\n\n**传输加密：**\n1. **强制HTTPS**：使用TLS 1.2+协议加密传输通道，防止中间人窃听。\n2. **字段级加密**：对身份证号、手机号等关键字段，采用**AES-256-GCM**等对称加密算法加密后传输，密钥通过非对称加密（如RSA）协商。\n3. **签名验证**：请求参数添加时间戳、随机数并签名，防止重放攻击和篡改。\n\n**存储加密：**\n1. **算法选择**：采用国密**SM4**或**AES-256**等加密算法进行存储加密，避免明文入库。\n2. **密钥管理**：使用**KMS**（密钥管理服务）托管密钥，实现密钥轮换、权限隔离，杜绝密钥硬编码。\n3. **脱敏处理**：业务层面执行最小化原则，查询时按需返回脱敏数据（如手机号显示为138****8888）。\n\n**补充措施**：\n- 实施严格的访问控制与审计日志。\n- 定期进行安全漏洞扫描与渗透测试。\n- 合规层面遵循《个人信息保护法》与**GDPR**（如适用）要求。\n\n通过端到端加密、密钥集中管控和最小数据暴露原则，构建纵深防御体系，保障敏感数据生命周期安全。"}, {"num": 20, "title": "朋友圈点赞功能如何实现，简单说说？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 系统设计, 场景题", "answer": "朋友圈点赞功能实现主要涉及以下几点：\n\n**数据模型设计**\n通常采用“用户-动态”关系表存储点赞记录，包含用户ID、动态ID、时间戳等字段。为提升查询效率，可在动态表中增加点赞数字段，通过事务保证数据一致性。\n\n**高并发处理**\n点赞接口需考虑防重复提交（如Redis分布式锁），并采用异步更新策略：用户点赞后先写入缓存，异步批量同步到数据库，通过消息队列削峰填谷。\n\n**性能优化**\n- **缓存层**：使用Redis缓存热点动态的点赞列表和计数\n- **读写分离**：读请求走缓存，写请求先更新缓存再异步持久化\n- **分库分表**：按用户ID哈希分片，避免单表数据量过大\n\n**一致性保障**\n通过Redis的INCR原子操作保证计数准确，结合数据库最终一致性。取消点赞时需同步扣减计数并删除记录。\n\n**扩展性考虑**\n设计时应预留字段支持未来可能增加的“点赞类型”功能（如❤️、👍等），采用轻量级API设计便于客户端适配。"}, {"num": 21, "title": "短信验证码如何防止被恶意轰炸？如果有人不断请求发送验证码，你会如何限制？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 系统设计, 场景题, 安全", "answer": "针对短信验证码防轰炸，应从多层限制和异常检测入手：\n\n1. **频率限制**：  \n   - **单号码维度**：限制同一手机号在短时间内的请求次数（如每分钟1次、每小时5次），超限后触发冷却期（如1小时）。  \n   - **全局维度**：设置单日总发送上限（如每日10次），避免持续骚扰。\n\n2. **风控策略**：  \n   - **IP限制**：对同一IP的请求频率进行监控，异常IP临时封禁或加入人机验证（如图形验证码）。  \n   - **行为分析**：结合设备指纹、User-Agent等信息，识别自动化脚本特征（如请求间隔固定、无后续验证）。\n\n3. **技术实现**：  \n   - 使用Redis等缓存工具存储计数，设置滑动窗口实现精准限流。  \n   - 异步队列处理发送请求，避免高峰时压垮短信服务。\n\n4. **应急措施**：  \n   - 监控实时发送量，设定阈值自动触发熔断机制，暂停服务并告警。  \n   - 对疑似恶意账号进行黑名单标记，后续请求直接拒绝。\n\n5. **用户体验平衡**：  \n   - 正常用户超限后可提供语音验证码等替代方案，并引导自助解除限制。\n\n最终通过**频率控制+行为分析+动态熔断**组合，既防止资源滥用，又减少误伤正常用户。"}, {"num": 22, "title": "网关层如何防止接口重放攻击？如果攻击者抓取了请求包进行重放，你如何识别和拦截？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 系统设计, 场景题, API网关, 安全", "answer": "网关层防止接口重放攻击的核心在于**请求的唯一性、时效性与完整性验证**。若攻击者截获有效请求包并进行重放，可通过以下方案识别与拦截：\n\n1.  **时间戳+签名验证**：客户端在请求中加入精确到秒级的时间戳和随机数（如UUID）。网关对参数进行签名验证，并检查时间戳与服务器当前时间的偏差（如±30秒）。超出时间窗口的请求直接拒绝，确保请求的时效性。\n\n2.  **唯一请求标识（Nonce）**：客户端为每次请求生成一个唯一的随机字符串（Nonce）。网关将已验证过的Nonce暂存于Redis等高速缓存中，并设置与请求时间戳匹配的过期时间。若检测到重复Nonce，则判定为重放攻击并拦截。此方案需考虑缓存的存储与清理性能。\n\n3.  **限流与IP策略**：结合接口特性，对同一用户/IP在极短时间内发起的相同请求进行频率限制或合并处理。对于敏感操作（如支付），可强制要求二次验证（如短信验证码）。\n\n4.  **安全令牌（如JWT）时效控制**：使用短时效的JWT作为访问令牌。即使令牌被截获，其短暂的有效期也能极大降低重放风险。网关在验证时需严格校验令牌的签发时间（`iat`）和过期时间（`exp`）。\n\n综上，**最佳实践是组合方案**：例如，为每个请求分配唯一Nonce并缓存，同时校验时间戳与签名。这样既能确保请求唯一性，又能防止长时间重放，且需在安全性和系统性能（如缓存压力）之间取得平衡。"}, {"num": 23, "title": "让你设计一个购物车功能，怎么设计？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 系统设计, 场景题", "answer": "购物车系统设计需平衡实时性、可靠性与并发性能，我会从以下方面展开：\n\n**一、核心功能模块**\n1. **基本操作**：商品增删改、数量调整、清空购物车。\n2. **价格计算**：实时聚合商品单价、优惠分摊、运费计算。\n3. **持久化与状态**：支持登录用户购物车永久存储与游客临时存储。\n\n**二、数据存储设计**\n- **短期数据**：使用Redis存储会话级购物车（键：用户ID/Token，值：商品序列化数据），保证低延迟读写。\n- **长期数据**：MySQL持久化订单未支付前的购物车快照，采用异步双写保证最终一致性。\n\n**三、关键技术考量**\n1. **高并发处理**：采用缓存优先策略，通过分布式锁解决库存扣减的原子性问题。\n2. **数据一致性**：设置购物车TTL定期清理，结合消息队列确保缓存与数据库的异步同步。\n3. **扩展性设计**：商品属性（如SKU）、优惠规则等采用可扩展的JSON字段存储，便于业务迭代。\n\n**四、接口设计要点**\n- 提供标准RESTful API，支持批量操作。\n- 关键操作（如库存校验）采用乐观锁版本控制。\n- 返回数据包含实时价格与失效商品标记，前端无需二次计算。\n\n**五、异常场景预案**\n- 商品下架或价格变更时标记提醒。\n- 分布式环境下采用最终一致性方案避免阻塞主流程。\n\n通过多级存储和异步机制，可在保障用户体验的同时支撑高并发场景。"}, {"num": 24, "title": "设计一个实时数据同步系统，将 MySQL 数据实时同步到数据仓库？", "difficulty": "中等", "difficulty_emoji": "🟡", "tags": "后端, 场景题, 系统设计题", "answer": "**实时数据同步系统（MySQL → 数据仓库）设计核心**  \n\n**1. 架构分层**  \n- **数据采集层**：通过 MySQL Binlog（如 Canal、Debezium）实时捕获增量变更，避免轮询源库。  \n- **处理与缓冲层**：使用 Kafka 暂存变更数据，解耦上下游，保障吞吐量和容错。  \n- **数据仓库同步层**：通过 Flink/Spark Streaming 消费 Kafka，完成数据清洗、格式转换，并批量写入数据仓库（如 ClickHouse、Hive）。  \n\n**2. 关键设计要点**  \n- **一致性保障**：采用\"At Least Once\"语义，通过 Kafka Offset 管理、写入幂等性（如唯一键去重）避免数据丢失或重复。  \n- **实时性优化**：Binlog 实时采集 + 流式计算，延迟可控制在秒级；批量写入仓库前进行微批处理，降低仓库压力。  \n- **容错与监控**：记录消费进度，故障后可从断点恢复；监控同步延迟、数据量，并设置报警。  \n\n**3. 简化流程**  \nMySQL Binlog → Canal → Kafka → Flink 计算 → 数据仓库  \n\n该设计兼顾实时性、可靠性与可扩展性，适用于大规模数据同步场景。"}, {"num": 25, "title": "如何设计一个灰度发布系统？如何控制流量逐步切换到新版本？", "difficulty": "困难", "difficulty_emoji": "🔴", "tags": "后端, 系统设计, 场景题", "answer": "灰度发布系统通过逐步将新版本部署到生产环境，降低全量上线的风险，确保系统稳定性。设计时应聚焦版本管理、流量控制、监控和回滚机制。\n\n**系统设计核心**：采用微服务架构，通过API网关或服务网格实现流量路由。关键组件包括：\n- **版本管理**：维护新旧版本信息，支持快速切换。\n- **流量路由层**：根据规则将请求导向不同版本。\n- **监控告警**：实时跟踪错误率、延迟等指标，异常时自动触发回滚。\n- **回滚机制**：预设回滚策略，确保问题版本能迅速恢复。\n\n**流量逐步切换策略**：\n1. **金丝雀发布**：初始阶段将少量流量（如1%）导向新版本，监控稳定后逐步提升至5%、10%，直至100%。\n2. **用户分组**：基于用户ID、地域或设备特征分组，优先对内部员工或特定群体开放新版本。\n3. **特征标志**：通过动态配置开关控制"}, {"num": 26, "title": "微服务架构中，如何设计一个配置中心？配置变更后如何实时通知各个服务？", "difficulty": "困难", "difficulty_emoji": "🔴", "tags": "后端, 系统设计, 场景题, 配置中心", "answer": "**配置中心设计要点：**\n\n1. **统一存储与版本管理**：使用独立数据库或分布式存储（如MySQL、ZooKeeper、etcd）集中管理配置，并为每次变更生成唯一版本号，支持历史回溯。\n2. **多环境与多维度支持**：通过命名空间区分环境（开发/生产），支持按应用、集群、实例粒度配置。\n3. **管理界面与API**：提供控制台实现可视化编辑、发布审核、权限控制，同时开放RESTful API供程序化操作。\n4. **高可用与容灾**：采用集群化部署，配置本地缓存，确保配置中心故障时服务仍能读取最后生效的配置。\n\n**配置变更实时通知方案：**\n\n1. **长轮询（Long Polling）**：客户端定期向配置中心发起请求，若配置无变更，服务端hold住请求直至超时或变更发生，再即时返回新配置。避免频繁无效请求。\n2. **消息队列广播**：配置中心发布变更事件至消息队列（如Kafka），各服务订阅对应Topic，实现高效异步通知。\n3. **客户端缓存与刷新**：服务本地缓存配置，并提供手动刷新接口或定时拉取机制作为兜底。\n\n**生产环境建议**：结合配置差异对比、灰度发布、熔断降级机制，确保变更安全可控。"}, {"num": 27, "title": "电商系统中，用户下单后如何保证订单、库存、支付三个服务的数据一致性？请设计一个分布式事务方案", "difficulty": "困难", "difficulty_emoji": "🔴", "tags": "后端, 系统设计, 场景题, 电商", "answer": "### **问题分析**\n电商下单涉及订单创建、库存扣减、支付处理三个独立服务，数据分布在不同数据库。直接调用可能因部分失败导致数据不一致（如订单已创建但库存未扣减）。\n\n### **设计思路**\n采用 **Saga模式（基于事件编排的最终一致性方案）**，将下单过程拆分为一系列本地事务，并通过消息队列进行异步协作。\n\n### **技术实现方案**\n1.  **分解流程**：\n    *   T1: 订单服务创建“待支付”订单，并发布`OrderCreated`事件。\n    *   T2: 库存服务监听事件，执行本地扣减，成功则发布`InventoryReserved`事件。\n    *   T3: 支付服务监听事件，调用支付网关，成功则发布`PaymentSuccess`事件。\n    *   T4: 订单服务监听事件，将订单状态更新为“已支付”。\n\n2.  **核心组件**：\n    *   **消息队列（如RocketMQ/Kafka）**：作为事件总线，保证消息可靠投递。\n    *   **本地事务表**：每个服务在执行本地操作时，将事件和状态记录在本地数据库，保证操作与发消息的原子性。\n    *   **Saga协调器**：可选，用于集中管理流程状态和触发补偿。\n\n3.  **容错与补偿机制**：\n    *   **重试**：消费者处理失败时进行指数退避重试。\n    *   **补偿事务**：关键步骤（如库存扣减）需提供对应的补偿操作（如回滚库存）。当流程超时或明确失败时，逆序触发已完成步骤的补偿事务，实现数据回滚。\n    *   **状态查询**：通过全局业务ID（如订单号）进行状态对账，确保最终一致。\n\n### **方案优势**\n*   **高可用**：服务间异步解耦，避免长事务和分布式锁，系统吞吐量高。\n*   **最终一致**：在秒级延迟内保证数据强一致，满足绝大多数电商场景。\n*   **清晰可控**：流程明确，便于排查问题和运维。"}, {"num": 28, "title": "让你实现一个分布式单例对象，如何实现？", "difficulty": "困难", "difficulty_emoji": "🔴", "tags": "后端, 系统设计, 场景题", "answer": "在分布式系统中实现单例对象，核心是确保在多个节点或服务实例中只有一个实例被创建和使用。常见实现方式包括基于分布式锁、共享存储或协调服务。\n\n首先，采用分布式锁机制。例如，使用Redis的SETNX命令或Redlock算法实现锁，节点在初始化实例前竞争获取锁，成功者创建实例并注册，其他节点等待或重试。这种方式高效，但需处理锁的超时和续约以防止死锁。\n\n其次，利用共享存储如数据库或配置中心。通过数据库唯一约束或ZooKeeper的临时节点来控制实例创建，ZooKeeper的顺序节点和Watch机制能自动处理故障转移，提供强一致性。\n\n实现时需考虑"}, {"num": 29, "title": "让你实现一个订单超时取消功能，怎么设计？", "difficulty": "困难", "difficulty_emoji": "🔴", "tags": "后端, 系统设计, 场景题", "answer": "订单超时取消的核心难点在于高并发下可靠触发，且需平衡实时性与系统开销。主流方案有二：**定时任务轮询**和**延迟消息队列**。\n\n**方案一：定时任务（适用于低并发初期）**\n定时扫描数据库中状态为“待支付”且创建时间超时的订单进行批量取消。实现简单，但**实时性差**，频繁扫描会带来数据库压力，且时间精度依赖任务间隔。\n\n**方案二：延迟消息（推荐生产方案）**\n1. **技术选型**：利用具备延迟功能的中间件，如 **RocketMQ** 的延迟消息、**RabbitMQ` 的 TTL + 死信队列、或 **Redisson** 的延迟队列。\n2. **核心流程**：订单创建成功时，向消息队列发送一条**延迟消息**，内容为订单ID。消费者收到消息后，**查询订单状态**，若仍为待支付，则执行取消逻辑，释放库存。\n3. **关键保障**：\n   * **可靠性**：需处理消费者**幂等**，防止重复取消。\n   * **兜底机制**：配合**定时任务**，作为延迟消息失败（如消费者宕机）的补充，确保最终一致性。\n   * **业务解耦**：取消动作应异步化，通过**状态机**严格流转，避免直接删除订单。\n   * **监控告警**：监控队列堆积与取消失败率，及时报警。\n\n**总结**：首选延迟消息方案，具备高实时性与扩展性。同时，必须设计**兜底补偿**（如定时扫描）与**监控体系**，以构建健壮、可靠的订单生命周期管理系统。"}, {"num": 30, "title": "让你设计一个 API 网关，需要考虑哪些核心功能？如何实现动态路由和限流？", "difficulty": "困难", "difficulty_emoji": "🔴", "tags": "后端, 系统设计, 场景题, API网关, 微服务, 限流", "answer": "API网关作为微服务架构的统一入口，核心设计需考虑以下功能：\n1.  **路由转发**：将请求动态路由至下游服务。\n2.  **安全认证**：统一处理身份验证（如JWT）、授权及流量加密。\n3.  **限流熔断**：保障系统稳定，防止服务过载或雪崩。\n4.  **监控日志**：集中收集、分析请求日志与指标。\n5.  **协议转换**：支持HTTP、gRPC、WebSocket等协议的转换与适配。\n\n**动态路由实现**：核心是路由规则与目标服务的映射管理。可将路由规则（如域名、路径前缀、Header匹配规则）存储在配置中心（如Nacos、Apollo）。网关启动或规则变更时，监听配置中心动态拉取并缓存，通过`Predicate`（如`Path=/order/**`）匹配请求，再经`Filter`链处理后，使用`LoadBalancer`从服务注册中心（如Consul）获取健康实例并转发，从而实现无需重启的动态路由与灰度发布。\n\n**限流实现**：常用令牌桶或滑动窗口算法。在分布式环境下，需借助Redis实现集中式、原子性的计数。例如，针对特定API或用户，在网关层拦截请求，使用Redis的`INCR`或`Lua`脚本实现滑动窗口计数，若超过阈值则快速返回错误。可按用户ID、API路径等多维度配置精细化限流策略，并通过配置中心实现规则的动态下发与更新。"}, {"num": 31, "title": "让你设计一个 HashMap ，怎么设计？", "difficulty": "困难", "difficulty_emoji": "🔴", "tags": "后端, 系统设计, 场景题", "answer": "设计一个 HashMap 需要考虑以下核心要素：\n\n1. **底层数据结构**：通常采用“数组+链表+红黑树”（如 Java 8+）。数组作为桶（Bucket），每个桶存放链表头节点或红黑树根节点。当链表长度超过阈值（如8）且数组长度≥64时，链表会转换为红黑树，以提升查询效率。\n\n2. **哈希函数**：通过 `hash(key)` 计算键的哈希值，并与数组长度取模确定存储位置。Java 中通过 `(n-1) & hash` 优化取模运算，要求数组长度为2的幂。同时对 hashCode 进行高位扰动，减少冲突。\n\n3. **冲突解决**：采用链地址法。哈希冲突时，键值对以链表或红黑树形式存储在同一桶中。查询时需遍历链表或红黑树，通过 `equals()` 方法确认键。\n\n4. **扩容机制**：当负载因子（元素数/数组容量）超过阈值（如0.75）时触发扩容。创建2倍新数组，重新哈希所有元素（rehash）。Java 8优化了扩容过程，通过高位判断元素新位置，避免全部重哈希。\n\n5. **性能与线程安全**：非线程安全。在并发场景需使用 `ConcurrentHashMap`（如CAS+synchronized锁桶）。优化点包括预设容量、合理负载因子、以及利用不可变对象作为键（保证 hashCode 稳定）。\n\n整体设计需权衡时间与空间复杂度，保证 put、get 操作平均 O(1) 性能。"}, {"num": 32, "title": "让你设计一个文件上传系统，怎么设计？", "difficulty": "困难", "difficulty_emoji": "🔴", "tags": "后端, 系统设计, 场景题", "answer": "文件上传系统设计需考虑可靠性、扩展性与安全性。核心组件如下：\n\n1. **前端交互**：支持拖拽上传、进度显示，并通过分片技术处理大文件，提升稳定性。\n\n2. **后端核心**：\n   - 接入层：采用Nginx或负载均衡接收请求，处理协议转换。\n   - 业务层：校验文件类型、大小，生成唯一ID，异步写入存储系统（如MinIO、S3），避免阻塞。\n   - 存储层：小文件直接存入分布式文件系统；大文件分块上传，支持断点续传。\n\n3. **数据库设计**：记录文件元数据（ID、路径、大小、上传者），建议用MySQL或MongoDB，确保可查询。\n\n4. **安全与优化**：\n   - 病毒扫描集成。\n   - 访问权限控制（如预签名URL）。\n   - CDN加速静态资源分发。\n\n5. **扩展考虑**：\n   - 服务无状态设计，方便水平扩展。\n   - 监控上传成功率、延迟等指标。\n\n总结：系统需平衡性能与安全，通过异步处理、分片存储和完善的元数据管理，实现高效可靠的文件上传。"}, {"num": 33, "title": "让你设计一个消息队列，怎么设计？", "difficulty": "困难", "difficulty_emoji": "🔴", "tags": "后端, 系统设计, 场景题", "answer": "设计一个消息队列，核心目标是实现异步解耦、流量削峰和数据通信。我的设计思路如下：\n\n**1. 核心组件与流程**\n系统包含三个角色：生产者、消费者和代理（Broker）。生产者将消息发送至Broker，Broker负责持久化存储并转发给订阅的消费者。通信协议可采用TCP自定义协议，以保证高效。\n\n**2. 高可用与可靠性**\n*   **存储**：采用顺序写盘的Segment文件作为底层存储，通过内存映射（mmap）提升IO性能。\n*   **可靠性**：采用主从副本同步机制，确保数据不丢失。消息投递后，需生产者确认（ACK）才视为成功。为防止消息丢失，消费者需在处理完成后手动提交偏移量（Offset）。\n\n**3. 扩展性与吞吐量**\n*   **分区（Partition）**：一个Topic可划分为多个分区，分区是并行处理和负载均衡的基本单元，不同分区可分布在不同机器上。\n*   **消费模型**：支持集群消费（同一分组内消费者均摊消息）和广播消费（每个消费者接收全部消息）。\n\n**4. 监控与运维**\n提供管理接口，用于监控队列积压、集群健康度、消费延迟等关键指标。支持动态扩缩容，以及基于规则的消息轨迹追踪。\n\n**总结**：通过“分区+副本”机制保障高可用与水平扩展，以顺序写和异步消费保障高吞吐，结合完善的监控和运维体系，构建一个可靠、高性能的基础组件。"}, {"num": 34, "title": "让你设计一个线程池，怎么设计？", "difficulty": "困难", "difficulty_emoji": "🔴", "tags": "后端, 系统设计, 场景题", "answer": "设计线程池的核心是通过复用线程来管理并发任务，提升系统资源利用率。我会从以下几个关键方面进行设计：\n\n首先，定义核心组件：\n1.  **任务队列**：用于存放待执行的任务，通常使用阻塞队列（如`LinkedBlockingQueue`）实现。可配置有界队列来防止资源耗尽。\n2.  **工作线程**：一组预先创建的线程，它们不断从任务队列中获取任务并执行。\n3.  **线程管理模块**：负责工作线程的创建、维护和销毁。\n\n其次，设定关键参数：\n*   **核心线程数**：即使空闲也保留的线程数量。\n*   **最大线程数**：允许创建的最大线程数，用于应对任务高峰。\n*   **空闲线程存活时间**：超过核心数量的线程空闲后的存活时长。\n*   **任务拒绝策略**：当队列已满且线程数达到最大值时，如何处理新任务（如抛出异常、丢弃、由调用线程执行等）。\n\n然后，实现任务提交与执行流程：\n1.  新任务提交时，若当前运行线程数小于核心线程数，则创建新核心线程执行任务。\n2.  否则，尝试将任务放入队列。\n3.  若队列已满，且当前线程数小于最大线程数，则创建非核心线程执行任务。\n4.  若以上条件均不满足，则执行预设的拒绝策略。\n\n最后，设计生命周期管理：提供启动（`start`）、平滑关闭（`shutdown`，等待已提交任务完成）和立即关闭（`shutdownNow`，尝试中断正在执行的任务）方法，确保资源安全释放。"}];